# scrape-startup-web

## Purpose

This project builds a **library of all startups launched in the last 24 months** and, for each one, stores an **exact replica of its website** — a full offline mirror with complete assets — in this repository.

## How it works

For every startup in the library, this repository contains a subfolder that is a faithful, self-contained copy of that startup's public website:

- **One subfolder per startup** — named after the startup (e.g. `acme-ai/`).
- **Full assets** — HTML, CSS, JavaScript, images, fonts, and any other static resources required to render the site exactly as it appears online.
- **Exact replica** — each subfolder can be opened and browsed locally, reproducing the original site's look and behavior without network access to the original domain.

The goal is a durable, versioned archive: as startups launch, pivot, or shut down, their launch-time web presence remains preserved and reproducible here.

## Selecting and capturing startups

- **Prefer mostly static marketing sites.** Client-side-rendered apps (React/SPA) render little in initial HTML and are expensive to mirror faithfully. YC batch pages are a good source of simple launch sites; see `search_methods.md` for funding-discovery feeds.
- **Screen "expensive-look" on flushed full-page captures, not HTML size/framework** (D26 lesson): judge any serious contender with a scroll-sweep-flushed fullPage screenshot on a fixed prompt — a single-shot fullPage on a backgrounded tab systematically under-scores lazy-load sites (Parameter graded 3/10 unflushed, 9/10 flushed).
- **Static sites:** capture with `wget --mirror --page-requisites --convert-links --adjust-extension -e robots=off`, then rewrite asset URLs to relative paths so the subfolder browses offline with no server.
- **Respect robots/ToS posture:** check `robots.txt` and terms before mirroring; prefer sites that permit fetching.
- **Exception (Lemma):** JS-heavy app-router sites need a byte-identical HTML + full API-path mapping; `lemma/` uses `serve_replica.py` (see its header) because `wget` would come back near-empty.

## Repository layout

```
<startup-name>/
  index.html        # entry point of the mirrored site
  assets/           # css, js, images, fonts, ...
  ...
```

Each subfolder is a complete, standalone snapshot of that startup's website at the time it was captured.

## Oversized assets (S3 pointer convention)

GitHub hard-caps blobs at 100 MB. Any mirrored asset that large is stored in
the GCS bucket `scrape-startup-web-assets` and the repo file at the same
relative path is a **pointer file**: first line `S3PTR <bucket-relative-key>`
plus optional `#` comment lines. This is automatic on both sides:

- **Capture**: `mirror_site.py` routes bodies >= `s3ptr.S3_MAX_BYTES` (100 MB)
  through `s3_upload()` into GCS and writes the pointer file — no per-site
  special-casing.
- **Serve**: `serve_replica.py` detects the `S3PTR ` magic and 302s to a
  freshly SigV4-presigned URL. Presigned (not public): the org's
  `domainRestrictedSharing` forbids `allUsers` GCS reads; signed URLs are
  minted per request via a stdlib-only signer (`.tmp_tools/s3ptr.py`, creds
  from env `GLM_S3_ACCESS_ID`/`GLM_S3_SECRET`, bucket env `S3_ASSET_BUCKET`,
  default `scrape-startup-web-assets`).
- Example: `happyrobot/HappyRobot_HeroLoop_v01_up.mp4` (105.8 MB -> GCS object
  110 914 261 B, `video/mp4`); served on 8922 as 302 -> presigned URL (verified).



## Startups in the library (current)

| Lemma | `lemma/` | Next.js app-router exception: byte-identical HTML + full API-path mapping via `mirror_site.py`, served via `serve_replica.py`. |
| Tsenta | `tsenta/` | |
| Nex | `nex/` | Framer site; see `search_methods.md` Framer pattern. |
| Conifer | `conifer/` | Next.js site; hydration/mirror patterns in `search_methods.md`. |
| Twin1 | `twin1/` | Turbopack site. |
| Forward | `forward/` | Astro site (useforward.co pattern). |
| Multiplier | `multiplier/` | Vite/Turbopack v2 site. |
| Ellis | `ellis/` | Vite-built SPA (ellis.ai pattern). |
| Uplane | `uplane/` | Framer site; SVG `<use>` fragment-URL rewrite fix. |
| OneCLI | `onecli/` | React App Router site, mirrored via `mirror_site.py` (220 files), served via `serve_replica.py` serve-time ref restoration. Full-page parity with live: identical height (7348px), nav 25/25 links, byte-identical SSR DOM modulo intended rewrites, `uncaught:[]` (React #418 eliminated), 0 broken images, videos streamed from origin. |
| Caution | `caution/` | Hand-rolled static SSR site (caution.co, YC S26, verifiable compute): 507/507 internal refs return HTTP 200 through the replica, byte-identical DOM in clean-room CDP audit (h1/h2=10/imgs=70/broken=0/height 7517), 36/36 headings match in David's Chrome, vision-verified hero/nav/workflow parity, video plays locally (webm 1920×1080, mp4 18.6 MB). |
| Magma | `magma/` | Next.js App Router site (magmahq.ai, YC S26): runtime-injected Basalt analytics + c15t consent scripts vendored as documented no-op stubs; 5/5 pages byte-equal DOM in clean-room CDP audit (home h1/h2=7/imgs=6/broken=0/height 5477), static pages byte-identical PNGs (MD5-equal), animated hero band = inherent frame variance, no failures/errors either side. |
| Last Accounting Company | `last-accounting-company/` | Hand-rolled no-framework static SSR site (lastaccountingcompany.com, YC S26, Helsinki, "agent-native accounting firm"): robots.txt explicitly welcomes all crawlers incl. AI bots, 34 files / 956 KB, 9/9 sitemap pages mirrored. In-page Firebase AppCheck module (gstatic CDN, mirrored byte-identically) gates the waitlist form — Google origin-attestation only succeeds on the registered domain; replicated as-is, the origin-enrollment ReCAPTCHA failure is environmental (echoes on every non-enrolled host), documented in `search_methods.md`. Homepage servable bytes == live bytes (114 315 B); clean-room CDP audit live vs replica: DOM identical (h1/h2/h3/nav/broken=0/height), article page identical too, `uncaught:[]` both sides; hero demo-cards rotate numbers via inline JS (identical scripts both sides); vision compare via browser relay + OMP computer use both confirm identical hero rendering in real Chrome. |
| General Legal | `general-legal/` | Webflow site (general.legal, YC W26, AI-native law firm): 797 files / 58 MB, 124 HTML pages, **115/115 sitemap URLs covered** via new `--sitemap auto` seeding (zero-inbound orphans `/old-home`, `/compare/harvey`, `/features/contract-review`, `/ainlc-2026` are BFS-unreachable); served via `serve_replica.py` on 8912. Clean-room CDP audit home + article: byte-identical DOM both sides (71/71 imgs 0 broken, height 9391, scripts 37, `failures:[]` `uncaught:[]`); article screenshots md5-identical; home pixel diff 0.186% < replica self-jitter 0.207% = hero animation frames, not drift. Live's own dead refs (investors logos 404, Webflow plugins placeholder 403-to-all) mirrored faithfully as broken; Intellimize + Osano external runtimes documented as environmental (not stubbed); vision compare via relay CDP + computer-use Safari windows read hero/banner verbatim-identical. |
| Hilstart | `hilstart/` | Nuxt 3 SSR site (hilstart.io, YC S26, "Autonomous Hardware Testing"): 37 files fully self-hosted, BFS-only crawl (sitemap.xml returns the Nuxt SPA fallback shell — no XML seeding), CF Email Protection decrypted client-side by mirrored `email-decode.min.js`; served via `serve_replica.py` on 8913. Clean-room CDP audit live vs replica: title/h1/imgs 6/6 broken 0/bodyH 5874/docW 1440/textLen 4323/scripts 4/css 3 identical, `errs:[]` both; full-page pixel diff 0.133% (hero band only, < D14 self-jitter floor); relay vision + innerText specs byte-identical; OMP computer-use AX trees content-identical. RUM beacon `static.cloudflareinsights.com` external per precedent. |
| Kebra | `kebra/` | Next.js Turbopack site (kebra.com, YC S26, "Making the field queryable"): 64 files fully self-hosted incl. both hero mp4s + poster, real sitemap.xml seeded (`--sitemap auto`, 5 HTML pages), served via `serve_replica.py` on 8914. Served bytes == live bytes (66 983 == 66 983, `?dpl=` refs restored at serve time); clean-room CDP audit: title/h1/imgs 16/16 broken 0/bodyH 7189/docW 1440/textLen 1714/scripts 27 identical, `errs:[]` both; innerText byte-identical (1 737 B); pixel band-map: every differing row inside the two animated video blocks (hero + s3 demo), all ~13k static rows byte-identical (video codec-state delta is inherent); relay vision + cua-driver computer-use AX trees content-identical (110==110 elements, sole divergence = address-bar `kebra.com` vs `127.0.0.1:8914`). GA4 + Google Appointments embed external per precedent. |

| River | `river/` | Hand-rolled static SSR site (river.ai, raised $1.1B across Series Seed + Series A, announced 2026-08-11): 42 files (41 mirrored + `.origin`), 9/9 sitemap URLs, served via `serve_replica.py` on 8915. Query-versioned assets (`styles.css?v=49` etc.) restored at serve time via new generic `__v-` rule — `styles.css?v=49` byte-identical (74 745 B == live); resolved-URL parity bar: 7 syntactic byte diffs (abs/relative forms + 2 unrecoverable fragments) all resolve identically in-browser; 214-ref sweep, all real assets 200; relay vision + computer-use window leg content-identical (hero headline + subhead + nav API/Blog/Careers), sole divergence = address bar (`river.ai` vs `127.0.0.1:8915`); CF Web Analytics beacon runtime injection + hero-headline rotator = documented non-parity runtime classes. |
| Wispr | `wispr/` | Webflow site (wisprflow.ai, YC S26, AI voice-memo productivity app): 326 HTML pages / 127 MB, all three Webflow project CDN prefixes (`682f84b3…`/`682fa127…`/`6838259b…`) stored under URL path, served via `serve_replica.py` on 8916. Webflow asset-host recipe documented — `--asset-hosts` is host-only (`cdn.prod.website-files.com` suffices, project IDs are dead entries); BFS covered 308/308 sitemap URLs. Real gap found + fixed 2026-08-23: Rive animations embed `.riv` via `data-rive-url` (was missing from `LinkFinder.ATTRS` → 11 refs stayed absolute CDN); added to `ATTRS` + targeted in-place repair (7 `.riv` files, ~2.1 MB, fetched into mirror, refs rewritten pagedir-relative, `&`/space-decode identical to `url_path_key`); replica serves all rewritten refs 200. fswp3 whole-site sweep: 326 pages / 0 serve bugs; residual 3 = faithful live-404 trio (2× Webflow plugins placeholder svg + 1 workflow link); commented `<video poster>` refs are dead markup (inside `<!-- -->`). Visible-text parity spot-check live vs replica: home 16976==16976 chars, features 5198==5198 bytes-identical. Leftover external dep: `unpkg.com/@rive-app/canvas` runtime only (third-party, unvendored per precedent). See `search_methods.md` Webflow section. |
| Herdr | `herdr/` | Astro docs+blog site (herdr.dev, YC F26, "command line for AI agents"): **134 HTML pages / 3 locale trees** (`/en/`, `/ja/`, `/zh-cn/` = docs + blog), sitemap 72/72, served via `serve_replica.py` on 8917. Repair 2026-08-23: BFS `depth=2` cut `/docs/preview/{10 names}/*` depth-3 siblings — 13 live-200 crawl-gaps + 3 faithful CF Email Protection residuals; targeted 30-page fetch (10 × 3 locales) → sweep residual exactly 3 (zero-serve-defect bar). **Pagefind search mirrored whole-tree**: `/pagefind/` is JS-string-loaded runtime data, invisible to the HTML-attr crawler — gzip'd `pf_meta` hash enumeration, 1171 files / 7856 KB (fragment 1056 + index 106 + filter 3 + root 6), classed by Content-Type (CF Pages 200+HTML-fallback on missing), `zh-cn_` full-prefix regex fix; offline search works. Text-level parity live vs replica byte-equal (home + en/ja preview + blog). |
| Veeda | `veeda/` | Hand-rolled static one-pager (veeda.ai, physical-AI world models, **$90M+ seed Aug 2026** Khosla+Radical, 2-source verified): **28 files + `.origin`** — zero JS, inline CSS + Google Fonts css2 vendored (21 `*.woff2` under `veeda/s/`, all `url(../s/…)` relative), served via `serve_replica.py` on 8918. **New crawler class fixed 2026-08-23**: extensionless CSS asset (css2 URL has no `.css`) escaped the `.css/.js/.mjs` gate → woff2 refs never fetched + verify scanned it vacuously; `mirror_site.py` now sniffs CSS (`_looks_like_css`) in `crawl_asset` AND `verify_no_missing_chunks`; fresh crawl → **"no missing referenced assets"**. Sweep `pages=2 refs=19 residual_uniq=0`; visible-text parity MATCH (2842B==2842B); relay DOM identical (title/h1/nav, 3 webfonts loaded, address bar sole divergence); **viewport screenshot byte-identical (MD5 equal)**, vision-model transcription verbatim match; no scripts/analytics either side, Ashby careers link external. |
| Starcloud | `starcloud/` | Webflow site (starcloud.com, space-born AI data centers, **$250M Series A extension @ $2.3B valuation Aug 2026** — FT/Reuters + TechCrunch + PitchBook verified, a16z + NVIDIA + YC + Founders Fund; 547M total, "2027" lift-off, CTO ex-OpenAI #1): **179 files** on disk (164 Webflow-CDN assets under the two project prefixes + 13 HTML pages + gsap.min.js + `.origin`), served via `serve_replica.py` on 8920. Sweep `pages=13 refs=793 residual_uniq=1` (the 1 residual = faithful live-404 placeholder) — zero serve defects; visible-text parity MATCH all 13. **Two durable crawler/serve fixes 2026-08-23**: (1) SRI serve-strip in `serve_replica.py` (`integrity`/`crossorigin` attrs stripped at serve time — Webflow's own-SRI stylesheet otherwise dropped on local origin); (2) **srcset space-filename bug** — Webflow upload names contain literal spaces, old `split()` mangled candidates into `…/index.html Rest…-p-500.webp 500w` (browser fell back +404 → 8192px raw src; naturalWidth 8192 vs live 1919 = detector); fixed via shared descriptor-anchored `srcset_candidates()` in `mirror_site.py` (durable for future crawls) + in-place repair of 68 attributes/178 candidates (0 dropped, 178/178 resolve on disk). Hero pixel-parity on pinned same-scroll captures: 24 band means Δ≤0.30/255, downscaled per-pixel max Δ 7.33/441, zero px >12 — content-identical (live base encode vs rep `-p-2000` variant only). |
| Corma | `corma/` | Framer v2 site (corma.ai, foundation models for defensive cybersecurity, **$60M seed Aug 2026** Sequoia + Khosla + Coatue, Fortune + SiliconANGLE verified): 154 files on disk, 4/4 sitemap pages, served via `serve_replica.py` on 8919. Crawl clean (`no missing referenced assets`; 31 mjs / 68 woff2 / 49 images self-hosted); sweep `pages=4 refs=291 residual_uniq=0`; visible-text parity MATCH all 4 pages (5112/44378/5376/9751 B); relay DOM identical live vs replica (title/h1/h1 font A2 Gothic 82px/720px, fonts loaded + same 8 families, bodyH 7137==7137, docW 1512, innerText 1984 B equal, nav equal, 15 imgs); full-frame pixel diff **0.020%** — 109 px isolated to animated logo/starfield grain (Framer animation phase, inherent), vision-verbatim nav+h1+subhead+buttons identical both sides. |
| Callosum | `callosum/` | Next.js Turbopack + Sanity CMS site (callosum.com, AI workload/inference-optimization infrastructure, **$100M seed Aug 20 2026, Atomico-led** — TechCrunch funding wire): **186 files on disk** (38 HTML pages sitemap-complete incl. `/blog/*/download/` PDF + Sanity images + `?dpl=` chunk twins), served via `serve_replica.py` on 8921. Sweep `pages=38 refs=2191 residual_uniq=0` — zero serve defects; DOM-fact parity live vs replica constant across page classes (title/h1/h2/imgs-loaded/nav/bodyLen; bodyLen exactly 2550==2550 home, 1176==1176 blog); zero in-browser 404s; React #418 hydration noise fires identically on live home (same chunk/stack) — faithful. **Durable fixes 2026-08-23**: (1) restored module-level `INLINE_STYLE_RE` (D22 commit had deleted it → every page-rewrite NameError), (2) verifier `:`-skip for Turbopack `:case` string-literal false positive, (3) **lazy-chunk + flight-payload gap closed**: HTML-attr sweeper can't see JS `import()` deps or flight-payload paths — served replica crashed with `ChunkLoadError` on two lazy chunks (`3f74337122e2b880`, `6b1c3372903d08dd`), plus `/_vercel/speed-insights/script.js` and the root-absolute hero mp4 from the flight payload; fetched from live, CDP Network-interception pass (7 pages) iterated to zero 404s, (4) Sanity Live CORS toast surgically silenced in the mirrored client (keep `console.error` + `handleError` registration, drop toast calls; live site on its allowlisted origin never shows it) — post-patch bodyLen parity exact. |
| HappyRobot | `happyrobot/` | Next.js App Router site (happyrobot.ai, **$150M Series C @ $1.2B post Aug 2026**, Prysm Capital + Eurazeo lead; "enterprise superintelligence" AI call agents): 1288 files / 302 HTML pages incl. `/es/` twins, BunnyCDN image host, sitemapindex recursion (pages/posts/hub/customer-stories), served via `serve_replica.py` on 8922. Hero mp4 = 105.8 MB -> **S3-pointer asset** (`S3PTR` pointer file, GCS object, 302-presign serve); sweep 0 residual on home/`/es/`/`/pricing`. |
| Palisade | `palisade/` | Hand-rolled static one-pager (palisade-ai.com, **YC S26 June 2026**, kernel-level OS vuln detection + fleet mitigation AI, $500k standard deal, 3-source verified): no robots.txt (fetch-all), 24 files / 1.7 MB, Google Fonts css2 + 14 woff2 vendored, served via `serve_replica.py` on 8923. Sweep `pages=2 refs=32 residual_uniq=0` — zero serve defects; DOM-fact parity byte-identical live vs replica (title / h1=1 / h2=8 / h3=12 / imgs 6 / broken 0 / nav / bodyH 7835==7835 / docW 1440 / textLen 7261 / scripts 1 / styles 2 / `errs:[]`); pixel diff live-vs-replica mean **0.048/3ch** (45 rows, demo band only) vs live-vs-live self-diff **2.37/3ch** (536 rows) — residual = the site's own JS feed/toast/caret demo animation (`.feed-row` `rowin` + `.caret` `blink`), inherent not replica drift; pinned-frozen vision parity reads identical "expensive/polished" scene both sides, YC S26 badge present. |
| Trident | `trident/` | Next.js App Router site (tridentsecurity.io, **YC S26, AI pentesting**): 142 files / 28 HTML pages (`--sitemap auto` seeded), clean SSR footprint, permissive robots, served via `serve_replica.py` on 8924. Served bytes == live bytes minus intended rewrites (134 diff hunks, all URL rewrites + `crossorigin=""` strip); 28/28 sitemap pages served; all 55 inlined asset URLs 200; scene JSONs + hero/section runtime byte-verified. **Unicorn runtime vendored** (`trident/unicorn/unicornStudio.umd.js`, jsdelivr v2.2.6 pinned) + chunk patched to load it locally — scene frames are core "expensive-look" content, offline fidelity required it (documented deviation). **Per-origin zoom pitfall**: tiles render white at 127.0.0.1 (browser zoom dpr 1.6 — explicit white clear, shader never runs) vs dark+painted at `localhost:8924` (dpr 2.0) — verified environment artifact, not a mirror defect; matched-pair pixel stats + vision parity at the 100%-zoom origin.
| Parameter | `parameter/` | Next.js Turbopack site (parameter.ai, **YC W26, AI pentesting** — "Security at the speed of development"): 287 files / 14 MB, **34 HTML pages** sitemap-complete + fully vendored `parameter-assets/` (120 entries: JS chunks, woff2, avif), `.origin` provenance file, served via `serve_replica.py` on **8925**. Sweep `pages=34 refs=3442 residual_uniq=0` — zero serve defects; DOM-fact parity live vs replica (h1 "Security at the speed of development" identical, bodyH identical, same imgs count); **innerText line-set parity**: live 5126 B ⊂ replica 5244 B — the only delta is the cookie-consent banner, which renders **only on the fresh localhost origin** (per-origin consent storage; live origin has stored consent), not a mirror gap; vision 8/8. Variant-serving site (`data-dpl-id`) — live-vs-live reloads differ by consent/variant state (same class as River's headline rotator); Turbopack `?dpl=` chunk twins restored at serve per the kebra/callosum precedent. |
| Fabraix | `fabraix/` | Vite SPA site (fabraix.com, **YC S26, "Adversarial Verification for AI Agents"** — agents that hack your AI before attackers do): 13 HTML pages (incl. missed-by-crawl `careers/`, `privacy-policy/`, `terms/`, `security/`, `compare/` + 5 blog article shells) + vendored `content/` tree (site.yaml 20.8 KB, research md + og PNGs, logo.svg; text hydrates from `/content/*` at runtime, so shells carry no copy — fetched and vendored locally), `.origin` provenance, served via `serve_replica.py` on **8926**. Sweep `pages=13 refs=142 residual_uniq=0` — zero serve defects; **innerText byte-identical** live vs replica (9849 B, 225 lines each, missing 0 / extra 0); page height identical (12075 px) both sides; **headless-CDP fullPage pair** (3840×36225, 3×/720-vh authentic layout — `--screenshot` tall-window rejected, `vh`-break bug): **90.0% of 36 225 rows byte-identical**, remainder confined to animated hero/scroll-triggered bands (mean max-diff 13/255), and hero/mid/footer strip crops **md5-identical** at q85; vision judge: **7.5/10 expensive-look** (clean no-template brand system; the one black band = unpainted scroll-triggered section, identical both sides). |
| Veria | `veria/` | Astro static site (verialabs.com, **YC F25, autonomous AI pentester** — "Verified exploits, Automatic fixes, Security review on every PR", "from the #1 US hacking team"): 33 HTML pages sitemap-complete (sitemap-index.xml -> 19 URLs incl. 10 research/blog posts), `_astro/` asset tree + author gravatar avatars vendored (256px PNGs), served via `serve_replica.py` on **8927**. Sweep `pages=33 refs=1275 residual_uniq=1` — the single residual `/privacy/` 404s identically on live (parity, not a gap); **innerText byte-identical** live vs replica on home + `/blog/pwning-pydantic-monty/` + `/challenge/` (chars 4921/4921, lines 173/173, height 4663==4663; a transient home diff in the parallel probe = the ticking demo timer, sequential probes byte-identical); **pixel parity** scale-3 fullPage 3840x12978: 12 680/12 978 rows byte-identical (97.7%), 298 diff rows avg diff 0.026/255 (font antialiasing + live ticker), footer strip md5-identical, bottom-band mean colour identical, last-content-row 10367==10367; **vision judge 7/10 both sides** (same model, same rubric, same capture protocol; hero "Leave nothing to chance." + serif/sans system, client logos PostHog/Phantom/Tempo/Infisical/Stoffel/Pydantic, real GitHub-native PR demo). Ghost-cache lesson: one crawl vendored a stale Cloudflare-edge Next.js shell at `/contact/` (451 `_next/static/chunks/` refs, all 404 on live too) — edge-cache poisoning, not live rewrite; re-crawl repaired it; byte-size spot-check vs fresh live fetch now standard post-crawl. YouTube embeds (2, one research post) stay external by policy; posthog + Cloudflare Web Analytics excluded. |
| Agentic Fabriq | `agenticfabriq/` | React/Next-style SSR site (agenticfabriq.com, **YC W26, "Identity and Permissioning for AI Agents"** — authZ/identity for agent fleets, GDrive/Slack/GitHub integrations, top hero band = live credential-event ticker): 335 files / 54 MB, robots allow-all (explicit AI-crawler allows), served via `serve_replica.py` on **8928**. Sweep `pages=178 refs=12707 residual_uniq=0` after vendoring one space+parens blog image the crawler missed (`blogs/token-refresh-…/s-l1200 (1).jpg`, 327 615 B real JPEG — crawler's unencoded-paren fetch failed). **Ghost-catch-all**: live serves the 141 355 B home shell for ANY unknown route (incl. `/blogs/token-refresh-…`), replicated faithfully; anti-ghost byte audit — live home stable 141 355 B ×3, vendored 141 607 B, the 252 B delta is pure absolute→relative asset-URL rewriting. **Text parity**: height-identical on all 5 probe pages (home 10420==10420); home innerText 7841 vs 7843 — 2-char delta = the ticking credential ticker, which rotates on BOTH sides across sequential fetches (same as Veria). **Pixel parity**: 3840×31260 fullPage both; 93.7% rows byte-identical, all diff confined to animated bands (hero ticker + architecture diagram — two vision reads of the 3000–3499 band: "identical", no layout shift); static footer strips md5-identical.
| Covera | `covera/` | Next.js Turbopack SSR site (www.covera-agents.com, **YC F26, "AI agents for insurance brokers"** — "Less admin, more selling.", en + fr locales): 54 files / 1.8 MB, robots allow-all, served via `serve_replica.py` on **8929**. Sweep `pages=5 refs=… residual_uniq=2` — both `apple-icon` refs 404 on LIVE too (parity, not a defect); opengraph-image vendored as PNG bytes under a `.html` path (crawler stores fetched bytes at the requested URL), md5 == live. **Text parity 3/3 MATCH** (/ , /fr/, /insurance-fest-2026 — title/h1/innerText chars/lines/scrollHeight exact). **Pixel audit 2026-08-24: every diff explained as a time-varying element** — synchronized fresh captures: top + mid viewport bands md5-identical; nav-strip band (initially the top differing run) = transient boot-animation frame (byte-identical after settle); logo-wall band (7622–7786 CSS px) = the infinite horizontal logo **marquee** (18 logos drift −30 px/800 ms, same speed both sides — captures freeze different x-phases; fixed y-extent ⇒ deterministic row band); bot band differs only in the booking form's **live clock** digits (01:22 vs 01:23, 41 rows @ x 1784–1872); fonts + logo img bytes md5-identical both sides. Zero real rendering differences. **Vision judge** 3 bands: top 8/7, mid 8/7, bot 7/7 — content identical, both sides ≥7 → PASS.
| Ndea | `ndea/` | Hand-rolled static Jekyll site (ndea.com, **YC W26, "Building AGI that can innovate."**, AI research lab, 15 people): ~26 files / no framework — one `media/css/global.css`, `media/js/{illustrations,scripts}.js`, 6 canvases (particle-field "network" art, per-load random), served via `serve_replica.py` on **8930**. Sweep `pages=7 refs=149 residual_uniq=0` — zero serve defects; **text parity 5/5 MATCH** (/ , /about, /jobs, /contact, /jobs/symbolic-reasoning-open — innerText chars/lines/heights exact; real-Chrome relay re-probe: textLen 4702==4702, hash identical, 6 canvases both); assets SHA-256-identical live vs vendored (js/css/fonts/jpgs). **Pixel audit 2026-08-24: residual = the site's own per-load randomness** — synchronized fresh captures: top band 2160/2160 rows byte-identical; mid band 44.4% identical rows (deltas 48–53) vs live-vs-live self-diff 44.7% (deltas 47.8–49.1) = statistically the same residual ⇒ replica ≡ another live load. Root cause: h1/h2/h3 `fade-in` (IO + CSS 1s transition) freezes at an arbitrary phase in hidden/backgrounded tabs — observed opacity spread 0/0.025/1 across loads INCLUDING two loads of the live site itself; per-load freeze, not mirror drift. Vision judge live 7/7/7, replica 7/7/7.
| Ossus | `ossus/` | Vercel SSR site (www.ossus.com, **YC W26, "Intelligence for cultural institutions" — formerly Librar Labs, $6M raise announced Jun-Jul 2026**; YC profile + founder LinkedIn + edsheet verified): 84 files / 11 MB — 15 HTML pages (home, 11 blog articles + author/category indexes, /ils/, /subscribe/) + `_next/static` chunks/fonts + `brand/` scenes, served via `serve_replica.py` on **8931**. Sweep `pages=22 refs=752 residual_uniq=0`; runtime lazy-chunk sweep zero fetch failures; **text parity 15/15 MATCH** post-deploy (byte-identical innerText/lines/heights on all 15 routes; home 5896==5896 px). **Deploy-race lesson 2026-08-24**: the site re-deployed mid-cycle (Vercel edge purge ~00:04Z re-encoded both `brand/scene-*.jpg` 252KB→673KB/345KB→817KB) — crawler runs raced the purge and stored pre-deploy bytes while curl/urllib probes got fresh ones (edge/POP variance on identical requests); byte-verify assets against live, never md5-vs-live the HTML (saved HTML is the intended URL-rewritten form — 100% of html diffs are URL-triaged slashes, zero content deltas). Pixel audit: no tint/offset, per-pixel Δ 1-2/255 (scene JPEG decode + AA dither), nav-frost delta from pre-deploy scene bytes resolved after repair; vision live 8/7/7-8, replica 8/7/7 — all ≥7. |
| Axelrod | `axelrod/` | Static Vercel site (axelrod.live, **YC S26, "Boutique Hotels that run themselves"** — operating layer for boutique hotels, founders Adrian Stoica + Saman Sayahpour, standard $500k deal; YC badge in footer): 43 files / 3.1 MB — single-viewport 720px landing (home is the whole short page) + 19 SEO guide pages (chatgpt-guides home + 18 PMS guides), JetBrains Mono css2+woff2 vendored, served via `serve_replica.py` on **8932**. Sweep `pages=21 refs=678 residual_uniq=0` (3 live-404 MCP-SDK code-block literals skipped); **text parity 20/20 MATCH** (byte-identical innerText/lines/heights; home 357==357 chars 720==720 px, beds24 10171==10171/10915==10915); assets 9/9 + fonts woff2 6/6 byte-identical, `document.fonts.check` true both sides; **replica capture MD5-identical to live** (92c5ade1, 8 545 941 B); vision 8 (live 7). |
| Cardboard | `cardboard/` | Next.js Turbopack SSR site (www.usecardboard.com, **YC W26, "Agentic video editor in your browser"** — yep; YC badge on-page + batch record = ≤24-mo funding; AI focus ✓): 671 files mirrored, 8 public routes (/, /pricing/, /privacy/, /terms/, /careers/, /changelog/, /hard-problems/, /signup/), served via `serve_replica.py` on **8933** (replicas 8918-8932 = prior cycles). Sweep residual closes as faithful dead-ref parity (`/share/L8BxE6Gl8l6O` 404s identically on live) — zero missing referenced assets; runtime chunk `15rgaz_mhqsn6.js` lives only in the webpack chunk-map (not an HTML ref) → vendored manually, byte-identical (md5 98a707f7) — **text parity 6/8 byte-exact**, remaining diffs all proven live-dynamic (randomized hero demo, PostHog A/B pricing CTA with replica toggle parity verified, Clerk signup widget SSR-identical); **pixel audit: home top+bottom bands 2160/2160 rows byte-identical, full-page 96.7% (diffs = playing-video frames only), pricing 90.4% (diffs = A/B promo card region only)**; vision pricing 7/7, home deltas = single-image judge variance on identical pixels (same file scored 5→7); screening binding held (full-page 8, top band 7). Origin + full evidence: `cardboard/.origin`.
| Stilta | `stilta/` | Framer-hosted site (stilta.com, **YC W26, "Agentic AI for high-stakes patent work"** — agentic AI/IP focus ✓; on-page YC badge + batch record = ≤24-mo funding): 15 files / 4.0 MB mirrored, all routes served via `serve_replica.py` on **8934** (replicas 8918-8933 = prior cycles); framerusercontent.com assets + events.framer.com analytics left external per cross-host rule, Framer runtime hydrates from CDN. **Render probe: 8 routes match exactly at settle, zero console errors**; **text parity 15/15 routes** (18559 chars raw text identical; 6 DIFFs = content-visibility timing); **pixel audit scale-3 identical framing: bottom band MD5-pixel-identical, top band 2057/2160 rows (95.2%) localized low-magnitude deltas**; vision 7/7 full page + **7/7 top band** (screening binding held); **computer-use (cua-driver + real Chrome, same window): AX 98.2% identical content (29/1635 chrome/marquee-timing lines; replica HTML holds all 10 logo alts = live-dynamic slice)**, cua vision pair live 7/10 / replica 7/10, no broken images. Origin + full evidence: `stilta/.origin`.
| Allia | `allia/` | Framer-hosted site (allia.health, **YC S26, "Clinically Integrated Group for Mental Health"** — first full-stack, AI-native clinical group for mental health, 600+ providers; YC batch record = ≤24-mo funding; AI-native ✓): 19 files / 9.4 MB — home + about/practice/care/intake/network/billing/contact/for-payers + blog/ + 6 posts + 2 legal routes, served via `serve_replica.py` on **8935** (+8936 zoom-free; 8918-8934 = prior cycles); framerusercontent.com assets + events.framer.com analytics left external per cross-host rule (subdomain apps, cal.com, googletagmanager, unpkg = live links), sweep clean. **Text parity 15/15 routes** (2 transient DIFFs = content-visibility timing, re-run MATCH); **render probe: title/height (11894)/innerText (6322) identical live vs replica, zero console errors, identical abort class**; **computer-use (cua-driver + real Chrome): AX web-content parity 391/391 nodes = 100.0%, window captures md5-BYTE-IDENTICAL (dfdf6f36…) = 0.0% pixel diff** after per-host zoom reset (127.0.0.1 saved 80% zoom persisted across ports; detected via AX Zoom button, fixed with cua-driver hotkey cmd+0 foreground); vision 7/7/7 full/top/bottom band (screening binding held), cua pair 7/10 on byte-identical files, no broken images. Origin + full evidence: `allia/.origin`. |

| Specific | `specific/` | Next.js static export (specific.dev, **YC F25, "AWS for coding agents"** — cloud platform for coding agents, founders Lindfors+Rajdavi, foundingDate 2025; batch record = ≤24-mo funding; AI focus ✓): **62 files mirrored** (12 HTML routes = 10 real + 2 og shells — `/docs` is a dead nav link 404 on LIVE, preserved for parity; 14 JS chunks, 17 self-hosted woff2, 11 raster, 5 svg, cdn-cgi email-protection), served via `serve_replica.py` on **8937**; PostHog /ph beacon + schema.org/og canonical absolute URLs left external (identity refs); `logo.svg`/`sitemap.xml` are canonical-only refs (JSON-LD/RSC strings, never `<img>`/fetch — unmirrored, zero impact). **Text parity 9/10 byte-exact** (home DIFF = live typing-terminal animation frame: +65 chars spinner glyphs); **render probe: title/scrollHeight (8091)/innerText (10389) identical, 0 console errors both, 1× replica client-canceled PostHog beacon**; AX 469/469 identical (sole diff = glyph frame); pixel: replica drift (56.0% identical rows) ≤ live-vs-live control (51.7%) — live-dynamic self-animation proven; **vision live 7→8 + top 7, replica 7 + top 7 — binding held** (incidentfox.ai replica failed re-judge 6/10 and was discarded from the same pool). Origin + full evidence: `specific/.origin`. |
| Zavo | `zavopay/` | Framer mirror (zavopay.com, **YC F25, "The Operating System for Modern Hospitality"** — AI OS for restaurants, PCI + Adyen badges; AI focus ✓): **17 HTML files / 11 MB** (16 subroutes + home — POS/payments/voice-AI/reservations/intelligence/marketing/multi-location/network + 4 policy pages; framerusercontent + framercommerce assets + GA4/AdWords/Snitcher beacons left external), served via `serve_replica.py` on **8937**; robots allow-all, 17/17 HTTP 200. **Text parity 17/17 byte-exact** (af_parity transient 2-DIFF = live animated-counter frames, re-probe 0 lines); **render probe: title/scrollHeight (12771)/innerText (4932) identical, 0 console errors both, identical failed-request/ORB profile (same CloudFront `4O7Z0HEYPDNX.js.gz` blocked on BOTH)**; AX 196/196 web-content nodes byte-identical in-window (window rows 34+ zero-diff, pair delta = favicon/spinner chrome band only); pixel full-page 956/960 identical rows (99.6%, sole delta = hero animated element); **vision live 8 + top 8, replica 8, composite same-batch 7/7 "no meaningful differences" — binding held, Framer HTML-only mirror in-spec**. Origin + full evidence: `zavopay/.origin`. |
See `search_methods.md` for the mirroring replay log and verification recipes.
