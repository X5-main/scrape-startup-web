(function(){try{var e=typeof window<`u`?window:typeof global<`u`?global:typeof globalThis<`u`?globalThis:typeof self<`u`?self:{};e.SENTRY_RELEASE={id:`f99fcf9f0a844cc9bd13fdc8e5782b84`};var t=new e.Error().stack;t&&(e._sentryDebugIds=e._sentryDebugIds||{},e._sentryDebugIds[t]=`3e544b4d-2ba8-43a9-9d94-6ab9bff54f88`,e._sentryDebugIdIdentifier=`sentry-dbid-3e544b4d-2ba8-43a9-9d94-6ab9bff54f88`)}catch{}})();import{$t as e,St as t,Tn as n,Tt as r,bt as i,c as a,d as o,en as s,tn as c,wn as l}from"./F_ixKBiO.js";import"./B1sc9Zdx.js";import"./Bb2deiU3.js";import{t as u}from"./JPsrybyr.js";import{t as d}from"./DeWGVqas2.js";import{t as f}from"./CdZDxCfO2.js";var p={title:`How LLM architecture has evolved from GPT-2 to gpt-oss`,description:`In early August 2025, OpenAI launched its first open-weight language model since GPT-2 in 2019: gpt-oss. This article explains the model's architecture and the implications for open-source use.`,date:`2025-08-13T12:00:00.000Z`,length:`7 minute read`,category:`Article`,subcategory:`LLMs`,published:!0,layout:`blog`,toc:[{depth:2,value:`Model Architecture`,id:`model-architecture`},{depth:2,value:`gpt-oss removes Dropout`,id:`gpt-oss-removes-dropout`},{depth:2,value:`gpt-oss favors Swish over GLU for the activation function`,id:`gpt-oss-favors-swish-over-glu-for-the-activation-function`},{depth:2,value:`gpt-oss incorporates Mixture-of-Experts (MoE)`,id:`gpt-oss-incorporates-mixture-of-experts-moe`},{depth:2,value:`gpt-oss leverages Sliding-Window Attention`,id:`gpt-oss-leverages-sliding-window-attention`},{depth:2,value:`gpt-oss uses RMSNorm over LayerNorm as a normalization layer`,id:`gpt-oss-uses-rmsnorm-over-layernorm-as-a-normalization-layer`},{depth:2,value:`gpt-oss uses RoPE over Positional Embeddings as its positional encoding method`,id:`gpt-oss-uses-rope-over-positional-embeddings-as-its-positional-encoding-method`},{depth:2,value:`Performance for gpt-oss`,id:`performance-for-gpt-oss`},{depth:2,value:`Why is gpt-oss a big deal?`,id:`why-is-gpt-oss-a-big-deal`}],rawContent:`Compared to its predecessor (GPT-2), gpt-oss is a significantly more efficient model for developers, with the capacity to carry-out inference with just 16GB of memory.

In early August, [OpenAI launched](https://openai.com/index/introducing-gpt-oss/) its first open-weight language model since GPT-2 in 2019: gpt-oss. The model has two variants: gpt-oss-120b and gpt-oss-20b, both released under the [Apache 2.0 license](https://www.apache.org/licenses/LICENSE-2.0). Both are customizable, support Chain-of-Thought (CoT) reasoning, few-shot function calling, tool use, and support for structured outputs.

These models were announced with a fair amount of fanfare. gpt-oss-120b was touted to have near-parity with OpenAI’s closed-source o4-mini model. Meanwhile, gpt-oss-20b was likened to o3-mini. Given the industry success of both o4-mini and o3-mini, these are noteworthy comparisons for an open-source model. However, what’s most interesting about gpt-oss, in particular gpt-oss-20b, is it’s low footprint, enabling it to run on very modest set-ups.

Given that it’s been over half a decade since OpenAI’s last open-weight model, we wanted to compare how gpt-oss’s architecture has modernized, how it achieves efficiency in inference, and the implications for open-source use. We’ll primarily compare gpt-oss with GPT-2, as that side-by-side showcases how OpenAI has rethought about model architecture.

## Model Architecture

GPT-2 and gpt-oss are both decoder-only models, a class of models where the input is treated as the start of the output. Decoder-only models have been the popular model architecture following the well-known [Attention is All You Need](https://arxiv.org/abs/1706.03762) from 2017.

However, GPT-2 and gpt-oss have [some distinctions](https://magazine.sebastianraschka.com/p/from-gpt-2-to-gpt-oss-analyzing-the). Given the lapse of time between the models, these decisions should be viewed as overall advancements in model architecture as opposed to reactionary changes to GPT-2. These changes might trivially degrade model performance, but they enable developers to run inference for significantly cheaper.

## gpt-oss removes Dropout

GPT-2, similar to other models at the time, featured a dropout stage. Dropout is a pre-training stage [where random attention scores are set to zero](https://arxiv.org/abs/1207.0580). The hypothesis was that these zeroed out scores prevent models from overly-imitating training data (overfitting).

![Dropout illustration](https://modal-cdn.com/cdnbot/Dropoutkfwzcyt2_2ffa461d.webp)

[This hypothesis was proven false with time](https://www.reddit.com/r/deeplearning/comments/12tw4jm/why_dropout_become_not_popular), as modern layers are robust to avoid overfitting and modern datasets are vast enough to better resemble real data. Now, dropout is just an extra compute requirement with no material benefit. Accordingly, most models, including closed-weight models, have eliminated dropout from their architecture. To no surprise, gpt-oss also nixed dropout.

**The Takeaway:** Relative to GPT-2’s architecture, gpt-oss removing dropout increases the model’s accuracy (and trivially saves on compute).

## gpt-oss favors Swish over GLU for the activation function

Transformer models depend on neurons, which are artificial mathematical constructs that imitate how brain neurons work. Neurons have two states: _inactive_ and _activated_, determining whether they should pass forward to the next cycle of training. A neuron’s state is determined by an activation function, calculated from the individual inputs and their weights.

GPT-2 used a Gaussian Error Linear Unit (GELU or GLU) activation function, represented mathematically as \`(x / 2) (1 + erf(x / sqrt(2)))\`. Many models have replaced GELU with Swish, which uses a sigmoid function instead. When plotted side-by-side, GELU and Swish look similar, but Swish is significantly easier on compute despite [GELU showing slightly better performance](https://www.researchgate.net/figure/Comparison-of-GELU-and-Swish-1-curves-Both-are-non-monotonic-functions-and-have-similar_fig7_362428120).

**The Takeaway:** Relative to GPT-2’s architecture, gpt-oss switch to Swish improves the model’s efficiency with a trivial degradation to model performance.

## gpt-oss incorporates Mixture-of-Experts (MoE)

A recently popular technique to increase the capacity of a model’s knowledge without heavily impacting compute needs is Mixture-of-Experts. [With roots in the 1990s](https://cameronrwolfe.substack.com/p/conditional-computation-the-birth), mixture-of-experts involves a set of discrete layers (known as experts) that could number to the thousands. These experts are consulted every pass, but only the top-K experts are chosen by a router sub-module, where K = \`1\`, \`2\`, or another low integer. Accordingly, a model having thousands of experts minimally impacts compute, because each stage only involves consulting the “top experts” based on a router.

![MOE illustratoin](https://modal-cdn.com/cdnbot/MoE1gyi0i_x_29d53f26.webp)

As an analogy, consider a university directory of professors. When a student has a question, they could quickly search the directory for the most relevant professors and consult the best 1-2 professors. Because each professor is smart, it’s entirely okay if the chosen professors isn’t hypothetically the best expert—rather, by crudely looking for the most pertinent professors, they’re more likely, on average, to learn the most.

**The Takeaway:** MoE expands the model’s knowledge, allowing it to include significantly more parameters without exploding compute needs.

## gpt-oss leverages Sliding-Window Attention

GPT-2 uses a technique called [Multi-Head Attention](https://www.metriccoders.com/post/understanding-multi-head-attention-in-gpt-models) (MHA). MHA has multiple queries, independent vectors trained on different perspectives of tokens, each with their own keys and values. Instead, gpt-oss leverages a new technique called [Grouped Query Attention](https://arxiv.org/abs/2305.13245) (GQA), where query heads are paired with shared key heads and value heads (while keeping distinct query values). This technique has a small impact to model performance but creates massive savings for memory.

gpt-oss pairs GQA with another technique called [Sliding-Window Attention](https://arxiv.org/abs/2502.18845), where the considered values in an attention’s context is limited to a sliding window of positionally-close values. The resulting pattern is known as a _locally-banded attention pattern_, in contrast to a _dense pattern_ when the full context is considered.

gpt-oss leverages Sliding-Window Attention in an alternating fashion, where dense and locally-banded patterns are used in different passes.

**The Takeaway:** gpt-oss’ use of Sliding-window Attention and GQA saves on compute and memory with limited impact to model performance.

## gpt-oss uses RMSNorm over LayerNorm as a normalization layer

Transformer models have normalization layers that mathematically normalize activation values. Normalization layers do _not_ change which neurons are and aren’t activated—instead, they scale and center values so that they’re not widely different. For normalization, values might look like \`<4.2, 5.2, -41.4>\`; after normalization, values look closer to \`<0.14, 0.24, -0.80>\` . These normalized values are then passed to the next stage of training.

Previously, GPT-2 used LayerNorm, a function that would normalize values so that their mean equaled \`0.0\` and variance equaled \`1.0\`. Instead, gpt-oss uses RMSNorm, a less expensive normalization function that shifts mean and variance to a reasonable range, while not centering it to zero. RMSNorm is simple: the root mean square of activations without subtracting the mean. While RMSNorm is less accurate than LayerNorm, the degradation is minimal and the compute savings are significant.

**The Takeaway:** gpt-oss’ use of RMSNorm saves on compute with little impact to model performance.

## gpt-oss uses RoPE over Positional Embeddings as its positional encoding method

Positional encoding is what informs the model of the placement of a token in a sequence. GPT-2 used traditional **positional embeddings**, where each token was assigned a sequential value (e.g. \`1\`, \`2\`, \`3\` etc).

gpt-oss, meanwhile, uses a more modern strategy: [**Rotary Positional Embeddings**](https://arxiv.org/abs/2104.09864) or RoPE. RoPE rotates a token’s query and key vectors where the angle is determined by the token’s position. The rotation happens on a complex number plane; accordingly, RoPE encodes relative position into the attention computation without having to separately track embeddings.

RoPE has a few benefits. By transforming the attention values directly, RoPE doesn’t have to store sequential values separately. This makes it easier to handle longer context windows since the length of a sequence doesn’t impact the storage complexity.

**The Takeaway:** gpt-oss switching to RoPE increases the model’s ability to handle longer contexts.

## Performance for gpt-oss

From a performance standpoint, comparing gpt-oss against GPT-2 is not a worthy comparison; gpt-oss was trained on far better data with a modern architecture. In fact, most modern benchmarks don’t even have GPT-2 metrics; the 2019 model preceded comparison sites like LMArena. Instead, gpt-oss is better compared against other open-weight models like [Qwen3 by Alibaba](https://www.alibabacloud.com/blog/alibaba-introduces-qwen3-setting-new-benchmark-in-open-source-ai-with-hybrid-reasoning_602192) or [Mistral’s flagship](https://mistral.ai/).

In particular, Qwen3 is a great comparison. Similar to gpt-oss, Qwen3 30b uses a MoE configuration with GQA. Specifically, Qwen3 30B supports 128 experts per layer and 32 query heads with 4 key-value heads. In comparison, gpt-oss 20B uses 32 experts per layer with 64 query heads with 8 key-value heads.

Based on OpenAI’s own internal tests, gpt-oss-120b and gpt-oss-20b [both outperform](https://magazine.sebastianraschka.com/p/from-gpt-2-to-gpt-oss-analyzing-the) Qwen3’s highest parameter variant, Qwen3 235B, at competition math. Meanwhile, Qwen3 235B outperforms both gpt-oss models at PhD-level science questions, but only marginally. These are quite impressive numbers given that both gpt-oss models are considerably smaller than Qwen3’s thinking model. In fact, gpt-oss-20b is _ten times_ smaller.

## Why is gpt-oss a big deal?

Generally speaking, [OpenAI](https://openai.com/) is the flagship of foundation model developers. OpenAI’s GPT-3 initially set-off the new age of AI, and OpenAI’s leading proprietary models—GPT 4o, o3, and now GPT 5—continue to command the attention of the community. However, OpenAI lacked an open weight model that was competitive with modern alternatives, forcing open weight developers to use Qwen3 by Alibaba or Mistral.

Given OpenAI’s positioning in the AI space, it’s entirely possible that the branding alone will command immense popularity for gpt-oss amongst open weight developers. That said, the model’s incredible statistics gives developers a stronger, mathematically-backed reason to switch over: gpt-oss pulls incredible numbers despite having a much smaller parameter count compared to its closes competition. This is strongly in thanks to its improved architecture.

Today, gpt-oss is freely available to [download on HuggingFace](https://huggingface.co/openai/gpt-oss-120b) and can run within 80GB and 16GB of memory respective to the model’s variant. You can deploy this on Modal today using our [gpt-oss vLLM example](/docs/examples/gpt_oss_inference).

Generally speaking, open models empower developers and organizations to innovate, customize, and deploy AI freely and transparently. They lower barriers to entry, fostering broader access and safer, more democratic AI development. opt-oss is a huge entry by OpenAI and continues to pave the way for an AI-forward future.
`,meta:{description:`In early August 2025, OpenAI launched its first open-weight language model since GPT-2 in 2019: gpt-oss. This article explains the model's architecture and the implications for open-source use.`}},{title:m,description:h,date:g,length:_,category:v,subcategory:y,published:b,layout:x,toc:S,rawContent:C,meta:w}=p,T=t(`<strong>Rotary Positional Embeddings</strong>`),E=t(`<p>Compared to its predecessor (GPT-2), gpt-oss is a significantly more efficient model for developers, with the capacity to carry-out inference with just 16GB of memory.</p> <p>In early August, <!> its first open-weight language model since GPT-2 in 2019: gpt-oss. The model has two variants: gpt-oss-120b and gpt-oss-20b, both released under the <!>. Both are customizable, support Chain-of-Thought (CoT) reasoning, few-shot function calling, tool use, and support for structured outputs.</p> <p>These models were announced with a fair amount of fanfare. gpt-oss-120b was touted to have near-parity with OpenAI’s closed-source o4-mini model. Meanwhile, gpt-oss-20b was likened to o3-mini. Given the industry success of both o4-mini and o3-mini, these are noteworthy comparisons for an open-source model. However, what’s most interesting about gpt-oss, in particular gpt-oss-20b, is it’s low footprint, enabling it to run on very modest set-ups.</p> <p>Given that it’s been over half a decade since OpenAI’s last open-weight model, we wanted to compare how gpt-oss’s architecture has modernized, how it achieves efficiency in inference, and the implications for open-source use. We’ll primarily compare gpt-oss with GPT-2, as that side-by-side showcases how OpenAI has rethought about model architecture.</p> <h2 id="model-architecture">Model Architecture</h2> <p>GPT-2 and gpt-oss are both decoder-only models, a class of models where the input is treated as the start of the output. Decoder-only models have been the popular model architecture following the well-known <!> from 2017.</p> <p>However, GPT-2 and gpt-oss have <!>. Given the lapse of time between the models, these decisions should be viewed as overall advancements in model architecture as opposed to reactionary changes to GPT-2. These changes might trivially degrade model performance, but they enable developers to run inference for significantly cheaper.</p> <h2 id="gpt-oss-removes-dropout">gpt-oss removes Dropout</h2> <p>GPT-2, similar to other models at the time, featured a dropout stage. Dropout is a pre-training stage <!>. The hypothesis was that these zeroed out scores prevent models from overly-imitating training data (overfitting).</p> <p><!></p> <p><!>, as modern layers are robust to avoid overfitting and modern datasets are vast enough to better resemble real data. Now, dropout is just an extra compute requirement with no material benefit. Accordingly, most models, including closed-weight models, have eliminated dropout from their architecture. To no surprise, gpt-oss also nixed dropout.</p> <p><strong>The Takeaway:</strong> Relative to GPT-2’s architecture, gpt-oss removing dropout increases the model’s accuracy (and trivially saves on compute).</p> <h2 id="gpt-oss-favors-swish-over-glu-for-the-activation-function">gpt-oss favors Swish over GLU for the activation function</h2> <p>Transformer models depend on neurons, which are artificial mathematical constructs that imitate how brain neurons work. Neurons have two states: <em>inactive</em> and <em>activated</em>, determining whether they should pass forward to the next cycle of training. A neuron’s state is determined by an activation function, calculated from the individual inputs and their weights.</p> <p>GPT-2 used a Gaussian Error Linear Unit (GELU or GLU) activation function, represented mathematically as <code>(x / 2) (1 + erf(x / sqrt(2)))</code>. Many models have replaced GELU with Swish, which uses a sigmoid function instead. When plotted side-by-side, GELU and Swish look similar, but Swish is significantly easier on compute despite <!>.</p> <p><strong>The Takeaway:</strong> Relative to GPT-2’s architecture, gpt-oss switch to Swish improves the model’s efficiency with a trivial degradation to model performance.</p> <h2 id="gpt-oss-incorporates-mixture-of-experts-moe">gpt-oss incorporates Mixture-of-Experts (MoE)</h2> <p>A recently popular technique to increase the capacity of a model’s knowledge without heavily impacting compute needs is Mixture-of-Experts. <!>, mixture-of-experts involves a set of discrete layers (known as experts) that could number to the thousands. These experts are consulted every pass, but only the top-K experts are chosen by a router sub-module, where K = <code>1</code>, <code>2</code>, or another low integer. Accordingly, a model having thousands of experts minimally impacts compute, because each stage only involves consulting the “top experts” based on a router.</p> <p><!></p> <p>As an analogy, consider a university directory of professors. When a student has a question, they could quickly search the directory for the most relevant professors and consult the best 1-2 professors. Because each professor is smart, it’s entirely okay if the chosen professors isn’t hypothetically the best expert—rather, by crudely looking for the most pertinent professors, they’re more likely, on average, to learn the most.</p> <p><strong>The Takeaway:</strong> MoE expands the model’s knowledge, allowing it to include significantly more parameters without exploding compute needs.</p> <h2 id="gpt-oss-leverages-sliding-window-attention">gpt-oss leverages Sliding-Window Attention</h2> <p>GPT-2 uses a technique called <!> (MHA). MHA has multiple queries, independent vectors trained on different perspectives of tokens, each with their own keys and values. Instead, gpt-oss leverages a new technique called <!> (GQA), where query heads are paired with shared key heads and value heads (while keeping distinct query values). This technique has a small impact to model performance but creates massive savings for memory.</p> <p>gpt-oss pairs GQA with another technique called <!>, where the considered values in an attention’s context is limited to a sliding window of positionally-close values. The resulting pattern is known as a <em>locally-banded attention pattern</em>, in contrast to a <em>dense pattern</em> when the full context is considered.</p> <p>gpt-oss leverages Sliding-Window Attention in an alternating fashion, where dense and locally-banded patterns are used in different passes.</p> <p><strong>The Takeaway:</strong> gpt-oss’ use of Sliding-window Attention and GQA saves on compute and memory with limited impact to model performance.</p> <h2 id="gpt-oss-uses-rmsnorm-over-layernorm-as-a-normalization-layer">gpt-oss uses RMSNorm over LayerNorm as a normalization layer</h2> <p>Transformer models have normalization layers that mathematically normalize activation values. Normalization layers do <em>not</em> change which neurons are and aren’t activated—instead, they scale and center values so that they’re not widely different. For normalization, values might look like <code>&lt;4.2, 5.2, -41.4&gt;</code>; after normalization, values look closer to <code>&lt;0.14, 0.24, -0.80&gt;</code> . These normalized values are then passed to the next stage of training.</p> <p>Previously, GPT-2 used LayerNorm, a function that would normalize values so that their mean equaled <code>0.0</code> and variance equaled <code>1.0</code>. Instead, gpt-oss uses RMSNorm, a less expensive normalization function that shifts mean and variance to a reasonable range, while not centering it to zero. RMSNorm is simple: the root mean square of activations without subtracting the mean. While RMSNorm is less accurate than LayerNorm, the degradation is minimal and the compute savings are significant.</p> <p><strong>The Takeaway:</strong> gpt-oss’ use of RMSNorm saves on compute with little impact to model performance.</p> <h2 id="gpt-oss-uses-rope-over-positional-embeddings-as-its-positional-encoding-method">gpt-oss uses RoPE over Positional Embeddings as its positional encoding method</h2> <p>Positional encoding is what informs the model of the placement of a token in a sequence. GPT-2 used traditional <strong>positional embeddings</strong>, where each token was assigned a sequential value (e.g. <code>1</code>, <code>2</code>, <code>3</code> etc).</p> <p>gpt-oss, meanwhile, uses a more modern strategy: <!> or RoPE. RoPE rotates a token’s query and key vectors where the angle is determined by the token’s position. The rotation happens on a complex number plane; accordingly, RoPE encodes relative position into the attention computation without having to separately track embeddings.</p> <p>RoPE has a few benefits. By transforming the attention values directly, RoPE doesn’t have to store sequential values separately. This makes it easier to handle longer context windows since the length of a sequence doesn’t impact the storage complexity.</p> <p><strong>The Takeaway:</strong> gpt-oss switching to RoPE increases the model’s ability to handle longer contexts.</p> <h2 id="performance-for-gpt-oss">Performance for gpt-oss</h2> <p>From a performance standpoint, comparing gpt-oss against GPT-2 is not a worthy comparison; gpt-oss was trained on far better data with a modern architecture. In fact, most modern benchmarks don’t even have GPT-2 metrics; the 2019 model preceded comparison sites like LMArena. Instead, gpt-oss is better compared against other open-weight models like <!> or <!>.</p> <p>In particular, Qwen3 is a great comparison. Similar to gpt-oss, Qwen3 30b uses a MoE configuration with GQA. Specifically, Qwen3 30B supports 128 experts per layer and 32 query heads with 4 key-value heads. In comparison, gpt-oss 20B uses 32 experts per layer with 64 query heads with 8 key-value heads.</p> <p>Based on OpenAI’s own internal tests, gpt-oss-120b and gpt-oss-20b <!> Qwen3’s highest parameter variant, Qwen3 235B, at competition math. Meanwhile, Qwen3 235B outperforms both gpt-oss models at PhD-level science questions, but only marginally. These are quite impressive numbers given that both gpt-oss models are considerably smaller than Qwen3’s thinking model. In fact, gpt-oss-20b is <em>ten times</em> smaller.</p> <h2 id="why-is-gpt-oss-a-big-deal">Why is gpt-oss a big deal?</h2> <p>Generally speaking, <!> is the flagship of foundation model developers. OpenAI’s GPT-3 initially set-off the new age of AI, and OpenAI’s leading proprietary models—GPT 4o, o3, and now GPT 5—continue to command the attention of the community. However, OpenAI lacked an open weight model that was competitive with modern alternatives, forcing open weight developers to use Qwen3 by Alibaba or Mistral.</p> <p>Given OpenAI’s positioning in the AI space, it’s entirely possible that the branding alone will command immense popularity for gpt-oss amongst open weight developers. That said, the model’s incredible statistics gives developers a stronger, mathematically-backed reason to switch over: gpt-oss pulls incredible numbers despite having a much smaller parameter count compared to its closes competition. This is strongly in thanks to its improved architecture.</p> <p>Today, gpt-oss is freely available to <!> and can run within 80GB and 16GB of memory respective to the model’s variant. You can deploy this on Modal today using our <!>.</p> <p>Generally speaking, open models empower developers and organizations to innovate, customize, and deploy AI freely and transparently. They lower barriers to entry, fostering broader access and safer, more democratic AI development. opt-oss is a huge entry by OpenAI and continues to pave the way for an AI-forward future.</p>`,1);function D(t,m){let h=a(m,[`children`,`$$slots`,`$$events`,`$$legacy`]);f(t,o(()=>h,()=>p,{children:(t,a)=>{var o=E(),f=c(s(o),2),p=c(e(f));d(p,{href:`https://openai.com/index/introducing-gpt-oss/`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`OpenAI launched`))},$$slots:{default:!0}}),d(c(p,2),{href:`https://www.apache.org/licenses/LICENSE-2.0`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`Apache 2.0 license`))},$$slots:{default:!0}}),l(),n(f);var m=c(f,8);d(c(e(m)),{href:`https://arxiv.org/abs/1706.03762`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`Attention is All You Need`))},$$slots:{default:!0}}),l(),n(m);var h=c(m,2);d(c(e(h)),{href:`https://magazine.sebastianraschka.com/p/from-gpt-2-to-gpt-oss-analyzing-the`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`some distinctions`))},$$slots:{default:!0}}),l(),n(h);var g=c(h,4);d(c(e(g)),{href:`https://arxiv.org/abs/1207.0580`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`where random attention scores are set to zero`))},$$slots:{default:!0}}),l(),n(g);var _=c(g,2);u(e(_),{src:`https://modal-cdn.com/cdnbot/Dropoutkfwzcyt2_2ffa461d.webp`,alt:`Dropout illustration`}),n(_);var v=c(_,2);d(e(v),{href:`https://www.reddit.com/r/deeplearning/comments/12tw4jm/why_dropout_become_not_popular`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`This hypothesis was proven false with time`))},$$slots:{default:!0}}),l(),n(v);var y=c(v,8);d(c(e(y),3),{href:`https://www.researchgate.net/figure/Comparison-of-GELU-and-Swish-1-curves-Both-are-non-monotonic-functions-and-have-similar_fig7_362428120`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`GELU showing slightly better performance`))},$$slots:{default:!0}}),l(),n(y);var b=c(y,6);d(c(e(b)),{href:`https://cameronrwolfe.substack.com/p/conditional-computation-the-birth`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`With roots in the 1990s`))},$$slots:{default:!0}}),l(5),n(b);var x=c(b,2);u(e(x),{src:`https://modal-cdn.com/cdnbot/MoE1gyi0i_x_29d53f26.webp`,alt:`MOE illustratoin`}),n(x);var S=c(x,8),C=c(e(S));d(C,{href:`https://www.metriccoders.com/post/understanding-multi-head-attention-in-gpt-models`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`Multi-Head Attention`))},$$slots:{default:!0}}),d(c(C,2),{href:`https://arxiv.org/abs/2305.13245`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`Grouped Query Attention`))},$$slots:{default:!0}}),l(),n(S);var w=c(S,2);d(c(e(w)),{href:`https://arxiv.org/abs/2502.18845`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`Sliding-Window Attention`))},$$slots:{default:!0}}),l(5),n(w);var D=c(w,18);d(c(e(D)),{href:`https://arxiv.org/abs/2104.09864`,rel:`nofollow`,children:(e,t)=>{i(e,T())},$$slots:{default:!0}}),l(),n(D);var O=c(D,8),k=c(e(O));d(k,{href:`https://www.alibabacloud.com/blog/alibaba-introduces-qwen3-setting-new-benchmark-in-open-source-ai-with-hybrid-reasoning_602192`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`Qwen3 by Alibaba`))},$$slots:{default:!0}}),d(c(k,2),{href:`https://mistral.ai/`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`Mistral’s flagship`))},$$slots:{default:!0}}),l(),n(O);var A=c(O,4);d(c(e(A)),{href:`https://magazine.sebastianraschka.com/p/from-gpt-2-to-gpt-oss-analyzing-the`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`both outperform`))},$$slots:{default:!0}}),l(3),n(A);var j=c(A,4);d(c(e(j)),{href:`https://openai.com/`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`OpenAI`))},$$slots:{default:!0}}),l(),n(j);var M=c(j,4),N=c(e(M));d(N,{href:`https://huggingface.co/openai/gpt-oss-120b`,rel:`nofollow`,children:(e,t)=>{l(),i(e,r(`download on HuggingFace`))},$$slots:{default:!0}}),d(c(N,2),{href:`/docs/examples/gpt_oss_inference`,children:(e,t)=>{l(),i(e,r(`gpt-oss vLLM example`))},$$slots:{default:!0}}),l(),n(M),l(2),i(t,o)},$$slots:{default:!0}}))}export{D as default,p as metadata};
//# sourceMappingURL=D_IQCr5i.js.map
