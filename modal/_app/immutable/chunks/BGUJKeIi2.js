(function(){try{var e=typeof window<`u`?window:typeof global<`u`?global:typeof globalThis<`u`?globalThis:typeof self<`u`?self:{};e.SENTRY_RELEASE={id:`f99fcf9f0a844cc9bd13fdc8e5782b84`};var t=new e.Error().stack;t&&(e._sentryDebugIds=e._sentryDebugIds||{},e._sentryDebugIds[t]=`dfd59d25-3b05-4abb-84af-3fb63f7bd104`,e._sentryDebugIdIdentifier=`sentry-dbid-dfd59d25-3b05-4abb-84af-3fb63f7bd104`)}catch{}})();import{$t as e,St as t,Tn as n,Tt as r,bt as i,c as ee,d as te,en as ne,tn as a,wn as o}from"./F_ixKBiO.js";import"./B1sc9Zdx.js";import"./Bb2deiU3.js";import{a as s,i as c,o as re}from"./CPby7b1n.js";import{t as l}from"./BILrvr3I.js";import{t as ie}from"./B4L_if842.js";import{t as u}from"./DeWGVqas2.js";var d={toc:[{depth:1,value:`Python SDK Release Notes`,id:`python-sdk-release-notes`,children:[{depth:2,value:`Latest`,id:`latest`,children:[{depth:3,value:`1.5.4 (2026-08-12)`,id:`154-2026-08-12`},{depth:3,value:`1.5.3 (2026-07-23)`,id:`153-2026-07-23`},{depth:3,value:`1.5.2 (2026-07-10)`,id:`152-2026-07-10`},{depth:3,value:`1.5.1 (2026-06-23)`,id:`151-2026-06-23`},{depth:3,value:`1.5.0 (2026-06-09)`,id:`150-2026-06-09`}]},{depth:2,value:`1.4`,id:`14`,children:[{depth:3,value:`1.4.3 (2026-05-18)`,id:`143-2026-05-18`},{depth:3,value:`1.4.2 (2026-04-16)`,id:`142-2026-04-16`},{depth:3,value:`1.4.1 (2026-03-30)`,id:`141-2026-03-30`},{depth:3,value:`1.4.0 (2026-03-25)`,id:`140-2026-03-25`}]},{depth:2,value:`1.3`,id:`13`,children:[{depth:3,value:`1.3.5 (2026-03-03)`,id:`135-2026-03-03`},{depth:3,value:`1.3.4 (2026-02-23)`,id:`134-2026-02-23`},{depth:3,value:`1.3.3 (2026-02-12)`,id:`133-2026-02-12`},{depth:3,value:`1.3.2 (2026-01-30)`,id:`132-2026-01-30`},{depth:3,value:`1.3.1 (2026-01-22)`,id:`131-2026-01-22`},{depth:3,value:`1.3.0 (2025-12-19)`,id:`130-2025-12-19`}]},{depth:2,value:`1.2`,id:`12`,children:[{depth:3,value:`1.2.6 (2025-12-16)`,id:`126-2025-12-16`},{depth:3,value:`1.2.5 (2025-12-12)`,id:`125-2025-12-12`},{depth:3,value:`1.2.4 (2025-11-21)`,id:`124-2025-11-21`},{depth:3,value:`1.2.3 (2025-11-20)`,id:`123-2025-11-20`},{depth:3,value:`1.2.2 (2025-11-10)`,id:`122-2025-11-10`},{depth:3,value:`1.2.1 (2025-10-22)`,id:`121-2025-10-22`},{depth:3,value:`1.2.0 (2025-10-09)`,id:`120-2025-10-09`}]},{depth:2,value:`1.1`,id:`11`,children:[{depth:3,value:`1.1.4 (2025-09-03)`,id:`114-2025-09-03`},{depth:3,value:`1.1.3 (2025-08-19)`,id:`113-2025-08-19`},{depth:3,value:`1.1.2 (2025-08-14)`,id:`112-2025-08-14`},{depth:3,value:`1.1.1 (2025-08-01)`,id:`111-2025-08-01`},{depth:3,value:`1.1.0 (2025-07-17)`,id:`110-2025-07-17`}]},{depth:2,value:`1.0`,id:`10`,children:[{depth:3,value:`1.0.5 (2025-06-27)`,id:`105-2025-06-27`},{depth:3,value:`1.0.4 (2025-06-13)`,id:`104-2025-06-13`},{depth:3,value:`1.0.3 (2025-06-05)`,id:`103-2025-06-05`},{depth:3,value:`1.0.2 (2025-05-26)`,id:`102-2025-05-26`},{depth:3,value:`1.0.1 (2025-05-19)`,id:`101-2025-05-19`},{depth:3,value:`1.0.0 (2025-05-16)`,id:`100-2025-05-16`}]}]}],rawContent:'# Python SDK Release Notes\n\nThese release notes document user-facing updates (features, enhancements, fixes, and deprecations) to the `modal` client library.\n\n## Latest\n\n### 1.5.4 (2026-08-12)\n\nWith this release, it\'s now possible to opt into a [more performant Sandbox backend](/blog/scaling-to-1-million-concurrent-sandboxes-in-seconds), which affords substantially higher creation rates and concurrency. Set `MODAL_SANDBOX_V2=1` as an environment variable and your Sandboxes will use this new system without any code changes. This will become the default behavior in version 1.6.0 of the Python SDK. Note that the new backend does not support the deprecated FileIO-based Sandbox filesystem API; please [migrate](/docs/guide/migrate-sandbox-filesystem) any code that is currently issuing deprecation warnings before enabling the feature flag.\n\n- We\'ve added logs APIs to the `modal.App` and `modal.Image` objects:\n  - The [`App.logs`](/docs/sdk/py/latest/App#logs) API allows you to `fetch()`, `tail()`, or `stream()` all logs from an App.\n  - The [`Image.logs`](/docs/sdk/py/latest/Image#logs) API supports `fetch()` and `tail()` on Image build logs. Streaming build logs is not supported at this time.\n- We\'ve also added a [`modal image logs`](/docs/cli/latest/image#modal-image-logs) CLI command with equivalent functionality.\n- We now support querying basic information about your Workspace\'s current pricing structure via the [`Workspace.billing.rates()`](/docs/sdk/py/latest/Workspace#billingrates) API and [`modal billing rates`](/docs/cli/latest/billing#modal-billing-rates) CLI.\n- The [`Function.update_autoscaler()`](/docs/sdk/py/latest/Function#update_autoscaler) and [`Server.update_autoscaler()`](/docs/sdk/py/latest/Server#update_autoscaler) methods now return the complete state of the autoscaler configuration after applying the update.\n- The `target_concurrency` parameter in [`@app.server()`](/docs/sdk/py/latest/App#server) and [`Server.update_autoscaler()`](/docs/sdk/py/latest/Server#update_autoscaler) now supports fractional values for more fine-grained control over autoscaling behavior.\n\n### 1.5.3 (2026-07-23)\n\n- We\'ve added new APIs for programmatically retrieving logs on several object types: [`Function`](/docs/sdk/py/latest/Function#logs), [`Server`](/docs/sdk/py/latest/Server#logs), and [`FunctionCall`](/docs/sdk/py/latest/FunctionCall#logs). Each object exposes three different methods, allowing you to `stream()` logs as they are generated, `fetch()` logs from a specific date/time range, or `tail()` the most recent logs.\n- It\'s now possible to retrieve a summary of billing information across billing cycles:\n  - Use the [`modal.Workspace.billing.summary()`](/docs/sdk/py/latest/Workspace#billingsummary) method or the [`modal billing summary`](/docs/cli/latest/billing#modal-billing-summary) CLI to see workspace-level spend (broken down by category), credit usage, and the impact of any compute reservations.\n  - Use the [`modal.Environment.billing.summary()`](/docs/sdk/py/latest/Environment#billingsummary) method or the [`modal environment billing summary`](/docs/cli/latest/environment#modal-environment-billing-summary) CLI to see environment-level spend.\n- We\'ve made some changes to the interfaces for managing [RBAC](/docs/guide/rbac) permissions:\n  - We now explicitly represent all workspace member and service user roles in each Environment instead of having separate concepts for Environment membership and role.\n  - We\'ve accordingly introduced a [`modal.Environment.roles`](/docs/sdk/py/latest/Environment#roles) interface (and [`modal environment roles`](/docs/cli/latest/environment#modal-environment-roles) CLI), replacing the `modal.Environment.members` interface and `modal environment members` CLI, which are now deprecated.\n- It\'s now possible to specify one or more `--compute-region` options in [`modal endpoint create`](/docs/cli/latest/endpoint#modal-endpoint-create) to configure the region where Endpoint containers run.\n- We\'ve improved performance for large writes by ≈2.5x in several Sandbox filesystem methods ([`copy_from_local()`](/docs/sdk/py/latest/Sandbox#filesystemcopy_from_local), [`write_bytes()`](/docs/sdk/py/latest/Sandbox#filesystemwrite_bytes), and [`write_text()`](/docs/sdk/py/latest/Sandbox#filesystemwrite_text)).\n\n### 1.5.2 (2026-07-10)\n\n- We\'ve added new interfaces for programmatic management of workspace settings:\n  - Use the [`modal.Workspace.settings.list()`](/docs/sdk/py/latest/Workspace#settingslist) method or [`modal workspace settings list`](/docs/cli/latest/workspace#modal-workspace-settings-list) CLI to see current workspace-level settings.\n  - Use the [`modal.Workspace.settings.set()`](/docs/sdk/py/latest/Workspace#settingsset) method or [`modal workspace settings set`](/docs/cli/latest/workspace#modal-workspace-settings-set) CLI to configure new values.\n- We\'ve added a [`modal.types`](/docs/sdk/py/latest/types) module for exposing dataclasses returned from public methods as public API. The types in this module are not typically constructed by user code, but it may be useful to reference them for, e.g., adding type annotations to code that wraps the relevant Modal API.\n- The [`modal.Function.with_options()`](/docs/sdk/py/latest/Function#with_options) method now accepts a `routing_region` argument to dynamically configure [regional routing](/docs/guide/region-selection#regional-routing) at the time of invocation.\n- The [`modal container stop`](/docs/cli/latest/container#modal-container-stop) CLI now accepts a `--graceful` flag. With it, the container stops fetching new inputs but finishes the inputs it is currently running before exiting, instead of having them cancelled and rescheduled. Graceful stops are only supported for containers running a Modal Function or Modal Server.\n- The [`modal container logs`](/docs/cli/latest/container#modal-container-logs) CLI now includes logs from the startup phase of the container.\n- The [`modal.Workspace.members.list()`](/docs/sdk/py/latest/Workspace#memberslist) method now uses `"member"` instead of `"user"` for the lowest-privilege Workspace role, matching the UI and documentation.\n- The [`modal.Sandbox.reload_volumes()`](/docs/sdk/py/latest/Sandbox#reload_volumes) method now blocks until the Volumes have been reloaded, bounded by a new `timeout` argument (55 seconds by default). If the reload does not complete within `timeout`, [`modal.exception.TimeoutError`](/docs/sdk/py/latest/exception#timeouterror) is raised.\n\n### 1.5.1 (2026-06-23)\n\nThis release includes several major new features, including the debut of a new serverless compute primitive for low-latency HTTP applications ([`@app.server()`](/docs/sdk/py/latest/App#server)) and a new CLI for deploying production-ready LLM inference endpoints with minimal configuration ([`modal endpoint`](/docs/cli/latest/endpoint)).\n\n- We\'re introducing a new [`@app.server()`](/docs/sdk/py/latest/App#server) decorator (and [`modal.Server`](/docs/sdk/py/latest/Server) object), representing a serverless compute primitive that shares many features with Modal Functions while being optimized for serving HTTP-based applications with ultra-low latency. Read the [guide](/docs/guide/servers) for more information.\n- We\'re also introducing the [`modal endpoint`](/docs/cli/latest/endpoint) CLI, providing programmatic access to our new [Endpoints](/docs/guide/endpoints) product. Endpoints let you deploy production-ready LLM inference servers with minimal configuration.\n- We\'ve added several new SDK features for workspace configuration and observability:\n  - We\'ve added new billing API methods on the [`modal.Workspace`](/docs/sdk/py/latest/Workspace) and [`modal.Environment`](/docs/sdk/py/latest/Environment) objects ([`workspace.billing.report()`](/docs/sdk/py/latest/Workspace#billingreport) and [`environment.billing.report()`](/docs/sdk/py/latest/Environment#billingreport), respectively). The new APIs include a resource-level cost breakdown (CPU, memory, and specific GPU types). We\'ve also added a [`modal environment billing`](/docs/cli/latest/environment#modal-environment-billing) CLI for generating environment-scoped billing reports. The new workspace-level API replaces the existing [`modal.billing.workspace_billing_report`](/docs/sdk/py/latest/billing#workspace_billing_report) function.\n  - We\'ve added support for creating and managing [proxy tokens](/docs/guide/webhook-proxy-auth) via the [`modal.Workspace`](/docs/sdk/py/latest/Workspace) object ([`workspace.proxy_tokens.create()`](/docs/sdk/py/latest/Workspace#proxy_tokenscreate), [`workspace.proxy_tokens.list()`](/docs/sdk/py/latest/Workspace#proxy_tokenslist), etc.) and a new [`modal workspace proxy-tokens`](/docs/cli/latest/workspace#modal-workspace-proxy-tokens) CLI. Modal Servers and Endpoints are authenticated via proxy tokens by default.\n  - We\'ve added a new [`modal workspace members`](/docs/cli/latest/workspace#modal-workspace-members) CLI for querying information about workspace membership.\n- The new [`modal curl`](/docs/cli/latest/curl) CLI command allows you to make requests to authenticated endpoints without explicitly passing proxy token headers. This is an experimental feature and may change in the future.\n- The [`modal app rollback`](/docs/cli/latest/app#modal-app-rollback) command now accepts a `--strategy` (`rolling` or `recreate`), like [`modal deploy`](/docs/cli/latest/deploy) and [`modal app rollover`](/docs/cli/latest/app#modal-app-rollover).\n- Sandbox connect tokens can now be scoped to a custom port ([`modal.Sandbox.create_connect_token(port=...)`](/docs/sdk/py/latest/Sandbox#create_connect_token)).\n- It is now possible to call [`image.publish()`](/docs/sdk/py/latest/Image#publish) on an Image constructed using [`modal.Image.from_id()`](/docs/sdk/py/latest/Image#from_id) without first calling [`image.build()`](/docs/sdk/py/latest/Image#build).\n- The Modal Python client now respects standard environment variables for HTTP CONNECT and SOCKS4/5 proxies (`HTTPS_PROXY` and `ALL_PROXY`). Proxy support requires installing extra dependencies, i.e. using `uv pip install \'modal[api-proxy-support]\'`. To opt out of proxy support, set `MODAL_DISABLE_API_PROXY=1` or put `disable_api_proxy = true` in your `.modal.toml` profile.\n\n### 1.5.0 (2026-06-09)\n\nThis is a major release that includes several new features ([named Images](/docs/guide/named-images), version-pinned Function lookups, [Sandbox domain allowlists](/docs/guide/sandbox-networking)), a new [`modal skills`](/docs/cli/latest/skills) CLI, and a small number of breaking changes.\n\n- We\'re introducing a "named Image" concept, akin to a Modal-native Image registry. This feature allows you to decouple Image builds from App deployment or Sandbox creation, and it simplifies the process of sharing a canonical Image across a large number of unrelated Apps.\n  - The new SDK method [`modal.Image.publish()`](/docs/sdk/py/latest/Image#publish) assigns a name to an existing Image. Names can optionally include a "tag" suffix (`"{name}:{tag}"`) to specify variants of an Image, e.g. to apply a versioning system.\n  - The new SDK method [`modal.Image.from_name()`](/docs/sdk/py/latest/Image#from_name) refers directly to that Image by its name. Unlike with Image methods that specify parts of a build recipe, the [`modal.Image.from_name()`](/docs/sdk/py/latest/Image#from_name) lookup will either succeed or fail with a [`modal.exception.NotFoundError`](/docs/sdk/py/latest/exception#notfounderror) error, but it will never trigger a build.\n  - The new [`modal image names`](/docs/cli/latest/image#modal-image-names) CLI can be used to see the current name assignments.\n- We\'re introducing a "version-pinning" concept for Function lookups. By passing `version=` to [`modal.Function.from_name()`](/docs/sdk/py/latest/Function#from_name) or [`modal.Cls.from_name()`](/docs/sdk/py/latest/Cls#from_name), you can retrieve an instance that will send all inputs to the same version of the Function, even after a subsequent redeployment of its App. Additionally, any transitive invocations of other Functions in the same App will also be pinned on that version. This feature makes it easy to execute workflows that involve multiple Functions when those Functions might change in incompatible ways across deployments.\n- It\'s now possible to restrict the *domains* that processes inside of a Sandbox can connect to. When you provide a list of domains to `outbound_domain_allowlist=[...]` in [`modal.Sandbox.create()`](/docs/sdk/py/latest/Sandbox#create), requests outside of the allowlist will be blocked by Modal infrastructure, and the denial will be recorded in the App logs.\n- We\'ve added a new [`modal skills`](/docs/cli/latest/skills) CLI for installing a foundational Modal agent skill ([`modal skills install`](/docs/cli/latest/skills#modal-skills-install)) and keeping it up to date over time ([`modal skills update`](/docs/cli/latest/skills#modal-skills-update)). Please help us improve it by sharing any feedback you have about its impact on agentic development with Modal.\n- We\'ve added a new [`modal.Workspace`](/docs/sdk/py/latest/Workspace) object for programmatic interaction with your Workspace configuration. The initial release offers a [`workspace.members.list()`](/docs/sdk/py/latest/Workspace#memberslist) method; expect more features soon.\n- The [`modal`](/docs/cli/latest) CLI now normalizes the keys of its `--json` output by lowercasing and substituting underscores for non-alphanumeric characters.\n- We\'ve added a new [`sandbox.filesystem.watch()`](/docs/sdk/py/latest/Sandbox#filesystemwatch) method to the Sandbox Filesystem API, and we\'ve deprecated the alpha [`modal.Sandbox.watch()`](/docs/sdk/py/latest/Sandbox#watch) method. The new method has a different implementation and offers significantly improved latency and reliability.\n- We\'ve made two small breaking changes around Sandbox snapshots:\n  - [`modal.Sandbox.snapshot_filesystem()`](/docs/sdk/py/latest/Sandbox#snapshot_filesystem) and [`modal.Sandbox.snapshot_directory()`](/docs/sdk/py/latest/Sandbox#snapshot_directory) now accept an explicit `ttl=` keyword argument that configures a retention interval (in seconds) for the resulting Image. Both methods default to 30 days (`ttl=30 * 24 * 3600`). **Note:** this alters the default behavior of these methods, because snapshot Images were previously persisted indefinitely. Pass an explicit `ttl=None` to retain the previous behavior.\n  - [`modal.Sandbox.snapshot_directory()`](/docs/sdk/py/latest/Sandbox#snapshot_directory) now also accepts a `timeout=` keyword argument (defaulting to `55` seconds), which brings it to parity with [`modal.Sandbox.snapshot_filesystem()`](/docs/sdk/py/latest/Sandbox#snapshot_filesystem). If the snapshot does not return before the deadline, a [`modal.exception.TimeoutError`](/docs/sdk/py/latest/exception#timeouterror) will be raised. **Note:** this alters the default behavior, which previously waited indefinitely, but you can set an arbitrarily long timeout.\n- We\'ve removed several deprecated static methods (`.delete()` and `.create_deployed()`) on Modal storage objects ([`modal.Volume`](/docs/sdk/py/latest/Volume), etc.). Use the [`.objects.delete()`](/docs/sdk/py/latest/Volume#objectsdelete) and [`.objects.create()`](/docs/sdk/py/latest/Volume#objectscreate) methods instead.\n\n## 1.4\n\n### 1.4.3 (2026-05-18)\n\n- This release introduces a new ["regional routing"](https://modal.com/docs/guide/region-selection#regional-routing) concept for Function inputs, which is now in Public Beta. By setting `routing_region="..."` in the `@app.function()` or `@app.cls()` decorators, you can configure the Function to route its inputs through servers in `us-west`, `eu-west`, or `ap-south` instead of `us-east`. This can reduce network latency and help you meet data residency obligations. While in Beta, this feature has a few constraints:\n  - `routing_region=` can only be set during the initial deployment of a Function and cannot be changed in a subsequent redeployment.\n  - Functions using regional routing outside of `us-east` can only be invoked with the `.remote()` and `.map()` methods.\n- We\'ve added a new `modal.Environment` object for programmatically managing Environments, and we\'ve expanded the `modal environment` CLI to support [RBAC](https://modal.com/docs/guide/rbac) configuration.\n- It is now possible to dynamically configure `modal.Function` behavior using `Function.with_options()`, `Function.with_concurrency()`, and `Function.with_batching()`.\n- The new `modal.Volume.with_mount_options()` method allows you to configure a Volume mount as read-only (`read_only=True`) and / or limit the mount to a subdirectory of the Volume (`sub_path="/some/path"`).\n- It\'s now possible to pass a custom App name for ephemeral Apps using the `--name` option in `modal run`/`modal serve` or by setting `name=` in `App.run()`.\n- We\'ve added two new methods to the `modal.Sandbox` [Filesystem API](https://modal.com/docs/guide/sandbox-files):\n  - `sandbox.filesystem.list_files(path)` lists entries (with metadata) in a given directory on the Sandbox filesystem. This replaces the alpha `modal.Sandbox.ls` method.\n  - `sandbox.filesystem.stat(path)` returns metadata for a specific file/symlink/directory on the Sandbox\'s filesystem.\n- It\'s now possible to place restrictions on *inbound* Sandbox connections by setting `inbound_cidr_allowlist=[...]` in `modal.Sandbox.create()`. We are also adding a new `outbound_cidr_allowlist=[...]` parameter and deprecating the existing `cidr_allowlist=[...]` to avoid confusion.\n- We\'ve improved the reliability of the `modal.Sandbox.snapshot_filesystem()` operation, especially for large snapshots, and we now support setting a `timeout=` longer than 55s when necessary.\n- Images returned by `modal.Sandbox.snapshot_directory()` can now be passed to `modal.Sandbox.create()` to use as a root filesystem for the new Sandbox.\n- We\'ve added a `modal.Image.pipe()` method to let you define reusable Image recipes that compose well with the fluent Image builder interface.\n- It\'s now possible to assign Sandbox tags at creation with `modal.Sandbox.create(..., tags=tags)`.\n- It\'s now possible to use `--chmod` and `--chown` flags on `COPY` commands within `modal.Image.from_dockerfile()`.\n- We\'ve improved the reliability and latency for `modal shell` and `modal container exec` CLI commands.\n\n### 1.4.2 (2026-04-16)\n\n- We\'ve added a new `modal app rollover` CLI command for triggering a redeployment of an App without making any code or configuration changes. A rollover replaces existing containers with fresh ones. As with `modal deploy`, there are two strategies for switching between deployments:\n  - `--strategy=rolling` (the default) will smoothly migrate traffic from old containers to new containers\n  - `--strategy=recreate` will terminate all running containers so that any subsequent inputs will go to new containers\n- We\'ve added a new `modal bootstrap` CLI command, which fetches deployable starter code for common AI applications (e.g., text generation, text-to-image, speech-to-text). This is an experiment: try it out and give us feedback!\n- We\'ve added two methods to the new Sandbox filesystem API:\n  - `sandbox.filesystem.make_directory()` creates a new directory on the Sandbox filesystem\n  - `sandbox.filesystem.remove()` deletes a file or directory from the Sandbox filesystem\n- The new Sandbox filesystem methods replace the `modal.Sandbox.mkdir` and `modal.Sandbox.rm` methods, which are now deprecated.\n- Sandboxes also now support `sb.unmount_image(path)` to remove a previously mounted Image from a path and reveal the underlying Sandbox filesystem there again.\n- The `modal app stop` and `modal container stop` CLI commands now prompt for confirmation (pass `--yes` to skip).\n- Several other `modal app` CLI commands will now map a name-based argument to a recently-stopped App that used that name. This is useful for, e.g., fetching logs from an App after it has been stopped.\n- We\'ve added a `build_args` parameter to `modal.Image.dockerfile_commands()`.\n\n### 1.4.1 (2026-03-30)\n\n- We\'re introducing a concept of "readiness probes" for `modal.Sandbox`. This feature lets you configure a readiness check on a TCP port (`modal.Probe.with_tcp()`) or by executing a process (`modal.Probe.with_exec()`). Calling `sb.wait_until_ready()` will block until the Probe succeeds:\n  ```python notest\n  app = modal.App.lookup(\'sandbox-app\', create_if_missing=True)\n  probe = modal.Probe.with_tcp(8080)\n  sb = modal.Sandbox.create(\n      "python3", "-m", "http.server", "8080",\n      readiness_probe=probe,\n      app=app,\n  )\n  sb.wait_until_ready()\n  ```\n- We fixed a longstanding bug that could cause WebSocket performance to degrade after handling hundreds of connections from the same container.\n- We improved the performance of `modal container logs` when fetching logs for an old container.\n- We fixed a bug introduced in 1.4.0 that made the `modal` CLI crash on `typer<0.19.0`.\n\n### 1.4.0 (2026-03-25)\n\nWe\'ve made significant CLI enhancements so that Modal logs can be more accessible to coding agents:\n\n- The `modal app logs` and `modal container logs` commands now have the ability to fetch historical logs using counting (e.g. `--tail 1000`) or time-based (e.g., `--since 4h`, `--until 2026-03-15`, etc.) configuration. Note that historical log access is subject to plan-level retention limits.\n- The `modal container logs` command also accepts `--all` to fetch the complete set of logs for that Container or Sandbox.\n- Both CLI commands now accept a `--search` filter, and they can also filter by `--source` (`stdout`/`stderr`/`system`).\n- The `modal app logs` command additionally accepts `--function`, `--function-call`, and `--container` filters.\n- The `modal app logs` command can prefix each line with the ID of the Function, FunctionCall, or Container where it originated (e.g. `--show-function-id`).\n- Note that the default behavior of these commands has changed. Previously, they would follow (i.e., stream) logs by default, but you now must pass `--follow` to get this behavior. The new default will always show the most recent 100 log entries.\n\nWe\'re releasing a new [Sandbox filesystem API](https://modal.com/docs/guide/sandbox-files) (currently in Beta) with significantly improved reliability and ergonomics:\n\n- Use `sb.filesystem.copy_from_local` / `sb.filesystem.copy_to_local` to transfer file contents between your local filesystem and the Sandbox filesystem.\n- Use `sb.filesystem.write_text` / `sb.filesystem.read_text` or `sb.filesystem.write_bytes` / `sb.filesystem.read_bytes` to transfer file contents between local memory and the Sandbox filesystem.\n- These new APIs replace the `modal.Sandbox.open` method and the `modal.file_io.FileIO` type that it returns; the old APIs are now deprecated.\n\nWe\'re introducing the concept of "deployment strategies" to give you more flexibility over what happens when redeploying your App:\n\n- By passing `modal deploy --strategy recreate` (or `app.deploy(strategy="recreate")` in the SDK), you can immediately terminate any containers that are running when the deployment completes. This is most useful for development workflows, as it guarantees that any subsequent input will be handled by containers running the new version of the App. This trades off some downtime for certainty about when the new version will be in use.\n- The disruptive "recreate" strategy is also useful when your App runs at its `max_containers` limit, as otherwise we are unable to bring up replacement capacity.\n- The `modal serve` command now uses a "recreate" strategy during code updates.\n- The default "rolling" strategy is unchanged. This strategy prioritizes uptime, but means that old containers may still continue handling inputs for some time.\n\nWe\'ve also included a number of smaller new features and improvements:\n\n- Sandboxes now accept an `include_oidc_identity_token` parameter in `modal.Sandbox.create`. When set to `True`, a `MODAL_IDENTITY_TOKEN` environment variable will be injected into the Sandbox, enabling OIDC-based authentication (e.g., for AWS federation). See the [OIDC integration guide](https://modal.com/docs/guide/oidc-integration) for more details.\n- The new `modal.Image.from_scratch()` constructor creates an empty Image, equivalent to `FROM scratch` in Docker. This is primarily useful as a lightweight filesystem to mount into a Sandbox via `modal.Sandbox.mount_image`.\n- The `modal container list` command now accepts an `--app-id` filter to return containers for a specific App.\n- We\'ve addressed an issue where `modal.Sandbox.exec` could hang if the Sandbox had terminated immediately after creation.\n- An exception is now raised if the *same* Volume or CloudBucketMount is mounted at multiple paths in a container.\n- The client will now error faster (≈60s) if it cannot establish an initial connection the Modal servers.\n\nFinally, we are introducing a small number of breaking changes and enforcing some deprecations of pre-1.0 APIs:\n\n- Exceptions returned by `modal.Function.map()` are no longer wrapped in a `UserCodeException` type, and we\'re deprecating the transitional `wrap_returned_exceptions=` parameter.\n- The `modal.enable_output()` context manager no longer yields a value; this had briefly leaked an internal type.\n- We\'ve removed unused `namespace` parameters from a number of APIs.\n- It\'s now required to pass `-m` on the CLI when using a module path spelling of the Function reference (e.g. `modal deploy -m project.app`)\n- We\'ve removed backwards compatibility for the old autoscaler configuration (`keep_warm`, `concurrency_limit`, etc.).\n- It\'s no longer possible to look up a specific method on a Cls using `modal.Function.from_name`; use `modal.Cls.from_name` instead.\n\n## 1.3\n\n### 1.3.5 (2026-03-03)\n\n- We\'ve added a `modal changelog` CLI for retrieving changelog entries with a flexible query interface (e.g. `modal changelog --since=1.2`, `modal changelog --since=2025-12-01`, `modal changelog --newer`). We expect that this will be a useful way to surface information about new features to coding agents.\n- We\'ve added a new `modal.Secret.update` method, which allows you to programmatically modify the environment variables within a Secret. This method has the semantics of Python\'s `dict.update`: Secret contents can be overwritten or extended when using it. Note that Secret updates will take effect only for containers that start up after the modification.\n- The dataclass returned by `modal.Function.get_current_stats()` now includes a `num_running_inputs` field that reports the number of inputs the Function is currently handling.\n\n\n### 1.3.4 (2026-02-23)\n\n- We\'re introducing "Directory Snapshots": a new beta feature for persisting specific directories past the lifetime of an individual Sandbox. Using the new methods `modal.Sandbox.snapshot_directory()` and `modal.Sandbox.mount_image()`, you can capture the state of a directory and then later include it in a different Sandbox:\n  ```python notest\n  sb = modal.Sandbox.create(app=app)\n  snapshot = sb.snapshot_directory("/project")\n\n  sb2 = modal.Sandbox.create(app=app)\n  sb2.mount_image("/project", snapshot)\n  ```\n  This feature can be useful for separating the lifecycle of application code in the Sandbox\'s main Image from project code that changes in each Sandbox session. Files in the mounted snapshot also benefit from several optimizations that allow them to be read faster. See the [Sandbox Snapshot guide](https://modal.com/docs/guide/sandbox-snapshots) for more information.\n- We\'ve added a new `modal.Sandbox.detach()` method that we recommend calling after you are done interacting with a Sandbox. This method disconnects your local client from the Sandbox and cleans up resources associated with the connection. After calling `detach`, operations on the Sandbox object may raise and are otherwise not guaranteed to work.\n- The `modal.Sandbox.terminate()` method now accepts a `wait` parameter. With `wait=True`, `terminate` will block until the Sandbox is finished and return the exit code. The default `wait=False` maintains the previous behavior.\n- Throughput for writing to the `stdin` of a `modal.Sandbox.exec` process has been increased by 8x.\n- We\'ve added a new `modal.Volume.from_id()` method for referencing a Volume by its object id.\n\n### 1.3.3 (2026-02-12)\n\n- We\'ve added a new `modal billing report` CLI and promoted the `modal.billing.workspace_billing_report` API to General Availability for all Team and Enterprise plan workspaces.\n- We\'ve added `modal.Queue.from_id()` and `modal.Dict.from_id()` methods to support referencing a Queue or Dict by its object id.\n- Modal\'s async usage warnings are now enabled by default. These warnings will fire when using a [blocking interface on a Modal object](https://modal.com/docs/guide/async) in an async context. We\'ve aimed to provide detailed and actionable suggestions for how to modify the code, which makes the warnings verbose. While we recommend addressing any warnings that pop up, as they can point to significant performance issues or bugs, we also provide a configuration option to disable them (`MODAL_ASYNC_WARNINGS=0` or `async_warnings = false` in the `.modal.toml`). Please report any apparent false positives or incorrect suggested fixes.\n- We\'ve fixed a bug where the ASGI scope\'s `state` contents could leak between requests when using `@modal.asgi_app`.\n\n### 1.3.2 (2026-01-30)\n\n- Modal objects now have a `.get_dashboard_url()` method. This method will return a URL for viewing that object on the Modal dashboard:\n  ```python\n  fc = f.spawn()\n  print(fc.get_dashboard_url())  # Easy access to logs, etc.\n  ```\n- There is also a new `modal dashboard` CLI and new `modal app dashboard` / `modal volume dashboard` CLI subcommands:\n  ```bash\n  modal dashboard  # Opens up the Apps homepage for the current environment\n  modal dashboard <object-id>  # Opens up a view of this object\n  modal app dashboard <app-name>  # Opens up the dashboard for this deployed App\n  modal volume dashboard <volume-name>  # Opens up the file browser for this persistent Volume\n  ```\n- You can now pass a Sandbox ID (`sb-xxxxx`) directly to the `modal container logs` CLI.\n- The `modal token info` CLI will now include the token name, if provided at token creation.\n- We\'ve fixed an issue where `modal.Cls.with_options()` (or the `with_concurrency()` / `with_batching()` methods) could sometimes use stale argument values when called repeatedly.\n\n\n### 1.3.1 (2026-01-22)\n\n- We\'ve improved our experimental support for Python 3.14t (free-threaded Python) inside Modal containers.\n  - The container environment will now use the Python implementation of the Protobuf runtime rather than the incompatible `upb` implementation.\n  - As 3.14t images are not being published to the official source for our prebuilt `modal.Image.debian_slim()` images, we recommend using `modal.Image.from_registry` to build a 3.14t Image:\n    ```python\n    modal.Image.from_registry("debian:bookworm-slim", add_python="3.14t")\n    ```\n  - Note that 3.14t support is available only on the 2025.06 [Image Builder Version](https://modal.com/settings/image-config).\n  - Support is still experimental, so please share any issues that you encounter running 3.14t in Modal containers.\n- It\'s now possible to provide a `custom_domain` for a `modal.Sandbox`:\n  ```python\n  sb = modal.Sandbox.create(..., custom_domain="sandboxes.mydomain.com")\n  ```\n  Note that Sandbox custom domains work differently from Function custom domains and must currently be set up manually by Modal; please get in touch if this feature interests you.\n- We added a new `modal token info` CLI command to retrieve information about the credentials that are currently in use.\n- We added a `--timestamps` flag to a number of CLI entrypoints (`modal run`, `modal serve`, `modal deploy`, and `modal container logs`) to show timestamps in the logging output.\n- The automatic CLI creation for `modal run` entrypoints now supports `Literal` type annotations, provided that the literal type contains either all `str` or all `int` values.\n- We\'ve fixed a bug that could cause App builds to fail with an uninformative `CancelledError` when the App was misconfigured.\n- We\'ve improved client resource management when running `modal.Sandbox.exec`, which avoids a rare thread race condition.\n\n### 1.3.0 (2025-12-19)\n\nModal now supports Python 3.14. Python 3.14t (the free-threading build) support is currently a work in progress, because we are waiting for dependencies to be updated with free-threaded support. Additionally, Modal no longer supports Python 3.9, which has reached [end-of-life](https://devguide.python.org/versions).\n\nWe are adding experimental support for detecting cases where Modal\'s blocking APIs are used in async contexts (which can be a source of bugs or performance issues). You can opt into runtime warnings by setting `MODAL_ASYNC_WARNINGS=1` as an environment variable or `async_warnings = true` as a config field. We will enable these warnings by default in the future; please report any apparent false positives or other issues while support is experimental.\n\nThis release also includes a small number of deprecations and behavioral changes:\n\n- The Modal SDK will no longer propagate `grpclib.GRPCError` types out to the user; our own `modal.Error` subtypes will be used instead. To avoid disrupting user code that has relied on `GRPCError` exceptions for control flow, we are temporarily making some exception types inherit from `GRPCError` so that they will also be caught by `except grpclib.GRPCError` statements. Accessing the `.status` attribute of the exception will issue a deprecation warning, but warnings cannot be issued if the exception object is only caught and there is no other interaction with it. We advise proactively migrating any exception handling to use Modal types, as we will remove the dependency on `grpclib` types entirely in the future. See the [`modal.exception`](https://modal.com/docs/sdk/py/latest/exception) docs for the mapping from gRPC status codes to Modal exception types.\n- The `max_inputs` parameter in the `@app.function()` and `@app.cls` decorators has been renamed to `single_use_containers` and now takes a boolean value rather than an integer. Note that only `max_inputs=1` has been supported, so this has no functional implications. This change is being made to reduce confusion with `@modal.concurrent(max_inputs=...)` and so that Modal\'s autoscaler can provide better performance for Functions with single-use containers.\n- The async (`.aio`) interface has been deprecated from `modal.FunctionCall.from_id`, `modal.Image.from_id`, and `modal.SandboxSnapshot.from_id`, because these methods do not perform I/O.\n- The `replace_bytes` and `delete_bytes` methods have been removed from the `modal.file_io` filesystem interface.\n- Images built with `modal.Image.micromamba()` using the 2023.12 [Image Builder Version](https://modal.com/docs/guide/images#image-builder-updates) will now use a Python version that matches their local environment by default, rather than defaulting to Python 3.9.\n\n## 1.2\n\n### 1.2.6 (2025-12-16)\n\n- Fixed bug where iterating on a `modal.Sandbox.exec` output stream could raise unauthenticated errors.\n\n### 1.2.5 (2025-12-12)\n\n- It is now possible to set a custom `name=` for a Function without using `serialized=True`. This can be useful when decorating a function multiple times, e.g. applying multiple Modal configurations to the same implementation.\n- It is now possible to start `modal shell` with a Modal Image ID (`modal shell im-abc123`). Additionally, `modal shell` will now warn if you pass invalid combinations of arguments (like `--cpu` together with the ID of an already running Sandbox, etc.).\n- Fixed a bug in `modal shell` that caused e.g. `vi` to fail with unicode decode errors.\n- Fixed a thread-safety issue in `modal.Sandbox` resource cleanup.\n- Improved performance when adding large local directories to an Image.\n- Improved async Sandbox performance by not blocking the event loop while reading from `stdout` or `stderr`.\n\n### 1.2.4 (2025-11-21)\n\n- Fixed a bug in `modal.Sandbox.exec` when using `stderr=StreamType.STDOUT` (introduced in v1.2.3).\n- Added a new `h2_enabled` option in `modal.forward`, which enables HTTP/2 advertisement in TLS establishment.\n\n### 1.2.3 (2025-11-20)\n\n- CPU Functions can now be configured to run on non-preemptible capacity by setting `nonpreemptible=True` in the `@app.function()` or `@app.cls()` decorator. This feature is not currently available when requesting a GPU. Note that non-preemptibility incurs a 3x multiplier on CPU and memory pricing. See the [Guide](https://modal.com/docs/guide/preemption) for more information on preemptions.\n- The Modal client can now respond more gracefully to server throttling (e.g., rate limiting) by backing off and automatically retrying. This behavior can be controlled with a new `MODAL_MAX_THROTTLE_WAIT` config variable. Setting the config to `0` will preserve the previous behavior and treat rate limits as an exception; setting it to a nonzero number (the unit is seconds) will allow a limited duration of retries.\n- The `modal.Sandbox.exec` implementation has been rewritten to be more reliable and efficient.\n- Added a new `--add-local` flag to `modal shell`, allowing local files and directories to be included in the shell\'s container.\n- Fixed a bug introduced in v1.2.2 where some Modal objects (e.g., `modal.FunctionCall`) were not usable after being captured in a Memory Snapshot. The bug would result in a `has no loader function` error when the object was used.\n\n### 1.2.2 (2025-11-10)\n\n- `modal.Image.run_commands` now supports `modal.Volume` mounts. This can be helpful for accelerating builds by keeping a package manager cache on the Volume:\n\n  ```python\n  cache_vol = modal.Volume.from_name("cache-mount")\n  cmd_using_cache = "..."\n  image = modal.Image.debian_slim().run_commands(cmd_using_cache, volumes={"/cache": cache_vol})\n  ```\n\n- All Modal objects now accept an optional `modal.Client` object in their constructor methods. Passing an explicit client can be helpful in cases where Modal credentials are retrieved from within the Python process that is making requests.\n- The `name=` passed to `modal.Sandbox.create` and `modal.Sandbox.from_name` is now required to follow other Modal object naming rules (must contain only alphanumeric characters, dashes, periods, or underscores and cannot exceed 64 characters). Passing an invalid name will now error.\n- `modal.CloudBucketMount` now supports `force_path_style=True` to disable virtual-host-style addressing. See [mountpoint-s3 endpoints docs](https://github.com/awslabs/mountpoint-s3/blob/main/doc/CONFIGURATION.md#endpoints-and-aws-privatelink) for details.\n- The output from `modal config show` is now valid JSON and can be parsed by CLI tools such as `jq`.\n- Fixed a bug where App tags were not attached to Image builds that occur when first deploying the App.\n\n### 1.2.1 (2025-10-22)\n\n- It\'s now possible to override the default `-dev` suffix applied to the autogenerated URLs for ephemeral Apps (i.e., when using `modal serve`) via a new `dev_suffix` field in the `.modal.toml` config file, or equivalently with the `MODAL_DEV_SUFFIX` environment variable. This can help avoid collisions when multiple users of a workspace are working on the same codebase simultaneously.\n- Fixed a bug where reading long stdout/stderr from `modal.Sandbox.exec()` could break in `text=True` mode.\n- Fixed a bug where the status code was not checked when downloading a file from a Volume.\n- `modal run --detach ...` will now exit more gracefully if you lose internet connection while your App is running.\n\n### 1.2.0 (2025-10-09)\n\nIn this release, we\'re introducing the concept of "App tags", which are simple key-value metadata that can be included to provide additional organizational context. Tags can be defined as part of the `modal.App` constructor:\n\n```python\napp = modal.App("llm-inference-server", tags={"team": "genai-platform"})\n```\n\nTags can also be added to an active App via the new `modal.App.set_tags()` method, and current tags can be retrieved with the new `modal.App.get_tags()`.\n\nThis release also introduces a new API for generating a tabular billing report: `modal.billing.workspace_billing_report()`. The billing API will report the cost incurred by each App, aggregated over time intervals (currently supporting a daily or hourly resolution). The report can optionally include App tags, allowing you to perform cost allocation using your own organizational schema.\n\nNote that the initial release of the billing API is a private beta. Please get in touch to discuss access.\n\nThis release also includes some internal changes to Function input/output serialization. These changes will provide better support for calling into Modal Functions from our `modal-js` and `modal-go` SDKs. Versions 0.4 or later of `modal-js` and `modal-go` will only be able to invoke Functions in Apps deployed with version 1.2 or later of the Python SDK.\n\nOther new features and improvements:\n\n- The new `modal.Sandbox.create_connect_token()` method facilitates authentication for making HTTP / Websocket requests to a server running in a Sandbox:\n\n  ```python notest\n  sb = modal.Sandbox.create(...)\n\n  # Create a connect token, optionally including arbitrary user metadata\n  creds = sb.create_connect_token(user_metadata={"user_id": "user123"})\n\n  # Make an http request, passing the token in the authorization header\n  requests.get(creds.url, headers={"Authorization": f"Bearer {creds.token}"})\n  ```\n\n  See the [Sandbox Networking guide](https://modal.com/docs/guide/sandbox-networking) for more information.\n\n- The new `modal.Image.build()` method allows you to eagerly trigger an Image build. This is particularly helpful when working with Sandboxes, as otherwise the Image build would happen lazily inside `modal.Sandbox.create()`:\n\n  ```python notest\n  app = modal.App.lookup("sandbox-app")\n  image = modal.Image.from_registry("ubuntu")\n\n  # This step will block until the build completes\n  image.build(app)\n\n  # Now the Sandbox will be created and scheduled immediately\n  sb = modal.Sandbox.create(app=app, image=image)\n  ```\n\n- We\'ve added an `env` parameter to a number of methods that configure Function, Sandbox, or Image execution. This parameter accepts a dictionary and adds the contents as environment variables in the relevant Modal container. This allows for simpler inclusion of non-sensitive information compared to using a `modal.Secret`.\n- It\'s now possible to pass a `modal.CloudBucketMount` instance to the `volumes=` parameter of `modal.Cls.with_options` (previously, only dynamic addition of `modal.Volume` mounts was supported).\n- The new `modal.Sandbox.get_tags()` method will fetch the tags currently in use by the Sandbox (i.e., after calling `modal.Sandbox.set_tags()`). Note that Sandbox tags are distinct from the new concept of App tags.\n- `modal.Dict.pop()` now accepts an optional `default` parameter, akin to Python\'s `dict.pop()`.\n- It\'s now possible to `modal shell` into a running Sandbox by passing its Sandbox ID (`modal shell sb-123`).\n- Sandboxes can now be configured to expose a PTY device via `Sandbox.create(..., pty=True)` and `Sandbox.exec(..., pty=True)`. This provides better support for Claude Code.\n- The new `modal.experimental.image_delete()` function can be used to delete the final layer of an Image given its ID, which can be particularly useful for cleaning up Sandbox Filesystem Snapshots.\n- Using `modal run --interactive` (or `-i`) will now suppress Modal\'s status spinner to avoid interfering with breakpoints in local entrypoint functions. We\'ve also improved support for printing large objects when attached to a debugger.\n- We\'ve improved support for Protobuf 5+ when using the Python implementation of the Protobuf runtime.\n\nThis release also introduces a small number of new deprecations:\n\n- We deprecated the `client` parameter from `Sandbox.set_tags()`. To use an explicit Client when interacting with the Sandbox, pass it into `modal.Sandbox.create()` instead.\n- We deprecated the `pty_info` parameter from `Sandbox.create()` and `Sandbox.exec()`. This was a private parameter accepting an internal Protobuf type. See the new boolean `pty` parameter instead.\n- We replaced the `--no-confirm` option with `--yes` in the `modal environment delete` CLI to align with other CLI commands that normally require confirmation.\n\nFinally, some functionality that began issuing deprecation warnings prior to v0.73 has now been completely removed:\n\n- It is now required to "instantiate" a `modal.Cls` before invoking one of its methods.\n- The eager `.lookup()` method has been removed from most Modal object classes (but not from `modal.App.lookup`, which remains supported). The lazy `.from_name()` method is recommended for accessing deployed objects going forward.\n- The public constructors on the `modal.mount.Mount` object have been removed; this is now an entirely internal class.\n- The `context_mount=` parameter has accordingly been removed from Docker-oriented `modal.Image` methods.\n- The unused `allow_cross_region_volumes` parameter has been removed from the function decorators.\n- The `modal.experimental.update_autoscaler()` function has been removed; this functionality now has a stable API as `modal.Function.update_autoscaler()`.\n\n## 1.1\n\n### 1.1.4 (2025-09-03)\n\n- Added a `startup_timeout` parameter to the `@app.function()` and `@app.cls()` decorators. When used, this configures the timeout applied to each container\'s startup period separately from the input `timeout`. For backwards compatibility, `timeout` still applies to the startup phase when `startup_timeout` is unset.\n- Added an optional `idle_timeout` parameter to `modal.Sandbox.create()`. When provided, Sandboxes will terminate after `idle_timeout` seconds of idleness.\n- The dataclass returned by `modal.experimental.get_cluster_info()` now includes a `cluster_id` field to identify the clustered set of containers.\n- When `block_network=True` is set in `modal.Sandbox.create()`, we now raise an error if any of `encrypted_ports`, `h2_ports`, or `unencrypted_ports` are also set.\n- Functions decorated with `@modal.asgi_app()` now return an HTTP 408 (request timeout) error code instead of a 502 (gateway timeout) in rare cases when an input fails to arrive at the container, e.g. due to cancellation.\n- `modal.Sandbox.create()` now warns when an invalid `name=` is passed, applying the same rules as other Modal object names: names must be alphanumeric and not longer than 64 characters. This will become an error in the future.\n\n### 1.1.3 (2025-08-19)\n\n- Fixed a bug introduced in `v1.1.2` that causes invocation of `modal.FunctionCall.get`, `modal.FunctionCall.get_call_graph`, `modal.FunctionCall.cancel`, and `modal.FunctionCall.gather` to fail when the `FunctionCall` object is retrieved via `modal.FunctionCall.from_id`.\n- Added retries to improve the robustness of `modal volume get`\n\n### 1.1.2 (2025-08-14)\n\nWe\'re introducing a new API pattern for imperative management of Modal resource types (`modal.Volume`, `modal.Secret`, `modal.Dict`, and `modal.Queue`). The API is accessible through the `.objects` namespace on each class. The object management namespace has methods for the following operations:\n\n- `.objects.create(name)` creates an object on our backend. E.g., with [`modal.Volume.objects.create`](https://modal.com/docs/sdk/py/latest/Volume#create):\n  ```python notest\n  modal.Volume.objects.create("huggingface-cache", environment_name="dev")\n  ```\n- `.objects.delete(name)` deletes the object with that name. E.g., with [`modal.Secret.objects.delete`](https://modal.com/docs/sdk/py/latest/Secret#delete):\n  ```python notest\n  modal.Secret.objects.delete("aws-token")\n  ```\n- `.objects.list()` returns a list of object instances. E.g., with [`modal.Queue.objects.list`](https://modal.com/docs/sdk/py/latest/Queue#list):\n  ```python notest\n  for queue in modal.Queue.objects.list():\n      queue_info = queue.info()\n      print(queue_info.name, queue_info.created_at, queue.len())\n  ```\n\nWith the introduction of these APIs, we\'re replacing a few older methods with similar functionality:\n\n- Static `.delete()` methods on the resource types themselves are being deprecated, because they are too easily confused with operations on the _contents_ of a resource (i.e., calling `modal.Dict.delete(key_name)` is an easy mistake that can have significant adverse consequences).\n- The undocumented `.create_deployed()` methods of `modal.Volume` and `modal.Secret` are being deprecated in favor of this consistent API for imperative management.\n\nOther changes:\n\n- `modal.Cls.with_options` now supports `region` and `cloud` keyword arguments to support runtime constraints on scheduling.\n- Fixed a bug that could cause Image builds to fail with `\'FilePatternMatcher\' object has no attribute \'patterns\'` when using a `modal.FilePatternMatcher.from_file` ignore pattern.\n- Fixed a bug where `rdma=True` was ignored when using `@modal.experimental.clustered()` with a `modal.Cls`.\n\n### 1.1.1 (2025-08-01)\n\nWe\'re introducing the concept of "named Sandboxes" for usecases where Sandboxes need to have unique ownership over a resource. A named Sandbox can be created by passing `name=` to `modal.Sandbox.create()`, and it can be retrieved with the new `modal.Sandbox.from_name()` constructor. Only one running Sandbox can use a given name (scoped within the App that is managing the Sandbox) at any time, so trying to create a Sandbox with a name that is already taken will fail. Sandboxes release their name when they terminate. See the [guide](https://modal.com/docs/guide/sandbox#named-sandboxes) for more information about using this new feature.\n\nOther changes:\n\n- We\'ve made an internal change to the `modal.Image.uv_pip_install` method to make it more portable across different base Images. As a consequence, Images built with this method on 1.1.0 will need to rebuild the next time they are used.\n- We\'ve added a `.name` property and `.info()` method to `modal.Dict`, `modal.Queue`, `modal.Volume`, and `modal.Secret` objects.\n- Sandboxes now support `experimental_options` configuration for enabling preview functionality.\n- We\'ve Improved Modal\'s rich output when used in a Jupyter notebook.\n\n### 1.1.0 (2025-07-17)\n\nThis release introduces support for the `2025.06` [Image Builder Version](https://modal.com/docs/guide/images#image-builder-updates), which is in a "preview" state. The new image builder includes several major changes to how the Modal client dependencies are included in Modal Images. These improvements should greatly reduce the risk of conflicts with user code dependencies. They also allow Modal Sandboxes to easily be used with existing Images or Dockerfiles that are not themselves compatible with the Modal client library. You can see more details and update your Workspace on its [Image Config](https://modal.com/settings/image-config) page. Please share any issues that you encounter as we work to make the version stable.\n\nWe\'re also introducing first-class support for building Modal Images with the [uv package manager](https://docs.astral.sh/uv/) through the new [`modal.Image.uv_pip_install`](https://modal.com/docs/sdk/py/latest/Image#uv_pip_install) and [`modal.Image.uv_sync`](https://modal.com/docs/sdk/py/latest/Image#uv_sync) methods:\n\n```python\nimport modal\n\n# uv_pip_install accepts a list of packages, like pip_install, but up to 50% faster\nimage = modal.Image.debian_slim().uv_pip_install("torch==2.7.1", "numpy==2.3.1")\n\n# uv_sync accepts a local `uv_project_dir` (defaulting to the local working directory)\n# and uses the pyproject.toml and uv.lock files to specify the environment\nimage = modal.Image.debian_slim().uv_sync()\n```\n\nPlease note that, as these methods are new, there is some chance that future releases will need to fix bugs or address edge cases in ways that break the cache for existing Images. When using `modal.Image.uv_pip_install`, we recommend pinning dependency versions so that any necessary rebuilds produce a consistent environment.\n\nThis release also includes a number of other new features and bug fixes:\n\n- Optimized handling of the `ignore` parameter in `Image.add_local_dir` and similar methods for cases where entire directories are ignored.\n- Added a `poetry_version` parameter to `modal.Image.poetry_install_from_file`, which supports installing a specific version of `poetry`. It\'s also possible to set `poetry_version=None` to skip the install step, i.e. when poetry is already available in the Image.\n- Added a [`modal.Sandbox.reload_volumes`](https://modal.com/docs/sdk/py/latest/Sandbox#reload_volumes) method, which triggers a reload of all Volumes currently mounted inside a running Sandbox.\n- Added a `build_args` parameter to `modal.Image.from_dockerfile` for passing arguments through to `ARG` instructions in the Dockerfile.\n- It\'s now possible to use `@modal.experimental.clustered` and `i6pn` networking with `modal.Cls`.\n- Fixed a bug where `Cls.with_options` would fail when provided with a `modal.Secret` object that was already hydrated.\n- Fixed a bug where the timeout specified in `modal.Sandbox.exec()` was not respected by `ContainerProcess.wait()` or `ContainerProcess.poll()`.\n- Fixed retry handling when using `modal run --detach` directly against a remote Function.\n\nFinally, this release introduces a small number of deprecations and potentially-breaking changes:\n\n- We now raise `modal.exception.NotFoundError` in all cases where Modal object lookups fail; previously some methods could leak an internal `GRPCError` with a `NOT_FOUND` status.\n- We\'re enforcing pre-1.0 deprecations on `modal.build`, `modal.Image.copy_local_file`, and `modal.Image.copy_local_dir`.\n- We\'re deprecating the `environment_name` parameter in `modal.Sandbox.create()`. A Sandbox\'s environment association will now be determined by its parent App. This should have no user-facing effects.\n- We\'ve deprecated the `namespace` parameter in the `.from_name` methods of `Function`, `Cls`, `Dict`, `Queue`, `Volume`, `NetworkFileSystem`, and `Secret`, along with `modal.runner.deploy_app`. These object types do not have a concept of distinct namespaces.\n\n## 1.0\n\n### 1.0.5 (2025-06-27)\n\n- Added a [`modal.Volume.read_only`](/docs/sdk/py/latest/Volume#read_only) method, which will configure a Volume instance to disallow writes:\n\n  ```python notest\n  vol = modal.Volume.from_name("models")\n  read_only_vol = vol.read_only()\n\n  @app.function(volumes={"/models": read_only_vol})\n  def f():\n      with open("/models/weights.pt", "w") as fid:  # Raises an OSError\n          ...\n\n  @app.local_entrypoint()\n  def main():\n      with read_only_vol.batch_upload() as batch:  # Raises a modal.exceptions.InvalidError\n          ...\n\n      with vol.batch_upload() as batch:  # This instance is still writeable\n          ...\n  ```\n\n- Introduced a gradual fix for a bug where `Function.map` and `Function.starmap` leak an internal exception wrapping type (`modal.exceptions.UserCodeException`) when `return_exceptions=True` is set. To avoid breaking any user code that depends on the specific types in the return list, these functions will continue returning the wrapper type by default, but they now issue a deprecation warning. To opt into the future behavior and silence the warning, you can set `wrap_returned_exceptions=False` in the call to `.map` or `.starmap`.\n- When an `@app.cls()`-decorated class inherits from a class (or classes) with `modal.parameter()` annotations, the parent parameters will now be inherited and included in the parameter set for the modal Cls.\n- Redeployments that migrate parameterized functions from an explicit constructor to `modal.parameter()` annotations will now handle requests from outdated clients more gracefully, avoiding a problem where new containers would crashloop on a deserialization error.\n- The Modal client will now retry its initial connection to the Modal server, improving stability on flaky networks.\n\n### 1.0.4 (2025-06-13)\n\n- When `modal.Cls.with_options` is called multiple times on the same instance, the overrides will now be merged. For example, the following configuration will use an H100 GPU and request 16 CPU cores:\n  ```python\n  Model.with_options(gpu="A100", cpu=16).with_options(gpu="H100")\n  ```\n- Added a `--secret` option to `modal shell` for including environment variables defined by named Secret(s) in the shell session:\n  ```\n  modal shell --secret huggingface --secret wandb\n  ```\n- Added a `verbose: bool` option to `modal.Sandbox.create()`. When this is set to `True`, execs and file system operations will appear in the Sandbox logs.\n- Updated `modal.Sandbox.watch()` so that exceptions are now raised in (and can be caught by) the calling task.\n\n### 1.0.3 (2025-06-05)\n\n- Added support for specifying a timezone on `Cron` schedules, which allows you to run a Function at a specific local time regardless of daylight savings:\n\n  ```python\n  import modal\n  app = modal.App()\n\n  @app.function(schedule=modal.Cron("* 6 * * *"), timezone="America/New_York")  # Use tz database naming conventions\n  def f():\n      print("This function will run every day at 6am New York time.")\n  ```\n\n- Added an `h2_ports` parameter to `Sandbox.create`, which exposes encrypted ports using HTTP/2. The following example will create an H2 port on 5002 and a port using HTTPS over HTTP/1.1 on 5003:\n  ```python\n  sb = modal.Sandbox.create(app=app, h2_ports = [5002], encrypted_ports = [5003])\n  ```\n- Added `--from-dotenv` and `--from-json` options to `modal secret create`, which will read from local files to populate Secret contents.\n- `Sandbox.terminate` no longer waits for container shutdown to complete before returning. It still ensures that a terminated container will shutdown imminently. To restore the previous behavior (i.e., to wait until the Sandbox is actually terminated), call `sb.wait(raise_on_termination=False)` after calling `sb.terminate()`.\n- Improved performance and stability for `modal volume get`.\n- Fixed a rare race condition that could sometimes make `Function.map` and similar calls deadlock.\n- Fixed an issue where `Function.map` and similar methods would stall for 55 seconds when passed an empty iterator as input instead of completing immediately.\n- We now raise an error during App setup when using interactive mode without the `modal.enable_output` context manager. Previously, this would run the App but raise when `modal.interact()` was called.\n\n### 1.0.2 (2025-05-26)\n\n- Fixed an incompatibility with breaking changes in `aiohttp` v3.12.0, which caused issues with Volume and large input uploads. The issues typically manifest as `Local data and remote data checksum mismatch` or `\'_io.BufferedReader\' object has no attribute \'getbuffer\'` errors.\n\n### 1.0.1 (2025-05-19)\n\n- Added a `--timestamps` flag to `modal app logs` that prepends a timestamp to each log line.\n- Fixed a bug where objects returned by `Sandbox.list` had `returncode == 0` for _running_ Sandboxes. Now the return code for running Sandboxes will be `None`.\n- Fixed a bug affecting systems where the `sys.platform.node` name includes unicode characters.\n\n### 1.0.0 (2025-05-16)\n\nWith this release, we\'re beginning to enforce the deprecations discussed in the [1.0 migration guide](https://modal.com/docs/guide/modal-1-0-migration). Going forward, we\'ll include breaking changes for outstanding deprecations in `1.Y.0` releases, so we recommend pinning Modal on a minor version (`modal~=1.0.0`) if you have not addressed the existing warnings. While we\'ll continue to make improvements to the Modal API, new deprecations will be introduced at a substantially reduced rate, and support windows for older client versions will lengthen.\n\n⚠️ In this release, we\'ve made some breaking changes to Modal\'s "automounting" behavior.️ If you\'ve not already adapted your source code in response to warnings about automounting, Apps built with 1.0+ will have different files included and may not run as expected:\n\n- Previously, Modal containers would automatically include the source for local Python packages that were imported by your Modal App. Going forward, it will be necessary to explicitly include such packages in the Image (i.e., with `modal.Image.add_local_python_source`).\n- Support for the `automount` configuration (`MODAL_AUTOMOUNT`) has been removed; this environment variable will no longer have any effect.\n- Modal will continue to automatically include the Python module or package where the Function is defined. If the Function is defined within a package, the entire directory tree containing the package will be mounted. This limited automounting can also be disabled in cases where your Image definition already includes the package defining the Function: set `include_source=False` in the `modal.App` constructor or `@app.function` decorator.\n\nAdditionally, we have enforced a number of previously-introduced deprecations:\n\n- Removed `modal.Mount` as a public object, along with various `mount=` parameters where Mounts could be passed into the Modal API. Usage can be replaced with `modal.Image` methods, e.g.:\n  ```python\n  @app.function(image=image, mounts=[modal.Mount.from_local_dir("data", "/root/data")])  # This is now an error!\n  @app.function(image=image.add_local_dir("data", "/root/data"))  # Correct spelling\n  ```\n- Removed the `show_progress` parameter from `modal.App.run`. This parameter has been replaced by the `modal.enable_output` context manager:\n  ```python\n  with modal.enable_output(), app.run():\n    ...  # Will produce verbose Modal output\n  ```\n- Passing flagged options to the `Image.pip_install` package list will now raise an error. Use the `extra_options` parameter to specify options that aren\'t exposed through the `Image.pip_install` signature:\n  ```python\n  image.pip_install("flash-attn", "--no-build-isolation")  # This is now an error!\n  image.pip_install("flash-attn", extra_options="--no-build-isolation")  # Correct spelling\n  ```\n- Removed backwards compatibility for using `label=` or `tag=` keywords in object lookup methods. We standardized these methods to use `name=` as the parameter name, but we recommend using positional arguments:\n  ```python\n  f = modal.Function.from_name("my-app", tag="f")  # No longer supported! Will raise an error!\n  f = modal.Function.from_name("my-app", "f")  # Preferred spelling\n  ```\n- It\'s no longer possible to invoke a generator Function with `Function.spawn`; previously this warned, now it raises an `InvalidError`. Additionally, the `FunctionCall.get_gen` method has been removed, and it\'s no longer possible to set `is_generator` when using `FunctionCall.from_id`.\n- Removed the `.resolve()` method on Modal objects. This method had not been publicly documented, but where used it can be replaced straightforwardly with `.hydrate()`. Note that explicit hydration should rarely be necessary: in most cases you can rely on lazy hydration semantics (i.e., objects will be hydrated when the first method that requires server metadata is called).\n- Functions decorated with `@modal.asgi_app` or `@modal.wsgi_app` are now required to be nullary. Previously, we warned in the case where a function was defined with parameters that all had default arguments.\n- Referencing the deprecated `modal.Stub` object will now raise an `AttributeError`, whereas previously it was an alias for `modal.App`. This is a simple name change.\n',meta:{title:`Python SDK Release Notes`,description:`These release notes document user-facing updates (features, enhancements, fixes, and deprecations) to the modal client library.`}},{toc:ae,rawContent:f,meta:oe}=d,se=t(`<code>App.logs</code>`),ce=t(`<code>Image.logs</code>`),le=t(`<code>modal image logs</code>`),ue=t(`<code>Workspace.billing.rates()</code>`),de=t(`<code>modal billing rates</code>`),fe=t(`<code>Function.update_autoscaler()</code>`),pe=t(`<code>Server.update_autoscaler()</code>`),me=t(`<code>@app.server()</code>`),he=t(`<code>Server.update_autoscaler()</code>`),ge=t(`<code>Function</code>`),_e=t(`<code>Server</code>`),ve=t(`<code>FunctionCall</code>`),ye=t(`<code>modal.Workspace.billing.summary()</code>`),be=t(`<code>modal billing summary</code>`),xe=t(`<code>modal.Environment.billing.summary()</code>`),Se=t(`<code>modal environment billing summary</code>`),Ce=t(`<code>modal.Environment.roles</code>`),we=t(`<code>modal environment roles</code>`),Te=t(`<code>modal endpoint create</code>`),Ee=t(`<code>copy_from_local()</code>`),De=t(`<code>write_bytes()</code>`),Oe=t(`<code>write_text()</code>`),ke=t(`<code>modal.Workspace.settings.list()</code>`),Ae=t(`<code>modal workspace settings list</code>`),je=t(`<code>modal.Workspace.settings.set()</code>`),Me=t(`<code>modal workspace settings set</code>`),Ne=t(`<code>modal.types</code>`),Pe=t(`<code>modal.Function.with_options()</code>`),Fe=t(`<code>modal container stop</code>`),Ie=t(`<code>modal container logs</code>`),Le=t(`<code>modal.Workspace.members.list()</code>`),Re=t(`<code>modal.Sandbox.reload_volumes()</code>`),ze=t(`<code>modal.exception.TimeoutError</code>`),Be=t(`<code>@app.server()</code>`),Ve=t(`<code>modal endpoint</code>`),He=t(`<code>@app.server()</code>`),Ue=t(`<code>modal.Server</code>`),We=t(`<code>modal endpoint</code>`),Ge=t(`<code>modal.Workspace</code>`),Ke=t(`<code>modal.Environment</code>`),qe=t(`<code>workspace.billing.report()</code>`),Je=t(`<code>environment.billing.report()</code>`),Ye=t(`<code>modal environment billing</code>`),Xe=t(`<code>modal.billing.workspace_billing_report</code>`),Ze=t(`<code>modal.Workspace</code>`),Qe=t(`<code>workspace.proxy_tokens.create()</code>`),$e=t(`<code>workspace.proxy_tokens.list()</code>`),et=t(`<code>modal workspace proxy-tokens</code>`),tt=t(`<code>modal workspace members</code>`),nt=t(`<code>modal curl</code>`),rt=t(`<code>modal app rollback</code>`),it=t(`<code>modal deploy</code>`),at=t(`<code>modal app rollover</code>`),ot=t(`<code>modal.Sandbox.create_connect_token(port=...)</code>`),st=t(`<code>image.publish()</code>`),ct=t(`<code>modal.Image.from_id()</code>`),lt=t(`<code>image.build()</code>`),ut=t(`<code>modal skills</code>`),dt=t(`<code>modal.Image.publish()</code>`),ft=t(`<code>modal.Image.from_name()</code>`),pt=t(`<code>modal.Image.from_name()</code>`),mt=t(`<code>modal.exception.NotFoundError</code>`),ht=t(`<code>modal image names</code>`),gt=t(`<code>modal.Function.from_name()</code>`),_t=t(`<code>modal.Cls.from_name()</code>`),vt=t(`<code>modal.Sandbox.create()</code>`),yt=t(`<code>modal skills</code>`),bt=t(`<code>modal skills install</code>`),xt=t(`<code>modal skills update</code>`),St=t(`<code>modal.Workspace</code>`),Ct=t(`<code>workspace.members.list()</code>`),wt=t(`<code>modal</code>`),Tt=t(`<code>sandbox.filesystem.watch()</code>`),Et=t(`<code>modal.Sandbox.watch()</code>`),Dt=t(`<code>modal.Sandbox.snapshot_filesystem()</code>`),Ot=t(`<code>modal.Sandbox.snapshot_directory()</code>`),kt=t(`<code>modal.Sandbox.snapshot_directory()</code>`),At=t(`<code>modal.Sandbox.snapshot_filesystem()</code>`),jt=t(`<code>modal.exception.TimeoutError</code>`),Mt=t(`<code>modal.Volume</code>`),Nt=t(`<code>.objects.delete()</code>`),Pt=t(`<code>.objects.create()</code>`),Ft=t(`<code>modal.exception</code>`),It=t(`<code>modal.Volume.objects.create</code>`),Lt=t(`<code>modal.Secret.objects.delete</code>`),Rt=t(`<code>modal.Queue.objects.list</code>`),zt=t(`<code>modal.Image.uv_pip_install</code>`),Bt=t(`<code>modal.Image.uv_sync</code>`),Vt=t(`<code>modal.Sandbox.reload_volumes</code>`),Ht=t(`<code>modal.Volume.read_only</code>`),Ut=t(`<!> <p>These release notes document user-facing updates (features, enhancements, fixes, and deprecations) to the <code>modal</code> client library.</p> <!> <!> <p>With this release, it’s now possible to opt into a <!>, which affords substantially higher creation rates and concurrency. Set <code>MODAL_SANDBOX_V2=1</code> as an environment variable and your Sandboxes will use this new system without any code changes. This will become the default behavior in version 1.6.0 of the Python SDK. Note that the new backend does not support the deprecated FileIO-based Sandbox filesystem API; please <!> any code that is currently issuing deprecation warnings before enabling the feature flag.</p> <ul><li>We’ve added logs APIs to the <code>modal.App</code> and <code>modal.Image</code> objects: <ul><li>The <!> API allows you to <code>fetch()</code>, <code>tail()</code>, or <code>stream()</code> all logs from an App.</li> <li>The <!> API supports <code>fetch()</code> and <code>tail()</code> on Image build logs. Streaming build logs is not supported at this time.</li></ul></li> <li>We’ve also added a <!> CLI command with equivalent functionality.</li> <li>We now support querying basic information about your Workspace’s current pricing structure via the <!> API and <!> CLI.</li> <li>The <!> and <!> methods now return the complete state of the autoscaler configuration after applying the update.</li> <li>The <code>target_concurrency</code> parameter in <!> and <!> now supports fractional values for more fine-grained control over autoscaling behavior.</li></ul> <!> <ul><li>We’ve added new APIs for programmatically retrieving logs on several object types: <!>, <!>, and <!>. Each object exposes three different methods, allowing you to <code>stream()</code> logs as they are generated, <code>fetch()</code> logs from a specific date/time range, or <code>tail()</code> the most recent logs.</li> <li>It’s now possible to retrieve a summary of billing information across billing cycles: <ul><li>Use the <!> method or the <!> CLI to see workspace-level spend (broken down by category), credit usage, and the impact of any compute reservations.</li> <li>Use the <!> method or the <!> CLI to see environment-level spend.</li></ul></li> <li>We’ve made some changes to the interfaces for managing <!> permissions: <ul><li>We now explicitly represent all workspace member and service user roles in each Environment instead of having separate concepts for Environment membership and role.</li> <li>We’ve accordingly introduced a <!> interface (and <!> CLI), replacing the <code>modal.Environment.members</code> interface and <code>modal environment members</code> CLI, which are now deprecated.</li></ul></li> <li>It’s now possible to specify one or more <code>--compute-region</code> options in <!> to configure the region where Endpoint containers run.</li> <li>We’ve improved performance for large writes by ≈2.5x in several Sandbox filesystem methods (<!>, <!>, and <!>).</li></ul> <!> <ul><li>We’ve added new interfaces for programmatic management of workspace settings: <ul><li>Use the <!> method or <!> CLI to see current workspace-level settings.</li> <li>Use the <!> method or <!> CLI to configure new values.</li></ul></li> <li>We’ve added a <!> module for exposing dataclasses returned from public methods as public API. The types in this module are not typically constructed by user code, but it may be useful to reference them for, e.g., adding type annotations to code that wraps the relevant Modal API.</li> <li>The <!> method now accepts a <code>routing_region</code> argument to dynamically configure <!> at the time of invocation.</li> <li>The <!> CLI now accepts a <code>--graceful</code> flag. With it, the container stops fetching new inputs but finishes the inputs it is currently running before exiting, instead of having them cancelled and rescheduled. Graceful stops are only supported for containers running a Modal Function or Modal Server.</li> <li>The <!> CLI now includes logs from the startup phase of the container.</li> <li>The <!> method now uses <code>"member"</code> instead of <code>"user"</code> for the lowest-privilege Workspace role, matching the UI and documentation.</li> <li>The <!> method now blocks until the Volumes have been reloaded, bounded by a new <code>timeout</code> argument (55 seconds by default). If the reload does not complete within <code>timeout</code>, <!> is raised.</li></ul> <!> <p>This release includes several major new features, including the debut of a new serverless compute primitive for low-latency HTTP applications (<!>) and a new CLI for deploying production-ready LLM inference endpoints with minimal configuration (<!>).</p> <ul><li>We’re introducing a new <!> decorator (and <!> object), representing a serverless compute primitive that shares many features with Modal Functions while being optimized for serving HTTP-based applications with ultra-low latency. Read the <!> for more information.</li> <li>We’re also introducing the <!> CLI, providing programmatic access to our new <!> product. Endpoints let you deploy production-ready LLM inference servers with minimal configuration.</li> <li>We’ve added several new SDK features for workspace configuration and observability: <ul><li>We’ve added new billing API methods on the <!> and <!> objects (<!> and <!>, respectively). The new APIs include a resource-level cost breakdown (CPU, memory, and specific GPU types). We’ve also added a <!> CLI for generating environment-scoped billing reports. The new workspace-level API replaces the existing <!> function.</li> <li>We’ve added support for creating and managing <!> via the <!> object (<!>, <!>, etc.) and a new <!> CLI. Modal Servers and Endpoints are authenticated via proxy tokens by default.</li> <li>We’ve added a new <!> CLI for querying information about workspace membership.</li></ul></li> <li>The new <!> CLI command allows you to make requests to authenticated endpoints without explicitly passing proxy token headers. This is an experimental feature and may change in the future.</li> <li>The <!> command now accepts a <code>--strategy</code> (<code>rolling</code> or <code>recreate</code>), like <!> and <!>.</li> <li>Sandbox connect tokens can now be scoped to a custom port (<!>).</li> <li>It is now possible to call <!> on an Image constructed using <!> without first calling <!>.</li> <li>The Modal Python client now respects standard environment variables for HTTP CONNECT and SOCKS4/5 proxies (<code>HTTPS_PROXY</code> and <code>ALL_PROXY</code>). Proxy support requires installing extra dependencies, i.e. using <code>uv pip install 'modal[api-proxy-support]'</code>. To opt out of proxy support, set <code>MODAL_DISABLE_API_PROXY=1</code> or put <code>disable_api_proxy = true</code> in your <code>.modal.toml</code> profile.</li></ul> <!> <p>This is a major release that includes several new features (<!>, version-pinned Function lookups, <!>), a new <!> CLI, and a small number of breaking changes.</p> <ul><li>We’re introducing a “named Image” concept, akin to a Modal-native Image registry. This feature allows you to decouple Image builds from App deployment or Sandbox creation, and it simplifies the process of sharing a canonical Image across a large number of unrelated Apps. <ul><li>The new SDK method <!> assigns a name to an existing Image. Names can optionally include a “tag” suffix (<code>"&#123;name&#125;:&#123;tag&#125;"</code>) to specify variants of an Image, e.g. to apply a versioning system.</li> <li>The new SDK method <!> refers directly to that Image by its name. Unlike with Image methods that specify parts of a build recipe, the <!> lookup will either succeed or fail with a <!> error, but it will never trigger a build.</li> <li>The new <!> CLI can be used to see the current name assignments.</li></ul></li> <li>We’re introducing a “version-pinning” concept for Function lookups. By passing <code>version=</code> to <!> or <!>, you can retrieve an instance that will send all inputs to the same version of the Function, even after a subsequent redeployment of its App. Additionally, any transitive invocations of other Functions in the same App will also be pinned on that version. This feature makes it easy to execute workflows that involve multiple Functions when those Functions might change in incompatible ways across deployments.</li> <li>It’s now possible to restrict the <em>domains</em> that processes inside of a Sandbox can connect to. When you provide a list of domains to <code>outbound_domain_allowlist=[...]</code> in <!>, requests outside of the allowlist will be blocked by Modal infrastructure, and the denial will be recorded in the App logs.</li> <li>We’ve added a new <!> CLI for installing a foundational Modal agent skill (<!>) and keeping it up to date over time (<!>). Please help us improve it by sharing any feedback you have about its impact on agentic development with Modal.</li> <li>We’ve added a new <!> object for programmatic interaction with your Workspace configuration. The initial release offers a <!> method; expect more features soon.</li> <li>The <!> CLI now normalizes the keys of its <code>--json</code> output by lowercasing and substituting underscores for non-alphanumeric characters.</li> <li>We’ve added a new <!> method to the Sandbox Filesystem API, and we’ve deprecated the alpha <!> method. The new method has a different implementation and offers significantly improved latency and reliability.</li> <li>We’ve made two small breaking changes around Sandbox snapshots: <ul><li><!> and <!> now accept an explicit <code>ttl=</code> keyword argument that configures a retention interval (in seconds) for the resulting Image. Both methods default to 30 days (<code>ttl=30 * 24 * 3600</code>). <strong>Note:</strong> this alters the default behavior of these methods, because snapshot Images were previously persisted indefinitely. Pass an explicit <code>ttl=None</code> to retain the previous behavior.</li> <li><!> now also accepts a <code>timeout=</code> keyword argument (defaulting to <code>55</code> seconds), which brings it to parity with <!>. If the snapshot does not return before the deadline, a <!> will be raised. <strong>Note:</strong> this alters the default behavior, which previously waited indefinitely, but you can set an arbitrarily long timeout.</li></ul></li> <li>We’ve removed several deprecated static methods (<code>.delete()</code> and <code>.create_deployed()</code>) on Modal storage objects (<!>, etc.). Use the <!> and <!> methods instead.</li></ul> <!> <!> <ul><li>This release introduces a new <!> concept for Function inputs, which is now in Public Beta. By setting <code>routing_region="..."</code> in the <code>@app.function()</code> or <code>@app.cls()</code> decorators, you can configure the Function to route its inputs through servers in <code>us-west</code>, <code>eu-west</code>, or <code>ap-south</code> instead of <code>us-east</code>. This can reduce network latency and help you meet data residency obligations. While in Beta, this feature has a few constraints: <ul><li><code>routing_region=</code> can only be set during the initial deployment of a Function and cannot be changed in a subsequent redeployment.</li> <li>Functions using regional routing outside of <code>us-east</code> can only be invoked with the <code>.remote()</code> and <code>.map()</code> methods.</li></ul></li> <li>We’ve added a new <code>modal.Environment</code> object for programmatically managing Environments, and we’ve expanded the <code>modal environment</code> CLI to support <!> configuration.</li> <li>It is now possible to dynamically configure <code>modal.Function</code> behavior using <code>Function.with_options()</code>, <code>Function.with_concurrency()</code>, and <code>Function.with_batching()</code>.</li> <li>The new <code>modal.Volume.with_mount_options()</code> method allows you to configure a Volume mount as read-only (<code>read_only=True</code>) and / or limit the mount to a subdirectory of the Volume (<code>sub_path="/some/path"</code>).</li> <li>It’s now possible to pass a custom App name for ephemeral Apps using the <code>--name</code> option in <code>modal run</code>/<code>modal serve</code> or by setting <code>name=</code> in <code>App.run()</code>.</li> <li>We’ve added two new methods to the <code>modal.Sandbox</code> <!>: <ul><li><code>sandbox.filesystem.list_files(path)</code> lists entries (with metadata) in a given directory on the Sandbox filesystem. This replaces the alpha <code>modal.Sandbox.ls</code> method.</li> <li><code>sandbox.filesystem.stat(path)</code> returns metadata for a specific file/symlink/directory on the Sandbox’s filesystem.</li></ul></li> <li>It’s now possible to place restrictions on <em>inbound</em> Sandbox connections by setting <code>inbound_cidr_allowlist=[...]</code> in <code>modal.Sandbox.create()</code>. We are also adding a new <code>outbound_cidr_allowlist=[...]</code> parameter and deprecating the existing <code>cidr_allowlist=[...]</code> to avoid confusion.</li> <li>We’ve improved the reliability of the <code>modal.Sandbox.snapshot_filesystem()</code> operation, especially for large snapshots, and we now support setting a <code>timeout=</code> longer than 55s when necessary.</li> <li>Images returned by <code>modal.Sandbox.snapshot_directory()</code> can now be passed to <code>modal.Sandbox.create()</code> to use as a root filesystem for the new Sandbox.</li> <li>We’ve added a <code>modal.Image.pipe()</code> method to let you define reusable Image recipes that compose well with the fluent Image builder interface.</li> <li>It’s now possible to assign Sandbox tags at creation with <code>modal.Sandbox.create(..., tags=tags)</code>.</li> <li>It’s now possible to use <code>--chmod</code> and <code>--chown</code> flags on <code>COPY</code> commands within <code>modal.Image.from_dockerfile()</code>.</li> <li>We’ve improved the reliability and latency for <code>modal shell</code> and <code>modal container exec</code> CLI commands.</li></ul> <!> <ul><li>We’ve added a new <code>modal app rollover</code> CLI command for triggering a redeployment of an App without making any code or configuration changes. A rollover replaces existing containers with fresh ones. As with <code>modal deploy</code>, there are two strategies for switching between deployments: <ul><li><code>--strategy=rolling</code> (the default) will smoothly migrate traffic from old containers to new containers</li> <li><code>--strategy=recreate</code> will terminate all running containers so that any subsequent inputs will go to new containers</li></ul></li> <li>We’ve added a new <code>modal bootstrap</code> CLI command, which fetches deployable starter code for common AI applications (e.g., text generation, text-to-image, speech-to-text). This is an experiment: try it out and give us feedback!</li> <li>We’ve added two methods to the new Sandbox filesystem API: <ul><li><code>sandbox.filesystem.make_directory()</code> creates a new directory on the Sandbox filesystem</li> <li><code>sandbox.filesystem.remove()</code> deletes a file or directory from the Sandbox filesystem</li></ul></li> <li>The new Sandbox filesystem methods replace the <code>modal.Sandbox.mkdir</code> and <code>modal.Sandbox.rm</code> methods, which are now deprecated.</li> <li>Sandboxes also now support <code>sb.unmount_image(path)</code> to remove a previously mounted Image from a path and reveal the underlying Sandbox filesystem there again.</li> <li>The <code>modal app stop</code> and <code>modal container stop</code> CLI commands now prompt for confirmation (pass <code>--yes</code> to skip).</li> <li>Several other <code>modal app</code> CLI commands will now map a name-based argument to a recently-stopped App that used that name. This is useful for, e.g., fetching logs from an App after it has been stopped.</li> <li>We’ve added a <code>build_args</code> parameter to <code>modal.Image.dockerfile_commands()</code>.</li></ul> <!> <ul><li>We’re introducing a concept of “readiness probes” for <code>modal.Sandbox</code>. This feature lets you configure a readiness check on a TCP port (<code>modal.Probe.with_tcp()</code>) or by executing a process (<code>modal.Probe.with_exec()</code>). Calling <code>sb.wait_until_ready()</code> will block until the Probe succeeds: <!></li> <li>We fixed a longstanding bug that could cause WebSocket performance to degrade after handling hundreds of connections from the same container.</li> <li>We improved the performance of <code>modal container logs</code> when fetching logs for an old container.</li> <li>We fixed a bug introduced in 1.4.0 that made the <code>modal</code> CLI crash on <code>typer&lt;0.19.0</code>.</li></ul> <!> <p>We’ve made significant CLI enhancements so that Modal logs can be more accessible to coding agents:</p> <ul><li>The <code>modal app logs</code> and <code>modal container logs</code> commands now have the ability to fetch historical logs using counting (e.g. <code>--tail 1000</code>) or time-based (e.g., <code>--since 4h</code>, <code>--until 2026-03-15</code>, etc.) configuration. Note that historical log access is subject to plan-level retention limits.</li> <li>The <code>modal container logs</code> command also accepts <code>--all</code> to fetch the complete set of logs for that Container or Sandbox.</li> <li>Both CLI commands now accept a <code>--search</code> filter, and they can also filter by <code>--source</code> (<code>stdout</code>/<code>stderr</code>/<code>system</code>).</li> <li>The <code>modal app logs</code> command additionally accepts <code>--function</code>, <code>--function-call</code>, and <code>--container</code> filters.</li> <li>The <code>modal app logs</code> command can prefix each line with the ID of the Function, FunctionCall, or Container where it originated (e.g. <code>--show-function-id</code>).</li> <li>Note that the default behavior of these commands has changed. Previously, they would follow (i.e., stream) logs by default, but you now must pass <code>--follow</code> to get this behavior. The new default will always show the most recent 100 log entries.</li></ul> <p>We’re releasing a new <!> (currently in Beta) with significantly improved reliability and ergonomics:</p> <ul><li>Use <code>sb.filesystem.copy_from_local</code> / <code>sb.filesystem.copy_to_local</code> to transfer file contents between your local filesystem and the Sandbox filesystem.</li> <li>Use <code>sb.filesystem.write_text</code> / <code>sb.filesystem.read_text</code> or <code>sb.filesystem.write_bytes</code> / <code>sb.filesystem.read_bytes</code> to transfer file contents between local memory and the Sandbox filesystem.</li> <li>These new APIs replace the <code>modal.Sandbox.open</code> method and the <code>modal.file_io.FileIO</code> type that it returns; the old APIs are now deprecated.</li></ul> <p>We’re introducing the concept of “deployment strategies” to give you more flexibility over what happens when redeploying your App:</p> <ul><li>By passing <code>modal deploy --strategy recreate</code> (or <code>app.deploy(strategy="recreate")</code> in the SDK), you can immediately terminate any containers that are running when the deployment completes. This is most useful for development workflows, as it guarantees that any subsequent input will be handled by containers running the new version of the App. This trades off some downtime for certainty about when the new version will be in use.</li> <li>The disruptive “recreate” strategy is also useful when your App runs at its <code>max_containers</code> limit, as otherwise we are unable to bring up replacement capacity.</li> <li>The <code>modal serve</code> command now uses a “recreate” strategy during code updates.</li> <li>The default “rolling” strategy is unchanged. This strategy prioritizes uptime, but means that old containers may still continue handling inputs for some time.</li></ul> <p>We’ve also included a number of smaller new features and improvements:</p> <ul><li>Sandboxes now accept an <code>include_oidc_identity_token</code> parameter in <code>modal.Sandbox.create</code>. When set to <code>True</code>, a <code>MODAL_IDENTITY_TOKEN</code> environment variable will be injected into the Sandbox, enabling OIDC-based authentication (e.g., for AWS federation). See the <!> for more details.</li> <li>The new <code>modal.Image.from_scratch()</code> constructor creates an empty Image, equivalent to <code>FROM scratch</code> in Docker. This is primarily useful as a lightweight filesystem to mount into a Sandbox via <code>modal.Sandbox.mount_image</code>.</li> <li>The <code>modal container list</code> command now accepts an <code>--app-id</code> filter to return containers for a specific App.</li> <li>We’ve addressed an issue where <code>modal.Sandbox.exec</code> could hang if the Sandbox had terminated immediately after creation.</li> <li>An exception is now raised if the <em>same</em> Volume or CloudBucketMount is mounted at multiple paths in a container.</li> <li>The client will now error faster (≈60s) if it cannot establish an initial connection the Modal servers.</li></ul> <p>Finally, we are introducing a small number of breaking changes and enforcing some deprecations of pre-1.0 APIs:</p> <ul><li>Exceptions returned by <code>modal.Function.map()</code> are no longer wrapped in a <code>UserCodeException</code> type, and we’re deprecating the transitional <code>wrap_returned_exceptions=</code> parameter.</li> <li>The <code>modal.enable_output()</code> context manager no longer yields a value; this had briefly leaked an internal type.</li> <li>We’ve removed unused <code>namespace</code> parameters from a number of APIs.</li> <li>It’s now required to pass <code>-m</code> on the CLI when using a module path spelling of the Function reference (e.g. <code>modal deploy -m project.app</code>)</li> <li>We’ve removed backwards compatibility for the old autoscaler configuration (<code>keep_warm</code>, <code>concurrency_limit</code>, etc.).</li> <li>It’s no longer possible to look up a specific method on a Cls using <code>modal.Function.from_name</code>; use <code>modal.Cls.from_name</code> instead.</li></ul> <!> <!> <ul><li>We’ve added a <code>modal changelog</code> CLI for retrieving changelog entries with a flexible query interface (e.g. <code>modal changelog --since=1.2</code>, <code>modal changelog --since=2025-12-01</code>, <code>modal changelog --newer</code>). We expect that this will be a useful way to surface information about new features to coding agents.</li> <li>We’ve added a new <code>modal.Secret.update</code> method, which allows you to programmatically modify the environment variables within a Secret. This method has the semantics of Python’s <code>dict.update</code>: Secret contents can be overwritten or extended when using it. Note that Secret updates will take effect only for containers that start up after the modification.</li> <li>The dataclass returned by <code>modal.Function.get_current_stats()</code> now includes a <code>num_running_inputs</code> field that reports the number of inputs the Function is currently handling.</li></ul> <!> <ul><li><p>We’re introducing “Directory Snapshots”: a new beta feature for persisting specific directories past the lifetime of an individual Sandbox. Using the new methods <code>modal.Sandbox.snapshot_directory()</code> and <code>modal.Sandbox.mount_image()</code>, you can capture the state of a directory and then later include it in a different Sandbox:</p> <!> <p>This feature can be useful for separating the lifecycle of application code in the Sandbox’s main Image from project code that changes in each Sandbox session. Files in the mounted snapshot also benefit from several optimizations that allow them to be read faster. See the <!> for more information.</p></li> <li><p>We’ve added a new <code>modal.Sandbox.detach()</code> method that we recommend calling after you are done interacting with a Sandbox. This method disconnects your local client from the Sandbox and cleans up resources associated with the connection. After calling <code>detach</code>, operations on the Sandbox object may raise and are otherwise not guaranteed to work.</p></li> <li><p>The <code>modal.Sandbox.terminate()</code> method now accepts a <code>wait</code> parameter. With <code>wait=True</code>, <code>terminate</code> will block until the Sandbox is finished and return the exit code. The default <code>wait=False</code> maintains the previous behavior.</p></li> <li><p>Throughput for writing to the <code>stdin</code> of a <code>modal.Sandbox.exec</code> process has been increased by 8x.</p></li> <li><p>We’ve added a new <code>modal.Volume.from_id()</code> method for referencing a Volume by its object id.</p></li></ul> <!> <ul><li>We’ve added a new <code>modal billing report</code> CLI and promoted the <code>modal.billing.workspace_billing_report</code> API to General Availability for all Team and Enterprise plan workspaces.</li> <li>We’ve added <code>modal.Queue.from_id()</code> and <code>modal.Dict.from_id()</code> methods to support referencing a Queue or Dict by its object id.</li> <li>Modal’s async usage warnings are now enabled by default. These warnings will fire when using a <!> in an async context. We’ve aimed to provide detailed and actionable suggestions for how to modify the code, which makes the warnings verbose. While we recommend addressing any warnings that pop up, as they can point to significant performance issues or bugs, we also provide a configuration option to disable them (<code>MODAL_ASYNC_WARNINGS=0</code> or <code>async_warnings = false</code> in the <code>.modal.toml</code>). Please report any apparent false positives or incorrect suggested fixes.</li> <li>We’ve fixed a bug where the ASGI scope’s <code>state</code> contents could leak between requests when using <code>@modal.asgi_app</code>.</li></ul> <!> <ul><li>Modal objects now have a <code>.get_dashboard_url()</code> method. This method will return a URL for viewing that object on the Modal dashboard: <!></li> <li>There is also a new <code>modal dashboard</code> CLI and new <code>modal app dashboard</code> / <code>modal volume dashboard</code> CLI subcommands: <!></li> <li>You can now pass a Sandbox ID (<code>sb-xxxxx</code>) directly to the <code>modal container logs</code> CLI.</li> <li>The <code>modal token info</code> CLI will now include the token name, if provided at token creation.</li> <li>We’ve fixed an issue where <code>modal.Cls.with_options()</code> (or the <code>with_concurrency()</code> / <code>with_batching()</code> methods) could sometimes use stale argument values when called repeatedly.</li></ul> <!> <ul><li>We’ve improved our experimental support for Python 3.14t (free-threaded Python) inside Modal containers. <ul><li>The container environment will now use the Python implementation of the Protobuf runtime rather than the incompatible <code>upb</code> implementation.</li> <li>As 3.14t images are not being published to the official source for our prebuilt <code>modal.Image.debian_slim()</code> images, we recommend using <code>modal.Image.from_registry</code> to build a 3.14t Image: <!></li> <li>Note that 3.14t support is available only on the 2025.06 <!>.</li> <li>Support is still experimental, so please share any issues that you encounter running 3.14t in Modal containers.</li></ul></li> <li>It’s now possible to provide a <code>custom_domain</code> for a <code>modal.Sandbox</code>: <!> Note that Sandbox custom domains work differently from Function custom domains and must currently be set up manually by Modal; please get in touch if this feature interests you.</li> <li>We added a new <code>modal token info</code> CLI command to retrieve information about the credentials that are currently in use.</li> <li>We added a <code>--timestamps</code> flag to a number of CLI entrypoints (<code>modal run</code>, <code>modal serve</code>, <code>modal deploy</code>, and <code>modal container logs</code>) to show timestamps in the logging output.</li> <li>The automatic CLI creation for <code>modal run</code> entrypoints now supports <code>Literal</code> type annotations, provided that the literal type contains either all <code>str</code> or all <code>int</code> values.</li> <li>We’ve fixed a bug that could cause App builds to fail with an uninformative <code>CancelledError</code> when the App was misconfigured.</li> <li>We’ve improved client resource management when running <code>modal.Sandbox.exec</code>, which avoids a rare thread race condition.</li></ul> <!> <p>Modal now supports Python 3.14. Python 3.14t (the free-threading build) support is currently a work in progress, because we are waiting for dependencies to be updated with free-threaded support. Additionally, Modal no longer supports Python 3.9, which has reached <!>.</p> <p>We are adding experimental support for detecting cases where Modal’s blocking APIs are used in async contexts (which can be a source of bugs or performance issues). You can opt into runtime warnings by setting <code>MODAL_ASYNC_WARNINGS=1</code> as an environment variable or <code>async_warnings = true</code> as a config field. We will enable these warnings by default in the future; please report any apparent false positives or other issues while support is experimental.</p> <p>This release also includes a small number of deprecations and behavioral changes:</p> <ul><li>The Modal SDK will no longer propagate <code>grpclib.GRPCError</code> types out to the user; our own <code>modal.Error</code> subtypes will be used instead. To avoid disrupting user code that has relied on <code>GRPCError</code> exceptions for control flow, we are temporarily making some exception types inherit from <code>GRPCError</code> so that they will also be caught by <code>except grpclib.GRPCError</code> statements. Accessing the <code>.status</code> attribute of the exception will issue a deprecation warning, but warnings cannot be issued if the exception object is only caught and there is no other interaction with it. We advise proactively migrating any exception handling to use Modal types, as we will remove the dependency on <code>grpclib</code> types entirely in the future. See the <!> docs for the mapping from gRPC status codes to Modal exception types.</li> <li>The <code>max_inputs</code> parameter in the <code>@app.function()</code> and <code>@app.cls</code> decorators has been renamed to <code>single_use_containers</code> and now takes a boolean value rather than an integer. Note that only <code>max_inputs=1</code> has been supported, so this has no functional implications. This change is being made to reduce confusion with <code>@modal.concurrent(max_inputs=...)</code> and so that Modal’s autoscaler can provide better performance for Functions with single-use containers.</li> <li>The async (<code>.aio</code>) interface has been deprecated from <code>modal.FunctionCall.from_id</code>, <code>modal.Image.from_id</code>, and <code>modal.SandboxSnapshot.from_id</code>, because these methods do not perform I/O.</li> <li>The <code>replace_bytes</code> and <code>delete_bytes</code> methods have been removed from the <code>modal.file_io</code> filesystem interface.</li> <li>Images built with <code>modal.Image.micromamba()</code> using the 2023.12 <!> will now use a Python version that matches their local environment by default, rather than defaulting to Python 3.9.</li></ul> <!> <!> <ul><li>Fixed bug where iterating on a <code>modal.Sandbox.exec</code> output stream could raise unauthenticated errors.</li></ul> <!> <ul><li>It is now possible to set a custom <code>name=</code> for a Function without using <code>serialized=True</code>. This can be useful when decorating a function multiple times, e.g. applying multiple Modal configurations to the same implementation.</li> <li>It is now possible to start <code>modal shell</code> with a Modal Image ID (<code>modal shell im-abc123</code>). Additionally, <code>modal shell</code> will now warn if you pass invalid combinations of arguments (like <code>--cpu</code> together with the ID of an already running Sandbox, etc.).</li> <li>Fixed a bug in <code>modal shell</code> that caused e.g. <code>vi</code> to fail with unicode decode errors.</li> <li>Fixed a thread-safety issue in <code>modal.Sandbox</code> resource cleanup.</li> <li>Improved performance when adding large local directories to an Image.</li> <li>Improved async Sandbox performance by not blocking the event loop while reading from <code>stdout</code> or <code>stderr</code>.</li></ul> <!> <ul><li>Fixed a bug in <code>modal.Sandbox.exec</code> when using <code>stderr=StreamType.STDOUT</code> (introduced in v1.2.3).</li> <li>Added a new <code>h2_enabled</code> option in <code>modal.forward</code>, which enables HTTP/2 advertisement in TLS establishment.</li></ul> <!> <ul><li>CPU Functions can now be configured to run on non-preemptible capacity by setting <code>nonpreemptible=True</code> in the <code>@app.function()</code> or <code>@app.cls()</code> decorator. This feature is not currently available when requesting a GPU. Note that non-preemptibility incurs a 3x multiplier on CPU and memory pricing. See the <!> for more information on preemptions.</li> <li>The Modal client can now respond more gracefully to server throttling (e.g., rate limiting) by backing off and automatically retrying. This behavior can be controlled with a new <code>MODAL_MAX_THROTTLE_WAIT</code> config variable. Setting the config to <code>0</code> will preserve the previous behavior and treat rate limits as an exception; setting it to a nonzero number (the unit is seconds) will allow a limited duration of retries.</li> <li>The <code>modal.Sandbox.exec</code> implementation has been rewritten to be more reliable and efficient.</li> <li>Added a new <code>--add-local</code> flag to <code>modal shell</code>, allowing local files and directories to be included in the shell’s container.</li> <li>Fixed a bug introduced in v1.2.2 where some Modal objects (e.g., <code>modal.FunctionCall</code>) were not usable after being captured in a Memory Snapshot. The bug would result in a <code>has no loader function</code> error when the object was used.</li></ul> <!> <ul><li><p><code>modal.Image.run_commands</code> now supports <code>modal.Volume</code> mounts. This can be helpful for accelerating builds by keeping a package manager cache on the Volume:</p> <!></li> <li><p>All Modal objects now accept an optional <code>modal.Client</code> object in their constructor methods. Passing an explicit client can be helpful in cases where Modal credentials are retrieved from within the Python process that is making requests.</p></li> <li><p>The <code>name=</code> passed to <code>modal.Sandbox.create</code> and <code>modal.Sandbox.from_name</code> is now required to follow other Modal object naming rules (must contain only alphanumeric characters, dashes, periods, or underscores and cannot exceed 64 characters). Passing an invalid name will now error.</p></li> <li><p><code>modal.CloudBucketMount</code> now supports <code>force_path_style=True</code> to disable virtual-host-style addressing. See <!> for details.</p></li> <li><p>The output from <code>modal config show</code> is now valid JSON and can be parsed by CLI tools such as <code>jq</code>.</p></li> <li><p>Fixed a bug where App tags were not attached to Image builds that occur when first deploying the App.</p></li></ul> <!> <ul><li>It’s now possible to override the default <code>-dev</code> suffix applied to the autogenerated URLs for ephemeral Apps (i.e., when using <code>modal serve</code>) via a new <code>dev_suffix</code> field in the <code>.modal.toml</code> config file, or equivalently with the <code>MODAL_DEV_SUFFIX</code> environment variable. This can help avoid collisions when multiple users of a workspace are working on the same codebase simultaneously.</li> <li>Fixed a bug where reading long stdout/stderr from <code>modal.Sandbox.exec()</code> could break in <code>text=True</code> mode.</li> <li>Fixed a bug where the status code was not checked when downloading a file from a Volume.</li> <li><code>modal run --detach ...</code> will now exit more gracefully if you lose internet connection while your App is running.</li></ul> <!> <p>In this release, we’re introducing the concept of “App tags”, which are simple key-value metadata that can be included to provide additional organizational context. Tags can be defined as part of the <code>modal.App</code> constructor:</p> <!> <p>Tags can also be added to an active App via the new <code>modal.App.set_tags()</code> method, and current tags can be retrieved with the new <code>modal.App.get_tags()</code>.</p> <p>This release also introduces a new API for generating a tabular billing report: <code>modal.billing.workspace_billing_report()</code>. The billing API will report the cost incurred by each App, aggregated over time intervals (currently supporting a daily or hourly resolution). The report can optionally include App tags, allowing you to perform cost allocation using your own organizational schema.</p> <p>Note that the initial release of the billing API is a private beta. Please get in touch to discuss access.</p> <p>This release also includes some internal changes to Function input/output serialization. These changes will provide better support for calling into Modal Functions from our <code>modal-js</code> and <code>modal-go</code> SDKs. Versions 0.4 or later of <code>modal-js</code> and <code>modal-go</code> will only be able to invoke Functions in Apps deployed with version 1.2 or later of the Python SDK.</p> <p>Other new features and improvements:</p> <ul><li><p>The new <code>modal.Sandbox.create_connect_token()</code> method facilitates authentication for making HTTP / Websocket requests to a server running in a Sandbox:</p> <!> <p>See the <!> for more information.</p></li> <li><p>The new <code>modal.Image.build()</code> method allows you to eagerly trigger an Image build. This is particularly helpful when working with Sandboxes, as otherwise the Image build would happen lazily inside <code>modal.Sandbox.create()</code>:</p> <!></li> <li><p>We’ve added an <code>env</code> parameter to a number of methods that configure Function, Sandbox, or Image execution. This parameter accepts a dictionary and adds the contents as environment variables in the relevant Modal container. This allows for simpler inclusion of non-sensitive information compared to using a <code>modal.Secret</code>.</p></li> <li><p>It’s now possible to pass a <code>modal.CloudBucketMount</code> instance to the <code>volumes=</code> parameter of <code>modal.Cls.with_options</code> (previously, only dynamic addition of <code>modal.Volume</code> mounts was supported).</p></li> <li><p>The new <code>modal.Sandbox.get_tags()</code> method will fetch the tags currently in use by the Sandbox (i.e., after calling <code>modal.Sandbox.set_tags()</code>). Note that Sandbox tags are distinct from the new concept of App tags.</p></li> <li><p><code>modal.Dict.pop()</code> now accepts an optional <code>default</code> parameter, akin to Python’s <code>dict.pop()</code>.</p></li> <li><p>It’s now possible to <code>modal shell</code> into a running Sandbox by passing its Sandbox ID (<code>modal shell sb-123</code>).</p></li> <li><p>Sandboxes can now be configured to expose a PTY device via <code>Sandbox.create(..., pty=True)</code> and <code>Sandbox.exec(..., pty=True)</code>. This provides better support for Claude Code.</p></li> <li><p>The new <code>modal.experimental.image_delete()</code> function can be used to delete the final layer of an Image given its ID, which can be particularly useful for cleaning up Sandbox Filesystem Snapshots.</p></li> <li><p>Using <code>modal run --interactive</code> (or <code>-i</code>) will now suppress Modal’s status spinner to avoid interfering with breakpoints in local entrypoint functions. We’ve also improved support for printing large objects when attached to a debugger.</p></li> <li><p>We’ve improved support for Protobuf 5+ when using the Python implementation of the Protobuf runtime.</p></li></ul> <p>This release also introduces a small number of new deprecations:</p> <ul><li>We deprecated the <code>client</code> parameter from <code>Sandbox.set_tags()</code>. To use an explicit Client when interacting with the Sandbox, pass it into <code>modal.Sandbox.create()</code> instead.</li> <li>We deprecated the <code>pty_info</code> parameter from <code>Sandbox.create()</code> and <code>Sandbox.exec()</code>. This was a private parameter accepting an internal Protobuf type. See the new boolean <code>pty</code> parameter instead.</li> <li>We replaced the <code>--no-confirm</code> option with <code>--yes</code> in the <code>modal environment delete</code> CLI to align with other CLI commands that normally require confirmation.</li></ul> <p>Finally, some functionality that began issuing deprecation warnings prior to v0.73 has now been completely removed:</p> <ul><li>It is now required to “instantiate” a <code>modal.Cls</code> before invoking one of its methods.</li> <li>The eager <code>.lookup()</code> method has been removed from most Modal object classes (but not from <code>modal.App.lookup</code>, which remains supported). The lazy <code>.from_name()</code> method is recommended for accessing deployed objects going forward.</li> <li>The public constructors on the <code>modal.mount.Mount</code> object have been removed; this is now an entirely internal class.</li> <li>The <code>context_mount=</code> parameter has accordingly been removed from Docker-oriented <code>modal.Image</code> methods.</li> <li>The unused <code>allow_cross_region_volumes</code> parameter has been removed from the function decorators.</li> <li>The <code>modal.experimental.update_autoscaler()</code> function has been removed; this functionality now has a stable API as <code>modal.Function.update_autoscaler()</code>.</li></ul> <!> <!> <ul><li>Added a <code>startup_timeout</code> parameter to the <code>@app.function()</code> and <code>@app.cls()</code> decorators. When used, this configures the timeout applied to each container’s startup period separately from the input <code>timeout</code>. For backwards compatibility, <code>timeout</code> still applies to the startup phase when <code>startup_timeout</code> is unset.</li> <li>Added an optional <code>idle_timeout</code> parameter to <code>modal.Sandbox.create()</code>. When provided, Sandboxes will terminate after <code>idle_timeout</code> seconds of idleness.</li> <li>The dataclass returned by <code>modal.experimental.get_cluster_info()</code> now includes a <code>cluster_id</code> field to identify the clustered set of containers.</li> <li>When <code>block_network=True</code> is set in <code>modal.Sandbox.create()</code>, we now raise an error if any of <code>encrypted_ports</code>, <code>h2_ports</code>, or <code>unencrypted_ports</code> are also set.</li> <li>Functions decorated with <code>@modal.asgi_app()</code> now return an HTTP 408 (request timeout) error code instead of a 502 (gateway timeout) in rare cases when an input fails to arrive at the container, e.g. due to cancellation.</li> <li><code>modal.Sandbox.create()</code> now warns when an invalid <code>name=</code> is passed, applying the same rules as other Modal object names: names must be alphanumeric and not longer than 64 characters. This will become an error in the future.</li></ul> <!> <ul><li>Fixed a bug introduced in <code>v1.1.2</code> that causes invocation of <code>modal.FunctionCall.get</code>, <code>modal.FunctionCall.get_call_graph</code>, <code>modal.FunctionCall.cancel</code>, and <code>modal.FunctionCall.gather</code> to fail when the <code>FunctionCall</code> object is retrieved via <code>modal.FunctionCall.from_id</code>.</li> <li>Added retries to improve the robustness of <code>modal volume get</code></li></ul> <!> <p>We’re introducing a new API pattern for imperative management of Modal resource types (<code>modal.Volume</code>, <code>modal.Secret</code>, <code>modal.Dict</code>, and <code>modal.Queue</code>). The API is accessible through the <code>.objects</code> namespace on each class. The object management namespace has methods for the following operations:</p> <ul><li><code>.objects.create(name)</code> creates an object on our backend. E.g., with <!>: <!></li> <li><code>.objects.delete(name)</code> deletes the object with that name. E.g., with <!>: <!></li> <li><code>.objects.list()</code> returns a list of object instances. E.g., with <!>: <!></li></ul> <p>With the introduction of these APIs, we’re replacing a few older methods with similar functionality:</p> <ul><li>Static <code>.delete()</code> methods on the resource types themselves are being deprecated, because they are too easily confused with operations on the <em>contents</em> of a resource (i.e., calling <code>modal.Dict.delete(key_name)</code> is an easy mistake that can have significant adverse consequences).</li> <li>The undocumented <code>.create_deployed()</code> methods of <code>modal.Volume</code> and <code>modal.Secret</code> are being deprecated in favor of this consistent API for imperative management.</li></ul> <p>Other changes:</p> <ul><li><code>modal.Cls.with_options</code> now supports <code>region</code> and <code>cloud</code> keyword arguments to support runtime constraints on scheduling.</li> <li>Fixed a bug that could cause Image builds to fail with <code>'FilePatternMatcher' object has no attribute 'patterns'</code> when using a <code>modal.FilePatternMatcher.from_file</code> ignore pattern.</li> <li>Fixed a bug where <code>rdma=True</code> was ignored when using <code>@modal.experimental.clustered()</code> with a <code>modal.Cls</code>.</li></ul> <!> <p>We’re introducing the concept of “named Sandboxes” for usecases where Sandboxes need to have unique ownership over a resource. A named Sandbox can be created by passing <code>name=</code> to <code>modal.Sandbox.create()</code>, and it can be retrieved with the new <code>modal.Sandbox.from_name()</code> constructor. Only one running Sandbox can use a given name (scoped within the App that is managing the Sandbox) at any time, so trying to create a Sandbox with a name that is already taken will fail. Sandboxes release their name when they terminate. See the <!> for more information about using this new feature.</p> <p>Other changes:</p> <ul><li>We’ve made an internal change to the <code>modal.Image.uv_pip_install</code> method to make it more portable across different base Images. As a consequence, Images built with this method on 1.1.0 will need to rebuild the next time they are used.</li> <li>We’ve added a <code>.name</code> property and <code>.info()</code> method to <code>modal.Dict</code>, <code>modal.Queue</code>, <code>modal.Volume</code>, and <code>modal.Secret</code> objects.</li> <li>Sandboxes now support <code>experimental_options</code> configuration for enabling preview functionality.</li> <li>We’ve Improved Modal’s rich output when used in a Jupyter notebook.</li></ul> <!> <p>This release introduces support for the <code>2025.06</code> <!>, which is in a “preview” state. The new image builder includes several major changes to how the Modal client dependencies are included in Modal Images. These improvements should greatly reduce the risk of conflicts with user code dependencies. They also allow Modal Sandboxes to easily be used with existing Images or Dockerfiles that are not themselves compatible with the Modal client library. You can see more details and update your Workspace on its <!> page. Please share any issues that you encounter as we work to make the version stable.</p> <p>We’re also introducing first-class support for building Modal Images with the <!> through the new <!> and <!> methods:</p> <!> <p>Please note that, as these methods are new, there is some chance that future releases will need to fix bugs or address edge cases in ways that break the cache for existing Images. When using <code>modal.Image.uv_pip_install</code>, we recommend pinning dependency versions so that any necessary rebuilds produce a consistent environment.</p> <p>This release also includes a number of other new features and bug fixes:</p> <ul><li>Optimized handling of the <code>ignore</code> parameter in <code>Image.add_local_dir</code> and similar methods for cases where entire directories are ignored.</li> <li>Added a <code>poetry_version</code> parameter to <code>modal.Image.poetry_install_from_file</code>, which supports installing a specific version of <code>poetry</code>. It’s also possible to set <code>poetry_version=None</code> to skip the install step, i.e. when poetry is already available in the Image.</li> <li>Added a <!> method, which triggers a reload of all Volumes currently mounted inside a running Sandbox.</li> <li>Added a <code>build_args</code> parameter to <code>modal.Image.from_dockerfile</code> for passing arguments through to <code>ARG</code> instructions in the Dockerfile.</li> <li>It’s now possible to use <code>@modal.experimental.clustered</code> and <code>i6pn</code> networking with <code>modal.Cls</code>.</li> <li>Fixed a bug where <code>Cls.with_options</code> would fail when provided with a <code>modal.Secret</code> object that was already hydrated.</li> <li>Fixed a bug where the timeout specified in <code>modal.Sandbox.exec()</code> was not respected by <code>ContainerProcess.wait()</code> or <code>ContainerProcess.poll()</code>.</li> <li>Fixed retry handling when using <code>modal run --detach</code> directly against a remote Function.</li></ul> <p>Finally, this release introduces a small number of deprecations and potentially-breaking changes:</p> <ul><li>We now raise <code>modal.exception.NotFoundError</code> in all cases where Modal object lookups fail; previously some methods could leak an internal <code>GRPCError</code> with a <code>NOT_FOUND</code> status.</li> <li>We’re enforcing pre-1.0 deprecations on <code>modal.build</code>, <code>modal.Image.copy_local_file</code>, and <code>modal.Image.copy_local_dir</code>.</li> <li>We’re deprecating the <code>environment_name</code> parameter in <code>modal.Sandbox.create()</code>. A Sandbox’s environment association will now be determined by its parent App. This should have no user-facing effects.</li> <li>We’ve deprecated the <code>namespace</code> parameter in the <code>.from_name</code> methods of <code>Function</code>, <code>Cls</code>, <code>Dict</code>, <code>Queue</code>, <code>Volume</code>, <code>NetworkFileSystem</code>, and <code>Secret</code>, along with <code>modal.runner.deploy_app</code>. These object types do not have a concept of distinct namespaces.</li></ul> <!> <!> <ul><li><p>Added a <!> method, which will configure a Volume instance to disallow writes:</p> <!></li> <li><p>Introduced a gradual fix for a bug where <code>Function.map</code> and <code>Function.starmap</code> leak an internal exception wrapping type (<code>modal.exceptions.UserCodeException</code>) when <code>return_exceptions=True</code> is set. To avoid breaking any user code that depends on the specific types in the return list, these functions will continue returning the wrapper type by default, but they now issue a deprecation warning. To opt into the future behavior and silence the warning, you can set <code>wrap_returned_exceptions=False</code> in the call to <code>.map</code> or <code>.starmap</code>.</p></li> <li><p>When an <code>@app.cls()</code>-decorated class inherits from a class (or classes) with <code>modal.parameter()</code> annotations, the parent parameters will now be inherited and included in the parameter set for the modal Cls.</p></li> <li><p>Redeployments that migrate parameterized functions from an explicit constructor to <code>modal.parameter()</code> annotations will now handle requests from outdated clients more gracefully, avoiding a problem where new containers would crashloop on a deserialization error.</p></li> <li><p>The Modal client will now retry its initial connection to the Modal server, improving stability on flaky networks.</p></li></ul> <!> <ul><li>When <code>modal.Cls.with_options</code> is called multiple times on the same instance, the overrides will now be merged. For example, the following configuration will use an H100 GPU and request 16 CPU cores: <!></li> <li>Added a <code>--secret</code> option to <code>modal shell</code> for including environment variables defined by named Secret(s) in the shell session: <!></li> <li>Added a <code>verbose: bool</code> option to <code>modal.Sandbox.create()</code>. When this is set to <code>True</code>, execs and file system operations will appear in the Sandbox logs.</li> <li>Updated <code>modal.Sandbox.watch()</code> so that exceptions are now raised in (and can be caught by) the calling task.</li></ul> <!> <ul><li><p>Added support for specifying a timezone on <code>Cron</code> schedules, which allows you to run a Function at a specific local time regardless of daylight savings:</p> <!></li> <li><p>Added an <code>h2_ports</code> parameter to <code>Sandbox.create</code>, which exposes encrypted ports using HTTP/2. The following example will create an H2 port on 5002 and a port using HTTPS over HTTP/1.1 on 5003:</p> <!></li> <li><p>Added <code>--from-dotenv</code> and <code>--from-json</code> options to <code>modal secret create</code>, which will read from local files to populate Secret contents.</p></li> <li><p><code>Sandbox.terminate</code> no longer waits for container shutdown to complete before returning. It still ensures that a terminated container will shutdown imminently. To restore the previous behavior (i.e., to wait until the Sandbox is actually terminated), call <code>sb.wait(raise_on_termination=False)</code> after calling <code>sb.terminate()</code>.</p></li> <li><p>Improved performance and stability for <code>modal volume get</code>.</p></li> <li><p>Fixed a rare race condition that could sometimes make <code>Function.map</code> and similar calls deadlock.</p></li> <li><p>Fixed an issue where <code>Function.map</code> and similar methods would stall for 55 seconds when passed an empty iterator as input instead of completing immediately.</p></li> <li><p>We now raise an error during App setup when using interactive mode without the <code>modal.enable_output</code> context manager. Previously, this would run the App but raise when <code>modal.interact()</code> was called.</p></li></ul> <!> <ul><li>Fixed an incompatibility with breaking changes in <code>aiohttp</code> v3.12.0, which caused issues with Volume and large input uploads. The issues typically manifest as <code>Local data and remote data checksum mismatch</code> or <code>'_io.BufferedReader' object has no attribute 'getbuffer'</code> errors.</li></ul> <!> <ul><li>Added a <code>--timestamps</code> flag to <code>modal app logs</code> that prepends a timestamp to each log line.</li> <li>Fixed a bug where objects returned by <code>Sandbox.list</code> had <code>returncode == 0</code> for <em>running</em> Sandboxes. Now the return code for running Sandboxes will be <code>None</code>.</li> <li>Fixed a bug affecting systems where the <code>sys.platform.node</code> name includes unicode characters.</li></ul> <!> <p>With this release, we’re beginning to enforce the deprecations discussed in the <!>. Going forward, we’ll include breaking changes for outstanding deprecations in <code>1.Y.0</code> releases, so we recommend pinning Modal on a minor version (<code>modal~=1.0.0</code>) if you have not addressed the existing warnings. While we’ll continue to make improvements to the Modal API, new deprecations will be introduced at a substantially reduced rate, and support windows for older client versions will lengthen.</p> <p>⚠️ In this release, we’ve made some breaking changes to Modal’s “automounting” behavior.️ If you’ve not already adapted your source code in response to warnings about automounting, Apps built with 1.0+ will have different files included and may not run as expected:</p> <ul><li>Previously, Modal containers would automatically include the source for local Python packages that were imported by your Modal App. Going forward, it will be necessary to explicitly include such packages in the Image (i.e., with <code>modal.Image.add_local_python_source</code>).</li> <li>Support for the <code>automount</code> configuration (<code>MODAL_AUTOMOUNT</code>) has been removed; this environment variable will no longer have any effect.</li> <li>Modal will continue to automatically include the Python module or package where the Function is defined. If the Function is defined within a package, the entire directory tree containing the package will be mounted. This limited automounting can also be disabled in cases where your Image definition already includes the package defining the Function: set <code>include_source=False</code> in the <code>modal.App</code> constructor or <code>@app.function</code> decorator.</li></ul> <p>Additionally, we have enforced a number of previously-introduced deprecations:</p> <ul><li>Removed <code>modal.Mount</code> as a public object, along with various <code>mount=</code> parameters where Mounts could be passed into the Modal API. Usage can be replaced with <code>modal.Image</code> methods, e.g.: <!></li> <li>Removed the <code>show_progress</code> parameter from <code>modal.App.run</code>. This parameter has been replaced by the <code>modal.enable_output</code> context manager: <!></li> <li>Passing flagged options to the <code>Image.pip_install</code> package list will now raise an error. Use the <code>extra_options</code> parameter to specify options that aren’t exposed through the <code>Image.pip_install</code> signature: <!></li> <li>Removed backwards compatibility for using <code>label=</code> or <code>tag=</code> keywords in object lookup methods. We standardized these methods to use <code>name=</code> as the parameter name, but we recommend using positional arguments: <!></li> <li>It’s no longer possible to invoke a generator Function with <code>Function.spawn</code>; previously this warned, now it raises an <code>InvalidError</code>. Additionally, the <code>FunctionCall.get_gen</code> method has been removed, and it’s no longer possible to set <code>is_generator</code> when using <code>FunctionCall.from_id</code>.</li> <li>Removed the <code>.resolve()</code> method on Modal objects. This method had not been publicly documented, but where used it can be replaced straightforwardly with <code>.hydrate()</code>. Note that explicit hydration should rarely be necessary: in most cases you can rely on lazy hydration semantics (i.e., objects will be hydrated when the first method that requires server metadata is called).</li> <li>Functions decorated with <code>@modal.asgi_app</code> or <code>@modal.wsgi_app</code> are now required to be nullary. Previously, we warned in the case where a function was defined with parameters that all had default arguments.</li> <li>Referencing the deprecated <code>modal.Stub</code> object will now raise an <code>AttributeError</code>, whereas previously it was an alias for <code>modal.App</code>. This is a simple name change.</li></ul>`,1);function p(t,ae){let f=ee(ae,[`children`,`$$slots`,`$$events`,`$$legacy`]);ie(t,te(()=>f,()=>d,{children:(t,ee)=>{var te=Ut(),ie=ne(te);re(ie,{id:`python-sdk-release-notes`,children:(e,t)=>{o(),i(e,r(`Python SDK Release Notes`))},$$slots:{default:!0}});var d=a(ie,4);s(d,{id:`latest`,children:(e,t)=>{o(),i(e,r(`Latest`))},$$slots:{default:!0}});var ae=a(d,2);c(ae,{id:`154-2026-08-12`,children:(e,t)=>{o(),i(e,r(`1.5.4 (2026-08-12)`))},$$slots:{default:!0}});var f=a(ae,2),oe=a(e(f));u(oe,{href:`/blog/scaling-to-1-million-concurrent-sandboxes-in-seconds`,children:(e,t)=>{o(),i(e,r(`more performant Sandbox backend`))},$$slots:{default:!0}}),u(a(oe,4),{href:`/docs/guide/migrate-sandbox-filesystem`,children:(e,t)=>{o(),i(e,r(`migrate`))},$$slots:{default:!0}}),o(),n(f);var p=a(f,2),m=e(p),Wt=a(e(m),5),h=e(Wt);u(a(e(h)),{href:`/docs/sdk/py/latest/App#logs`,children:(e,t)=>{i(e,se())},$$slots:{default:!0}}),o(7),n(h);var Gt=a(h,2);u(a(e(Gt)),{href:`/docs/sdk/py/latest/Image#logs`,children:(e,t)=>{i(e,ce())},$$slots:{default:!0}}),o(5),n(Gt),n(Wt),n(m);var g=a(m,2);u(a(e(g)),{href:`/docs/cli/latest/image#modal-image-logs`,children:(e,t)=>{i(e,le())},$$slots:{default:!0}}),o(),n(g);var _=a(g,2),Kt=a(e(_));u(Kt,{href:`/docs/sdk/py/latest/Workspace#billingrates`,children:(e,t)=>{i(e,ue())},$$slots:{default:!0}}),u(a(Kt,2),{href:`/docs/cli/latest/billing#modal-billing-rates`,children:(e,t)=>{i(e,de())},$$slots:{default:!0}}),o(),n(_);var v=a(_,2),qt=a(e(v));u(qt,{href:`/docs/sdk/py/latest/Function#update_autoscaler`,children:(e,t)=>{i(e,fe())},$$slots:{default:!0}}),u(a(qt,2),{href:`/docs/sdk/py/latest/Server#update_autoscaler`,children:(e,t)=>{i(e,pe())},$$slots:{default:!0}}),o(),n(v);var Jt=a(v,2),Yt=a(e(Jt),3);u(Yt,{href:`/docs/sdk/py/latest/App#server`,children:(e,t)=>{i(e,me())},$$slots:{default:!0}}),u(a(Yt,2),{href:`/docs/sdk/py/latest/Server#update_autoscaler`,children:(e,t)=>{i(e,he())},$$slots:{default:!0}}),o(),n(Jt),n(p);var Xt=a(p,2);c(Xt,{id:`153-2026-07-23`,children:(e,t)=>{o(),i(e,r(`1.5.3 (2026-07-23)`))},$$slots:{default:!0}});var y=a(Xt,2),b=e(y),Zt=a(e(b));u(Zt,{href:`/docs/sdk/py/latest/Function#logs`,children:(e,t)=>{i(e,ge())},$$slots:{default:!0}});var Qt=a(Zt,2);u(Qt,{href:`/docs/sdk/py/latest/Server#logs`,children:(e,t)=>{i(e,_e())},$$slots:{default:!0}}),u(a(Qt,2),{href:`/docs/sdk/py/latest/FunctionCall#logs`,children:(e,t)=>{i(e,ve())},$$slots:{default:!0}}),o(7),n(b);var x=a(b,2),$t=a(e(x)),S=e($t),en=a(e(S));u(en,{href:`/docs/sdk/py/latest/Workspace#billingsummary`,children:(e,t)=>{i(e,ye())},$$slots:{default:!0}}),u(a(en,2),{href:`/docs/cli/latest/billing#modal-billing-summary`,children:(e,t)=>{i(e,be())},$$slots:{default:!0}}),o(),n(S);var tn=a(S,2),nn=a(e(tn));u(nn,{href:`/docs/sdk/py/latest/Environment#billingsummary`,children:(e,t)=>{i(e,xe())},$$slots:{default:!0}}),u(a(nn,2),{href:`/docs/cli/latest/environment#modal-environment-billing-summary`,children:(e,t)=>{i(e,Se())},$$slots:{default:!0}}),o(),n(tn),n($t),n(x);var C=a(x,2),rn=a(e(C));u(rn,{href:`/docs/guide/rbac`,children:(e,t)=>{o(),i(e,r(`RBAC`))},$$slots:{default:!0}});var an=a(rn,2),on=a(e(an),2),sn=a(e(on));u(sn,{href:`/docs/sdk/py/latest/Environment#roles`,children:(e,t)=>{i(e,Ce())},$$slots:{default:!0}}),u(a(sn,2),{href:`/docs/cli/latest/environment#modal-environment-roles`,children:(e,t)=>{i(e,we())},$$slots:{default:!0}}),o(5),n(on),n(an),n(C);var w=a(C,2);u(a(e(w),3),{href:`/docs/cli/latest/endpoint#modal-endpoint-create`,children:(e,t)=>{i(e,Te())},$$slots:{default:!0}}),o(),n(w);var cn=a(w,2),ln=a(e(cn));u(ln,{href:`/docs/sdk/py/latest/Sandbox#filesystemcopy_from_local`,children:(e,t)=>{i(e,Ee())},$$slots:{default:!0}});var un=a(ln,2);u(un,{href:`/docs/sdk/py/latest/Sandbox#filesystemwrite_bytes`,children:(e,t)=>{i(e,De())},$$slots:{default:!0}}),u(a(un,2),{href:`/docs/sdk/py/latest/Sandbox#filesystemwrite_text`,children:(e,t)=>{i(e,Oe())},$$slots:{default:!0}}),o(),n(cn),n(y);var dn=a(y,2);c(dn,{id:`152-2026-07-10`,children:(e,t)=>{o(),i(e,r(`1.5.2 (2026-07-10)`))},$$slots:{default:!0}});var T=a(dn,2),E=e(T),fn=a(e(E)),D=e(fn),pn=a(e(D));u(pn,{href:`/docs/sdk/py/latest/Workspace#settingslist`,children:(e,t)=>{i(e,ke())},$$slots:{default:!0}}),u(a(pn,2),{href:`/docs/cli/latest/workspace#modal-workspace-settings-list`,children:(e,t)=>{i(e,Ae())},$$slots:{default:!0}}),o(),n(D);var mn=a(D,2),hn=a(e(mn));u(hn,{href:`/docs/sdk/py/latest/Workspace#settingsset`,children:(e,t)=>{i(e,je())},$$slots:{default:!0}}),u(a(hn,2),{href:`/docs/cli/latest/workspace#modal-workspace-settings-set`,children:(e,t)=>{i(e,Me())},$$slots:{default:!0}}),o(),n(mn),n(fn),n(E);var O=a(E,2);u(a(e(O)),{href:`/docs/sdk/py/latest/types`,children:(e,t)=>{i(e,Ne())},$$slots:{default:!0}}),o(),n(O);var k=a(O,2),gn=a(e(k));u(gn,{href:`/docs/sdk/py/latest/Function#with_options`,children:(e,t)=>{i(e,Pe())},$$slots:{default:!0}}),u(a(gn,4),{href:`/docs/guide/region-selection#regional-routing`,children:(e,t)=>{o(),i(e,r(`regional routing`))},$$slots:{default:!0}}),o(),n(k);var A=a(k,2);u(a(e(A)),{href:`/docs/cli/latest/container#modal-container-stop`,children:(e,t)=>{i(e,Fe())},$$slots:{default:!0}}),o(3),n(A);var j=a(A,2);u(a(e(j)),{href:`/docs/cli/latest/container#modal-container-logs`,children:(e,t)=>{i(e,Ie())},$$slots:{default:!0}}),o(),n(j);var M=a(j,2);u(a(e(M)),{href:`/docs/sdk/py/latest/Workspace#memberslist`,children:(e,t)=>{i(e,Le())},$$slots:{default:!0}}),o(5),n(M);var _n=a(M,2),vn=a(e(_n));u(vn,{href:`/docs/sdk/py/latest/Sandbox#reload_volumes`,children:(e,t)=>{i(e,Re())},$$slots:{default:!0}}),u(a(vn,6),{href:`/docs/sdk/py/latest/exception#timeouterror`,children:(e,t)=>{i(e,ze())},$$slots:{default:!0}}),o(),n(_n),n(T);var yn=a(T,2);c(yn,{id:`151-2026-06-23`,children:(e,t)=>{o(),i(e,r(`1.5.1 (2026-06-23)`))},$$slots:{default:!0}});var N=a(yn,2),bn=a(e(N));u(bn,{href:`/docs/sdk/py/latest/App#server`,children:(e,t)=>{i(e,Be())},$$slots:{default:!0}}),u(a(bn,2),{href:`/docs/cli/latest/endpoint`,children:(e,t)=>{i(e,Ve())},$$slots:{default:!0}}),o(),n(N);var P=a(N,2),F=e(P),xn=a(e(F));u(xn,{href:`/docs/sdk/py/latest/App#server`,children:(e,t)=>{i(e,He())},$$slots:{default:!0}});var Sn=a(xn,2);u(Sn,{href:`/docs/sdk/py/latest/Server`,children:(e,t)=>{i(e,Ue())},$$slots:{default:!0}}),u(a(Sn,2),{href:`/docs/guide/servers`,children:(e,t)=>{o(),i(e,r(`guide`))},$$slots:{default:!0}}),o(),n(F);var I=a(F,2),Cn=a(e(I));u(Cn,{href:`/docs/cli/latest/endpoint`,children:(e,t)=>{i(e,We())},$$slots:{default:!0}}),u(a(Cn,2),{href:`/docs/guide/endpoints`,children:(e,t)=>{o(),i(e,r(`Endpoints`))},$$slots:{default:!0}}),o(),n(I);var L=a(I,2),wn=a(e(L)),R=e(wn),Tn=a(e(R));u(Tn,{href:`/docs/sdk/py/latest/Workspace`,children:(e,t)=>{i(e,Ge())},$$slots:{default:!0}});var En=a(Tn,2);u(En,{href:`/docs/sdk/py/latest/Environment`,children:(e,t)=>{i(e,Ke())},$$slots:{default:!0}});var Dn=a(En,2);u(Dn,{href:`/docs/sdk/py/latest/Workspace#billingreport`,children:(e,t)=>{i(e,qe())},$$slots:{default:!0}});var On=a(Dn,2);u(On,{href:`/docs/sdk/py/latest/Environment#billingreport`,children:(e,t)=>{i(e,Je())},$$slots:{default:!0}});var kn=a(On,2);u(kn,{href:`/docs/cli/latest/environment#modal-environment-billing`,children:(e,t)=>{i(e,Ye())},$$slots:{default:!0}}),u(a(kn,2),{href:`/docs/sdk/py/latest/billing#workspace_billing_report`,children:(e,t)=>{i(e,Xe())},$$slots:{default:!0}}),o(),n(R);var z=a(R,2),An=a(e(z));u(An,{href:`/docs/guide/webhook-proxy-auth`,children:(e,t)=>{o(),i(e,r(`proxy tokens`))},$$slots:{default:!0}});var jn=a(An,2);u(jn,{href:`/docs/sdk/py/latest/Workspace`,children:(e,t)=>{i(e,Ze())},$$slots:{default:!0}});var Mn=a(jn,2);u(Mn,{href:`/docs/sdk/py/latest/Workspace#proxy_tokenscreate`,children:(e,t)=>{i(e,Qe())},$$slots:{default:!0}});var Nn=a(Mn,2);u(Nn,{href:`/docs/sdk/py/latest/Workspace#proxy_tokenslist`,children:(e,t)=>{i(e,$e())},$$slots:{default:!0}}),u(a(Nn,2),{href:`/docs/cli/latest/workspace#modal-workspace-proxy-tokens`,children:(e,t)=>{i(e,et())},$$slots:{default:!0}}),o(),n(z);var Pn=a(z,2);u(a(e(Pn)),{href:`/docs/cli/latest/workspace#modal-workspace-members`,children:(e,t)=>{i(e,tt())},$$slots:{default:!0}}),o(),n(Pn),n(wn),n(L);var B=a(L,2);u(a(e(B)),{href:`/docs/cli/latest/curl`,children:(e,t)=>{i(e,nt())},$$slots:{default:!0}}),o(),n(B);var V=a(B,2),Fn=a(e(V));u(Fn,{href:`/docs/cli/latest/app#modal-app-rollback`,children:(e,t)=>{i(e,rt())},$$slots:{default:!0}});var In=a(Fn,8);u(In,{href:`/docs/cli/latest/deploy`,children:(e,t)=>{i(e,it())},$$slots:{default:!0}}),u(a(In,2),{href:`/docs/cli/latest/app#modal-app-rollover`,children:(e,t)=>{i(e,at())},$$slots:{default:!0}}),o(),n(V);var H=a(V,2);u(a(e(H)),{href:`/docs/sdk/py/latest/Sandbox#create_connect_token`,children:(e,t)=>{i(e,ot())},$$slots:{default:!0}}),o(),n(H);var Ln=a(H,2),Rn=a(e(Ln));u(Rn,{href:`/docs/sdk/py/latest/Image#publish`,children:(e,t)=>{i(e,st())},$$slots:{default:!0}});var zn=a(Rn,2);u(zn,{href:`/docs/sdk/py/latest/Image#from_id`,children:(e,t)=>{i(e,ct())},$$slots:{default:!0}}),u(a(zn,2),{href:`/docs/sdk/py/latest/Image#build`,children:(e,t)=>{i(e,lt())},$$slots:{default:!0}}),o(),n(Ln),o(2),n(P);var Bn=a(P,2);c(Bn,{id:`150-2026-06-09`,children:(e,t)=>{o(),i(e,r(`1.5.0 (2026-06-09)`))},$$slots:{default:!0}});var U=a(Bn,2),Vn=a(e(U));u(Vn,{href:`/docs/guide/named-images`,children:(e,t)=>{o(),i(e,r(`named Images`))},$$slots:{default:!0}});var Hn=a(Vn,2);u(Hn,{href:`/docs/guide/sandbox-networking`,children:(e,t)=>{o(),i(e,r(`Sandbox domain allowlists`))},$$slots:{default:!0}}),u(a(Hn,2),{href:`/docs/cli/latest/skills`,children:(e,t)=>{i(e,ut())},$$slots:{default:!0}}),o(),n(U);var W=a(U,2),G=e(W),Un=a(e(G)),K=e(Un);u(a(e(K)),{href:`/docs/sdk/py/latest/Image#publish`,children:(e,t)=>{i(e,dt())},$$slots:{default:!0}}),o(3),n(K);var q=a(K,2),Wn=a(e(q));u(Wn,{href:`/docs/sdk/py/latest/Image#from_name`,children:(e,t)=>{i(e,ft())},$$slots:{default:!0}});var Gn=a(Wn,2);u(Gn,{href:`/docs/sdk/py/latest/Image#from_name`,children:(e,t)=>{i(e,pt())},$$slots:{default:!0}}),u(a(Gn,2),{href:`/docs/sdk/py/latest/exception#notfounderror`,children:(e,t)=>{i(e,mt())},$$slots:{default:!0}}),o(),n(q);var Kn=a(q,2);u(a(e(Kn)),{href:`/docs/cli/latest/image#modal-image-names`,children:(e,t)=>{i(e,ht())},$$slots:{default:!0}}),o(),n(Kn),n(Un),n(G);var J=a(G,2),qn=a(e(J),3);u(qn,{href:`/docs/sdk/py/latest/Function#from_name`,children:(e,t)=>{i(e,gt())},$$slots:{default:!0}}),u(a(qn,2),{href:`/docs/sdk/py/latest/Cls#from_name`,children:(e,t)=>{i(e,_t())},$$slots:{default:!0}}),o(),n(J);var Y=a(J,2);u(a(e(Y),5),{href:`/docs/sdk/py/latest/Sandbox#create`,children:(e,t)=>{i(e,vt())},$$slots:{default:!0}}),o(),n(Y);var X=a(Y,2),Jn=a(e(X));u(Jn,{href:`/docs/cli/latest/skills`,children:(e,t)=>{i(e,yt())},$$slots:{default:!0}});var Yn=a(Jn,2);u(Yn,{href:`/docs/cli/latest/skills#modal-skills-install`,children:(e,t)=>{i(e,bt())},$$slots:{default:!0}}),u(a(Yn,2),{href:`/docs/cli/latest/skills#modal-skills-update`,children:(e,t)=>{i(e,xt())},$$slots:{default:!0}}),o(),n(X);var Z=a(X,2),Xn=a(e(Z));u(Xn,{href:`/docs/sdk/py/latest/Workspace`,children:(e,t)=>{i(e,St())},$$slots:{default:!0}}),u(a(Xn,2),{href:`/docs/sdk/py/latest/Workspace#memberslist`,children:(e,t)=>{i(e,Ct())},$$slots:{default:!0}}),o(),n(Z);var Q=a(Z,2);u(a(e(Q)),{href:`/docs/cli/latest`,children:(e,t)=>{i(e,wt())},$$slots:{default:!0}}),o(3),n(Q);var Zn=a(Q,2),Qn=a(e(Zn));u(Qn,{href:`/docs/sdk/py/latest/Sandbox#filesystemwatch`,children:(e,t)=>{i(e,Tt())},$$slots:{default:!0}}),u(a(Qn,2),{href:`/docs/sdk/py/latest/Sandbox#watch`,children:(e,t)=>{i(e,Et())},$$slots:{default:!0}}),o(),n(Zn);var $n=a(Zn,2),er=a(e($n)),tr=e(er),nr=e(tr);u(nr,{href:`/docs/sdk/py/latest/Sandbox#snapshot_filesystem`,children:(e,t)=>{i(e,Dt())},$$slots:{default:!0}}),u(a(nr,2),{href:`/docs/sdk/py/latest/Sandbox#snapshot_directory`,children:(e,t)=>{i(e,Ot())},$$slots:{default:!0}}),o(9),n(tr);var rr=a(tr,2),ir=e(rr);u(ir,{href:`/docs/sdk/py/latest/Sandbox#snapshot_directory`,children:(e,t)=>{i(e,kt())},$$slots:{default:!0}});var ar=a(ir,6);u(ar,{href:`/docs/sdk/py/latest/Sandbox#snapshot_filesystem`,children:(e,t)=>{i(e,At())},$$slots:{default:!0}}),u(a(ar,2),{href:`/docs/sdk/py/latest/exception#timeouterror`,children:(e,t)=>{i(e,jt())},$$slots:{default:!0}}),o(3),n(rr),n(er),n($n);var or=a($n,2),sr=a(e(or),5);u(sr,{href:`/docs/sdk/py/latest/Volume`,children:(e,t)=>{i(e,Mt())},$$slots:{default:!0}});var cr=a(sr,2);u(cr,{href:`/docs/sdk/py/latest/Volume#objectsdelete`,children:(e,t)=>{i(e,Nt())},$$slots:{default:!0}}),u(a(cr,2),{href:`/docs/sdk/py/latest/Volume#objectscreate`,children:(e,t)=>{i(e,Pt())},$$slots:{default:!0}}),o(),n(or),n(W);var lr=a(W,2);s(lr,{id:`14`,children:(e,t)=>{o(),i(e,r(`1.4`))},$$slots:{default:!0}});var ur=a(lr,2);c(ur,{id:`143-2026-05-18`,children:(e,t)=>{o(),i(e,r(`1.4.3 (2026-05-18)`))},$$slots:{default:!0}});var dr=a(ur,2),fr=e(dr);u(a(e(fr)),{href:`https://modal.com/docs/guide/region-selection#regional-routing`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`“regional routing”`))},$$slots:{default:!0}}),o(16),n(fr);var pr=a(fr,2);u(a(e(pr),5),{href:`https://modal.com/docs/guide/rbac`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`RBAC`))},$$slots:{default:!0}}),o(),n(pr);var mr=a(pr,8);u(a(e(mr),3),{href:`https://modal.com/docs/guide/sandbox-files`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Filesystem API`))},$$slots:{default:!0}}),o(2),n(mr),o(14),n(dr);var hr=a(dr,2);c(hr,{id:`142-2026-04-16`,children:(e,t)=>{o(),i(e,r(`1.4.2 (2026-04-16)`))},$$slots:{default:!0}});var gr=a(hr,4);c(gr,{id:`141-2026-03-30`,children:(e,t)=>{o(),i(e,r(`1.4.1 (2026-03-30)`))},$$slots:{default:!0}});var _r=a(gr,2),vr=e(_r);l(a(e(vr),9),{code:`app%20%3D%20modal.App.lookup('sandbox-app'%2C%20create_if_missing%3DTrue)%0Aprobe%20%3D%20modal.Probe.with_tcp(8080)%0Asb%20%3D%20modal.Sandbox.create(%0A%20%20%20%20%22python3%22%2C%20%22-m%22%2C%20%22http.server%22%2C%20%228080%22%2C%0A%20%20%20%20readiness_probe%3Dprobe%2C%0A%20%20%20%20app%3Dapp%2C%0A)%0Asb.wait_until_ready()`,lang:`python`}),n(vr),o(6),n(_r);var yr=a(_r,2);c(yr,{id:`140-2026-03-25`,children:(e,t)=>{o(),i(e,r(`1.4.0 (2026-03-25)`))},$$slots:{default:!0}});var br=a(yr,6);u(a(e(br)),{href:`https://modal.com/docs/guide/sandbox-files`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Sandbox filesystem API`))},$$slots:{default:!0}}),o(),n(br);var xr=a(br,10),Sr=e(xr);u(a(e(Sr),9),{href:`https://modal.com/docs/guide/oidc-integration`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`OIDC integration guide`))},$$slots:{default:!0}}),o(),n(Sr),o(10),n(xr);var Cr=a(xr,6);s(Cr,{id:`13`,children:(e,t)=>{o(),i(e,r(`1.3`))},$$slots:{default:!0}});var wr=a(Cr,2);c(wr,{id:`135-2026-03-03`,children:(e,t)=>{o(),i(e,r(`1.3.5 (2026-03-03)`))},$$slots:{default:!0}});var Tr=a(wr,4);c(Tr,{id:`134-2026-02-23`,children:(e,t)=>{o(),i(e,r(`1.3.4 (2026-02-23)`))},$$slots:{default:!0}});var Er=a(Tr,2),Dr=e(Er),Or=a(e(Dr),2);l(Or,{code:`sb%20%3D%20modal.Sandbox.create(app%3Dapp)%0Asnapshot%20%3D%20sb.snapshot_directory(%22%2Fproject%22)%0A%0Asb2%20%3D%20modal.Sandbox.create(app%3Dapp)%0Asb2.mount_image(%22%2Fproject%22%2C%20snapshot)`,lang:`python`});var kr=a(Or,2);u(a(e(kr)),{href:`https://modal.com/docs/guide/sandbox-snapshots`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Sandbox Snapshot guide`))},$$slots:{default:!0}}),o(),n(kr),n(Dr),o(8),n(Er);var Ar=a(Er,2);c(Ar,{id:`133-2026-02-12`,children:(e,t)=>{o(),i(e,r(`1.3.3 (2026-02-12)`))},$$slots:{default:!0}});var jr=a(Ar,2),Mr=a(e(jr),4);u(a(e(Mr)),{href:`https://modal.com/docs/guide/async`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`blocking interface on a Modal object`))},$$slots:{default:!0}}),o(7),n(Mr),o(2),n(jr);var Nr=a(jr,2);c(Nr,{id:`132-2026-01-30`,children:(e,t)=>{o(),i(e,r(`1.3.2 (2026-01-30)`))},$$slots:{default:!0}});var Pr=a(Nr,2),Fr=e(Pr);l(a(e(Fr),3),{code:`fc%20%3D%20f.spawn()%0Aprint(fc.get_dashboard_url())%20%20%23%20Easy%20access%20to%20logs%2C%20etc.`,lang:`python`}),n(Fr);var Ir=a(Fr,2);l(a(e(Ir),7),{code:`modal%20dashboard%20%20%23%20Opens%20up%20the%20Apps%20homepage%20for%20the%20current%20environment%0Amodal%20dashboard%20%3Cobject-id%3E%20%20%23%20Opens%20up%20a%20view%20of%20this%20object%0Amodal%20app%20dashboard%20%3Capp-name%3E%20%20%23%20Opens%20up%20the%20dashboard%20for%20this%20deployed%20App%0Amodal%20volume%20dashboard%20%3Cvolume-name%3E%20%20%23%20Opens%20up%20the%20file%20browser%20for%20this%20persistent%20Volume`,lang:`bash`}),n(Ir),o(6),n(Pr);var Lr=a(Pr,2);c(Lr,{id:`131-2026-01-22`,children:(e,t)=>{o(),i(e,r(`1.3.1 (2026-01-22)`))},$$slots:{default:!0}});var Rr=a(Lr,2),zr=e(Rr),Br=a(e(zr)),Vr=a(e(Br),2);l(a(e(Vr),5),{code:`modal.Image.from_registry(%22debian%3Abookworm-slim%22%2C%20add_python%3D%223.14t%22)`,lang:`python`}),n(Vr);var Hr=a(Vr,2);u(a(e(Hr)),{href:`https://modal.com/settings/image-config`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Image Builder Version`))},$$slots:{default:!0}}),o(),n(Hr),o(2),n(Br),n(zr);var Ur=a(zr,2);l(a(e(Ur),5),{code:`sb%20%3D%20modal.Sandbox.create(...%2C%20custom_domain%3D%22sandboxes.mydomain.com%22)`,lang:`python`}),o(),n(Ur),o(10),n(Rr);var Wr=a(Rr,2);c(Wr,{id:`130-2025-12-19`,children:(e,t)=>{o(),i(e,r(`1.3.0 (2025-12-19)`))},$$slots:{default:!0}});var Gr=a(Wr,2);u(a(e(Gr)),{href:`https://devguide.python.org/versions`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`end-of-life`))},$$slots:{default:!0}}),o(),n(Gr);var Kr=a(Gr,6),qr=e(Kr);u(a(e(qr),15),{href:`https://modal.com/docs/sdk/py/latest/exception`,rel:`nofollow`,children:(e,t)=>{i(e,Ft())},$$slots:{default:!0}}),o(),n(qr);var Jr=a(qr,8);u(a(e(Jr),3),{href:`https://modal.com/docs/guide/images#image-builder-updates`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Image Builder Version`))},$$slots:{default:!0}}),o(),n(Jr),n(Kr);var Yr=a(Kr,2);s(Yr,{id:`12`,children:(e,t)=>{o(),i(e,r(`1.2`))},$$slots:{default:!0}});var Xr=a(Yr,2);c(Xr,{id:`126-2025-12-16`,children:(e,t)=>{o(),i(e,r(`1.2.6 (2025-12-16)`))},$$slots:{default:!0}});var Zr=a(Xr,4);c(Zr,{id:`125-2025-12-12`,children:(e,t)=>{o(),i(e,r(`1.2.5 (2025-12-12)`))},$$slots:{default:!0}});var Qr=a(Zr,4);c(Qr,{id:`124-2025-11-21`,children:(e,t)=>{o(),i(e,r(`1.2.4 (2025-11-21)`))},$$slots:{default:!0}});var $r=a(Qr,4);c($r,{id:`123-2025-11-20`,children:(e,t)=>{o(),i(e,r(`1.2.3 (2025-11-20)`))},$$slots:{default:!0}});var ei=a($r,2),ti=e(ei);u(a(e(ti),7),{href:`https://modal.com/docs/guide/preemption`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Guide`))},$$slots:{default:!0}}),o(),n(ti),o(8),n(ei);var ni=a(ei,2);c(ni,{id:`122-2025-11-10`,children:(e,t)=>{o(),i(e,r(`1.2.2 (2025-11-10)`))},$$slots:{default:!0}});var ri=a(ni,2),ii=e(ri);l(a(e(ii),2),{code:`cache_vol%20%3D%20modal.Volume.from_name(%22cache-mount%22)%0Acmd_using_cache%20%3D%20%22...%22%0Aimage%20%3D%20modal.Image.debian_slim().run_commands(cmd_using_cache%2C%20volumes%3D%7B%22%2Fcache%22%3A%20cache_vol%7D)`,lang:`python`}),n(ii);var ai=a(ii,6),oi=e(ai);u(a(e(oi),4),{href:`https://github.com/awslabs/mountpoint-s3/blob/main/doc/CONFIGURATION.md#endpoints-and-aws-privatelink`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`mountpoint-s3 endpoints docs`))},$$slots:{default:!0}}),o(),n(oi),n(ai),o(4),n(ri);var si=a(ri,2);c(si,{id:`121-2025-10-22`,children:(e,t)=>{o(),i(e,r(`1.2.1 (2025-10-22)`))},$$slots:{default:!0}});var ci=a(si,4);c(ci,{id:`120-2025-10-09`,children:(e,t)=>{o(),i(e,r(`1.2.0 (2025-10-09)`))},$$slots:{default:!0}});var li=a(ci,4);l(li,{code:`app%20%3D%20modal.App(%22llm-inference-server%22%2C%20tags%3D%7B%22team%22%3A%20%22genai-platform%22%7D)`,lang:`python`});var ui=a(li,12),di=e(ui),fi=a(e(di),2);l(fi,{code:`sb%20%3D%20modal.Sandbox.create(...)%0A%0A%23%20Create%20a%20connect%20token%2C%20optionally%20including%20arbitrary%20user%20metadata%0Acreds%20%3D%20sb.create_connect_token(user_metadata%3D%7B%22user_id%22%3A%20%22user123%22%7D)%0A%0A%23%20Make%20an%20http%20request%2C%20passing%20the%20token%20in%20the%20authorization%20header%0Arequests.get(creds.url%2C%20headers%3D%7B%22Authorization%22%3A%20f%22Bearer%20%7Bcreds.token%7D%22%7D)`,lang:`python`});var pi=a(fi,2);u(a(e(pi)),{href:`https://modal.com/docs/guide/sandbox-networking`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Sandbox Networking guide`))},$$slots:{default:!0}}),o(),n(pi),n(di);var mi=a(di,2);l(a(e(mi),2),{code:`app%20%3D%20modal.App.lookup(%22sandbox-app%22)%0Aimage%20%3D%20modal.Image.from_registry(%22ubuntu%22)%0A%0A%23%20This%20step%20will%20block%20until%20the%20build%20completes%0Aimage.build(app)%0A%0A%23%20Now%20the%20Sandbox%20will%20be%20created%20and%20scheduled%20immediately%0Asb%20%3D%20modal.Sandbox.create(app%3Dapp%2C%20image%3Dimage)`,lang:`python`}),n(mi),o(18),n(ui);var hi=a(ui,10);s(hi,{id:`11`,children:(e,t)=>{o(),i(e,r(`1.1`))},$$slots:{default:!0}});var gi=a(hi,2);c(gi,{id:`114-2025-09-03`,children:(e,t)=>{o(),i(e,r(`1.1.4 (2025-09-03)`))},$$slots:{default:!0}});var _i=a(gi,4);c(_i,{id:`113-2025-08-19`,children:(e,t)=>{o(),i(e,r(`1.1.3 (2025-08-19)`))},$$slots:{default:!0}});var vi=a(_i,4);c(vi,{id:`112-2025-08-14`,children:(e,t)=>{o(),i(e,r(`1.1.2 (2025-08-14)`))},$$slots:{default:!0}});var yi=a(vi,4),bi=e(yi),xi=a(e(bi),2);u(xi,{href:`https://modal.com/docs/sdk/py/latest/Volume#create`,rel:`nofollow`,children:(e,t)=>{i(e,It())},$$slots:{default:!0}}),l(a(xi,2),{code:`modal.Volume.objects.create(%22huggingface-cache%22%2C%20environment_name%3D%22dev%22)`,lang:`python`}),n(bi);var Si=a(bi,2),Ci=a(e(Si),2);u(Ci,{href:`https://modal.com/docs/sdk/py/latest/Secret#delete`,rel:`nofollow`,children:(e,t)=>{i(e,Lt())},$$slots:{default:!0}}),l(a(Ci,2),{code:`modal.Secret.objects.delete(%22aws-token%22)`,lang:`python`}),n(Si);var wi=a(Si,2),Ti=a(e(wi),2);u(Ti,{href:`https://modal.com/docs/sdk/py/latest/Queue#list`,rel:`nofollow`,children:(e,t)=>{i(e,Rt())},$$slots:{default:!0}}),l(a(Ti,2),{code:`for%20queue%20in%20modal.Queue.objects.list()%3A%0A%20%20%20%20queue_info%20%3D%20queue.info()%0A%20%20%20%20print(queue_info.name%2C%20queue_info.created_at%2C%20queue.len())`,lang:`python`}),n(wi),n(yi);var Ei=a(yi,10);c(Ei,{id:`111-2025-08-01`,children:(e,t)=>{o(),i(e,r(`1.1.1 (2025-08-01)`))},$$slots:{default:!0}});var Di=a(Ei,2);u(a(e(Di),7),{href:`https://modal.com/docs/guide/sandbox#named-sandboxes`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`guide`))},$$slots:{default:!0}}),o(),n(Di);var Oi=a(Di,6);c(Oi,{id:`110-2025-07-17`,children:(e,t)=>{o(),i(e,r(`1.1.0 (2025-07-17)`))},$$slots:{default:!0}});var ki=a(Oi,2),Ai=a(e(ki),3);u(Ai,{href:`https://modal.com/docs/guide/images#image-builder-updates`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Image Builder Version`))},$$slots:{default:!0}}),u(a(Ai,2),{href:`https://modal.com/settings/image-config`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`Image Config`))},$$slots:{default:!0}}),o(),n(ki);var ji=a(ki,2),Mi=a(e(ji));u(Mi,{href:`https://docs.astral.sh/uv/`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`uv package manager`))},$$slots:{default:!0}});var Ni=a(Mi,2);u(Ni,{href:`https://modal.com/docs/sdk/py/latest/Image#uv_pip_install`,rel:`nofollow`,children:(e,t)=>{i(e,zt())},$$slots:{default:!0}}),u(a(Ni,2),{href:`https://modal.com/docs/sdk/py/latest/Image#uv_sync`,rel:`nofollow`,children:(e,t)=>{i(e,Bt())},$$slots:{default:!0}}),o(),n(ji);var Pi=a(ji,2);l(Pi,{code:`import%20modal%0A%0A%23%20uv_pip_install%20accepts%20a%20list%20of%20packages%2C%20like%20pip_install%2C%20but%20up%20to%2050%25%20faster%0Aimage%20%3D%20modal.Image.debian_slim().uv_pip_install(%22torch%3D%3D2.7.1%22%2C%20%22numpy%3D%3D2.3.1%22)%0A%0A%23%20uv_sync%20accepts%20a%20local%20%60uv_project_dir%60%20(defaulting%20to%20the%20local%20working%20directory)%0A%23%20and%20uses%20the%20pyproject.toml%20and%20uv.lock%20files%20to%20specify%20the%20environment%0Aimage%20%3D%20modal.Image.debian_slim().uv_sync()`,lang:`python`});var Fi=a(Pi,6),Ii=a(e(Fi),4);u(a(e(Ii)),{href:`https://modal.com/docs/sdk/py/latest/Sandbox#reload_volumes`,rel:`nofollow`,children:(e,t)=>{i(e,Vt())},$$slots:{default:!0}}),o(),n(Ii),o(10),n(Fi);var Li=a(Fi,6);s(Li,{id:`10`,children:(e,t)=>{o(),i(e,r(`1.0`))},$$slots:{default:!0}});var Ri=a(Li,2);c(Ri,{id:`105-2025-06-27`,children:(e,t)=>{o(),i(e,r(`1.0.5 (2025-06-27)`))},$$slots:{default:!0}});var zi=a(Ri,2),Bi=e(zi),$=e(Bi);u(a(e($)),{href:`/docs/sdk/py/latest/Volume#read_only`,children:(e,t)=>{i(e,Ht())},$$slots:{default:!0}}),o(),n($),l(a($,2),{code:`vol%20%3D%20modal.Volume.from_name(%22models%22)%0Aread_only_vol%20%3D%20vol.read_only()%0A%0A%40app.function(volumes%3D%7B%22%2Fmodels%22%3A%20read_only_vol%7D)%0Adef%20f()%3A%0A%20%20%20%20with%20open(%22%2Fmodels%2Fweights.pt%22%2C%20%22w%22)%20as%20fid%3A%20%20%23%20Raises%20an%20OSError%0A%20%20%20%20%20%20%20%20...%0A%0A%40app.local_entrypoint()%0Adef%20main()%3A%0A%20%20%20%20with%20read_only_vol.batch_upload()%20as%20batch%3A%20%20%23%20Raises%20a%20modal.exceptions.InvalidError%0A%20%20%20%20%20%20%20%20...%0A%0A%20%20%20%20with%20vol.batch_upload()%20as%20batch%3A%20%20%23%20This%20instance%20is%20still%20writeable%0A%20%20%20%20%20%20%20%20...`,lang:`python`}),n(Bi),o(8),n(zi);var Vi=a(zi,2);c(Vi,{id:`104-2025-06-13`,children:(e,t)=>{o(),i(e,r(`1.0.4 (2025-06-13)`))},$$slots:{default:!0}});var Hi=a(Vi,2),Ui=e(Hi);l(a(e(Ui),3),{code:`Model.with_options(gpu%3D%22A100%22%2C%20cpu%3D16).with_options(gpu%3D%22H100%22)`,lang:`python`}),n(Ui);var Wi=a(Ui,2);l(a(e(Wi),5),{code:`modal%20shell%20--secret%20huggingface%20--secret%20wandb`,lang:`text`}),n(Wi),o(4),n(Hi);var Gi=a(Hi,2);c(Gi,{id:`103-2025-06-05`,children:(e,t)=>{o(),i(e,r(`1.0.3 (2025-06-05)`))},$$slots:{default:!0}});var Ki=a(Gi,2),qi=e(Ki);l(a(e(qi),2),{code:`import%20modal%0Aapp%20%3D%20modal.App()%0A%0A%40app.function(schedule%3Dmodal.Cron(%22*%206%20*%20*%20*%22)%2C%20timezone%3D%22America%2FNew_York%22)%20%20%23%20Use%20tz%20database%20naming%20conventions%0Adef%20f()%3A%0A%20%20%20%20print(%22This%20function%20will%20run%20every%20day%20at%206am%20New%20York%20time.%22)`,lang:`python`}),n(qi);var Ji=a(qi,2);l(a(e(Ji),2),{code:`sb%20%3D%20modal.Sandbox.create(app%3Dapp%2C%20h2_ports%20%3D%20%5B5002%5D%2C%20encrypted_ports%20%3D%20%5B5003%5D)`,lang:`python`}),n(Ji),o(12),n(Ki);var Yi=a(Ki,2);c(Yi,{id:`102-2025-05-26`,children:(e,t)=>{o(),i(e,r(`1.0.2 (2025-05-26)`))},$$slots:{default:!0}});var Xi=a(Yi,4);c(Xi,{id:`101-2025-05-19`,children:(e,t)=>{o(),i(e,r(`1.0.1 (2025-05-19)`))},$$slots:{default:!0}});var Zi=a(Xi,4);c(Zi,{id:`100-2025-05-16`,children:(e,t)=>{o(),i(e,r(`1.0.0 (2025-05-16)`))},$$slots:{default:!0}});var Qi=a(Zi,2);u(a(e(Qi)),{href:`https://modal.com/docs/guide/modal-1-0-migration`,rel:`nofollow`,children:(e,t)=>{o(),i(e,r(`1.0 migration guide`))},$$slots:{default:!0}}),o(5),n(Qi);var $i=a(Qi,8),ea=e($i);l(a(e(ea),7),{code:`%40app.function(image%3Dimage%2C%20mounts%3D%5Bmodal.Mount.from_local_dir(%22data%22%2C%20%22%2Froot%2Fdata%22)%5D)%20%20%23%20This%20is%20now%20an%20error!%0A%40app.function(image%3Dimage.add_local_dir(%22data%22%2C%20%22%2Froot%2Fdata%22))%20%20%23%20Correct%20spelling`,lang:`python`}),n(ea);var ta=a(ea,2);l(a(e(ta),7),{code:`with%20modal.enable_output()%2C%20app.run()%3A%0A%20%20...%20%20%23%20Will%20produce%20verbose%20Modal%20output`,lang:`python`}),n(ta);var na=a(ta,2);l(a(e(na),7),{code:`image.pip_install(%22flash-attn%22%2C%20%22--no-build-isolation%22)%20%20%23%20This%20is%20now%20an%20error!%0Aimage.pip_install(%22flash-attn%22%2C%20extra_options%3D%22--no-build-isolation%22)%20%20%23%20Correct%20spelling`,lang:`python`}),n(na);var ra=a(na,2);l(a(e(ra),7),{code:`f%20%3D%20modal.Function.from_name(%22my-app%22%2C%20tag%3D%22f%22)%20%20%23%20No%20longer%20supported!%20Will%20raise%20an%20error!%0Af%20%3D%20modal.Function.from_name(%22my-app%22%2C%20%22f%22)%20%20%23%20Preferred%20spelling`,lang:`python`}),n(ra),o(8),n($i),i(t,te)},$$slots:{default:!0}}))}export{p as default,d as metadata};
//# sourceMappingURL=BGUJKeIi2.js.map
