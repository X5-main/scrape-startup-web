artemissecurity.com — mirrored 2026-08-24 (D47) via mirror_site.py into artemissecurity.com/. Site is a WordPress site (wp-json REST endpoints, xmlrpc, category/tag feeds; `__ver-`-suffixed assets) — "Artemis Security | AI-Native Protection Platform for Modern SecOps": "Traditional SIEMs weren't built for today's world. Artemis closes that gap with an AI-native Protection Platform designed to dramatically reduce MTTD and MTTR for modern threats." Funding gate: leadprysm funded-month feed (april-2026) row "Artemis Security" — seed round announced April 2026 = funded ≤24 months ✓; AI focus ✓. Canonical host https://www.artemissecurity.com/ (mirror dir uses the apex hostname). robots.txt allowed crawling. D47 binding: won the round-2 hero-craft tiebreak vs firmable.com — full-page 7/10 + hero 7/10 under the pipeline-standard rubric (strict craft rubric AD6 / TY7 / BS6 / OA6). Screening: design_signal.py score 6.2 → full-page flush capture → prelim 7/10.

Mirror completion (D47):
- 213 HTML pages on disk. Crawl: mirror_site.py BFS from home, plus targeted `--depth 1` runs for `/ai` and `/customers` (first probe 9/14 MATCH with 3 DIFFs — those two served replica "Error response", missing index.html; fixed), plus 13 tag pages mirrored, plus 32 wp-json REST endpoints + 14 tag feeds + 5 category feeds (`blog/{ai,customers,research,security,product,company-news}/feed/`) + attack-stories feed vendored from exact live bodies (JSON/XML saved as index.html at resolved paths). Served via serve_replica.py on 8904.
- af_parity battery: 26/26 routes byte-exact MATCH (innerText/lines identical). Post-fix re-check of / /ai /customers /tag/ai /tag/anthropic /blog/company-news — all "title == | h1 == | innerText == | lines == | MATCH".
- Pixel/vision parity: full-page pair (fab_cdp_shot.mjs 9344, scale 3, 3840×24606 raw both sides): stdlib-PNG decoder diff — mean abs diff 3.92/255, 0.90% px above threshold, diffs confined to rows 1107–4685 (hero/testimonial band ≈ CSS y 369–1562, animation shimmer); vision reads both bands identically (same headline, testimonial grid, Complianz cookie banner). Root-height anomaly resolved: first re-check showed live 7498 vs replica 8202 — live-side Complianz consent-banner collapse in the shared CDP profile, not a replica defect; fresh same-session pair → both 24606 again.
- Netdiff sweep (pages=213, html-refs=8519): failures 78 → 33 → 32 (fixes: 13 tag pages; 32 wp-json REST + 14 tag feeds + 5 category feeds). FINAL residual 32 = 28 oembed (live 404) + 3 xmlrpc (live 403) + 1 attack-stories feed (live 404) — every one verified live non-200 = EXACT dead-live set → acceptance gate MET (same-origin failures == live non-200 set; zero serve defects).
- Vision judging: full-page 7/10; hero 7/10 (standard rubric). D47 selection chain: 45 candidates (april+march-2026 leadprysm pools) → design_signal ≥4.5 → 20 judged captures → 14 flat 7/10 custom → 8 round-2 heroes → standard-rubric re-judge (sett 6, firmable 7, artemissecurity 7, mosaic 6) → winner artemissecurity.com.
