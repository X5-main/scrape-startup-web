# avelin-agentic (renamed)

> **`avelin-agentic` is now [`avelin-agentic-pro`](avelin-agentic-pro.md).** This page moved when the model lineup adopted the uniform fast/pro/ultra naming scheme.

The `avelin-agentic` model name keeps working indefinitely as a server-side alias of `avelin-agentic-pro` — existing integrations need no changes. Requests sent with either name are served and billed identically.

See the [`avelin-agentic-pro` model page](avelin-agentic-pro.md) for current capabilities and pricing, or the [Model Catalog](README.md#legacy-model-names) for the full legacy-name table.
