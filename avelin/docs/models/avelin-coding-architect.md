# avelin-coding-architect (renamed)

> **`avelin-coding-architect` is now [`avelin-coding-ultra`](avelin-coding-ultra.md).** This page moved when the model lineup adopted the uniform fast/pro/ultra naming scheme.

The `avelin-coding-architect` model name keeps working indefinitely as a server-side alias of `avelin-coding-ultra` — existing integrations need no changes. Requests sent with either name are served and billed identically.

See the [`avelin-coding-ultra` model page](avelin-coding-ultra.md) for current capabilities and pricing, or the [Model Catalog](README.md#legacy-model-names) for the full legacy-name table.
