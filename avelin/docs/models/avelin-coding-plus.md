# avelin-coding-plus (renamed)

> **`avelin-coding-plus` is now [`avelin-coding-pro`](avelin-coding-pro.md).** This page moved when the model lineup adopted the uniform fast/pro/ultra naming scheme.

The `avelin-coding-plus` model name keeps working indefinitely as a server-side alias of `avelin-coding-pro` — existing integrations need no changes. Requests sent with either name are served and billed identically.

See the [`avelin-coding-pro` model page](avelin-coding-pro.md) for current capabilities and pricing, or the [Model Catalog](README.md#legacy-model-names) for the full legacy-name table.
