# avelin-coding (renamed)

> **`avelin-coding` is now [`avelin-coding-fast`](avelin-coding-fast.md).** This page moved when the model lineup adopted the uniform fast/pro/ultra naming scheme.

The `avelin-coding` model name keeps working indefinitely as a server-side alias of `avelin-coding-fast` — existing integrations need no changes. Requests sent with either name are served and billed identically.

See the [`avelin-coding-fast` model page](avelin-coding-fast.md) for current capabilities and pricing, or the [Model Catalog](README.md#legacy-model-names) for the full legacy-name table.
